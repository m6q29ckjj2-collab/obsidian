#flashquiz/proga/js/spec/7

---

> [!question] В каком сценарии наиболее точно проявляется различие между ECMAScript-спецификацией и Host-Environment в отношении глобальных API?  
> a) ECMAScript определяет поведение всех глобальных функций, включая таймеры и DOM  
> b) Host может добавлять глобальные идентификаторы, которые не существуют в спецификации, но участвуют в Resolution Identifier  
> c) ECMAScript запрещает расширение глобального объекта в любом окружении  
> d) Все глобальные API всегда описаны в разделе Annex B спецификации
> 
> > [!success]- Answer  
> > b) Host может добавлять глобальные идентификаторы, которые не существуют в спецификации, но участвуют в Resolution Identifier  
> > Explanation:  
> > Host-Environment (например браузер) расширяет глобальное окружение через собственные bindings, такие как setTimeout или fetch.  
> > a неверен: ECMAScript не описывает DOM и таймеры.  
> > c неверен: расширение глобального объекта — нормальная практика Host.  
> > d неверен: Annex B не охватывает все host-API и не является обязательной частью ядра.

---

> [!question] Почему выполнение `setTimeout(() => {}, 0)` не означает “немедленное выполнение” с точки зрения Execution Context Stack?  
> a) Потому что callback выполняется внутри текущего Execution Context  
> b) Потому что callback помещается в очередь задач и выполняется только после освобождения стека  
> c) Потому что setTimeout синхронно блокирует поток на 0 миллисекунд  
> d) Потому что движок преобразует setTimeout в Promise microtask
> 
> > [!success]- Answer  
> > b) Потому что callback помещается в очередь задач и выполняется только после освобождения стека  
> > Explanation:  
> > setTimeout планирует макрозадачу, которая выполняется после завершения текущего Execution Context Stack.  
> > a неверен: callback не выполняется в текущем контексте.  
> > c неверен: блокировки нет.  
> > d неверен: setTimeout не относится к microtask queue.

---

> [!question] Что наиболее точно описывает роль Event Loop в модели выполнения JavaScript?  
> a) Он управляет компиляцией JS-кода в байткод  
> b) Он синхронизирует выполнение Execution Context Stack и очередей задач  
> c) Он является частью ECMAScript-спецификации  
> d) Он выполняет только Promise microtasks
> 
> > [!success]- Answer  
> > b) Он синхронизирует выполнение Execution Context Stack и очередей задач  
> > Explanation:  
> > Event Loop координирует выполнение стеков и очередей (macrotasks/microtasks).  
> > a неверен: компиляция — задача движка, не event loop.  
> > c неверен: event loop не является частью ECMAScript.  
> > d неверен: он обрабатывает не только microtasks.

---

> [!question] Какой механизм наиболее точно объясняет, почему переменные `let` недоступны до строки объявления?  
> a) Они не hoisted вообще  
> b) Они находятся в TDZ до момента инициализации binding’а в Environment Record  
> c) Они удаляются оптимизатором до исполнения  
> d) Они хранятся в отдельном Execution Context, недоступном из текущего
> 
> > [!success]- Answer  
> > b) Они находятся в TDZ до момента инициализации binding’а в Environment Record  
> > Explanation:  
> > TDZ (Temporal Dead Zone) — состояние binding’а до инициализации.  
> > a неверен: hoisting есть, но без инициализации.  
> > c неверен: ничего не удаляется.  
> > d неверен: нет отдельного Execution Context.

---

> [!question] Что наиболее точно описывает поведение `var` в глобальном контексте в браузере?  
> a) Он создаёт переменную только в Environment Record  
> b) Он создаёт property на global object (window)  
> c) Он создаёт closure внутри global Execution Context  
> d) Он становится частью prototype chain глобального объекта
> 
> > [!success]- Answer  
> > b) Он создаёт property на global object (window)  
> > Explanation:  
> > var в global scope также создаёт property глобального объекта в браузере.  
> > a неверен: это не только Environment Record.  
> > c неверен: closure здесь не участвует.  
> > d неверен: prototype chain не используется для var binding.

---

> [!question] Почему выражение `console.log(typeof undeclaredVariable)` не вызывает ReferenceError?  
> a) Потому что typeof всегда подавляет ошибки доступа  
> b) Потому что Identifier Resolution возвращает специальное значение при отсутствующем binding  
> c) Потому что JS создаёт временный binding со значением undefined  
> d) Потому что переменные всегда предварительно объявляются в глобальном scope
> 
> > [!success]- Answer  
> > b) Потому что Identifier Resolution возвращает специальное значение при отсутствующем binding  
> > Explanation:  
> > typeof имеет особое поведение при unresolved references.  
> > a неверен: не “подавление ошибок”, а специфицированное правило resolution.  
> > c неверен: binding не создаётся.  
> > d неверен: переменная не существует в scope.

---

> [!question] Какое утверждение наиболее точно отражает связь между Promise microtasks и Execution Context Stack?  
> a) Microtasks выполняются параллельно Execution Context Stack  
> b) Microtasks выполняются только внутри текущего Execution Context  
> c) Microtasks выполняются после завершения текущего Execution Context Stack, но до следующего macrotask  
> d) Microtasks заменяют Event Loop в современных движках
> 
> > [!success]- Answer  
> > c) Microtasks выполняются после завершения текущего Execution Context Stack, но до следующего macrotask  
> > Explanation:  
> > Microtasks flush происходят между фазами event loop.  
> > a неверен: нет параллельного выполнения.  
> > b неверен: они не выполняются внутри текущего стека.  
> > d неверен: event loop остаётся ключевым механизмом.

---

> [!question] Почему операция `await` не может быть эквивалентна простому `Promise.then` с точки зрения спецификации?  
> a) Потому что await блокирует поток выполнения  
> b) Потому что await создаёт новый Execution Context  
> c) Потому что await приостанавливает текущий Execution Context и сохраняет его состояние  
> d) Потому что await выполняется только в microtask queue
> 
> > [!success]- Answer  
> > c) Потому что await приостанавливает текущий Execution Context и сохраняет его состояние  
> > Explanation:  
> > await семантически suspends execution context, а не просто добавляет then-handler.  
> > a неверен: поток не блокируется.  
> > b неверен: новый контекст не создаётся.  
> > d неверен: await не является microtask сам по себе.

---
> [!question] Что наиболее точно описывает различие между Realm и Execution Context в ECMAScript?  
> a) Realm и Execution Context — это разные имена одной и той же структуры выполнения  
> b) Realm определяет изолированную среду со своими intrinsic objects, тогда как Execution Context описывает текущее выполнение кода  
> c) Realm существует только в браузере и не применяется в Node.js  
> d) Execution Context хранит глобальные объекты, а Realm — только локальные переменные
> 
> > [!success]- Answer  
> > b) Realm определяет изолированную среду со своими intrinsic objects, тогда как Execution Context описывает текущее выполнение кода  
> > Explanation:  
> > Realm — это изолированная среда с собственными встроенными объектами (intrinsics), глобальным объектом и стандартной библиотекой. Execution Context — это состояние выполнения кода.  
> > a неверен: это разные уровни абстракции.  
> > c неверен: Realm есть и в браузере, и в Node.js.  
> > d неверен: Realm не ограничен локальными переменными.

---

> [!question] Почему два iframe в браузере могут иметь разные результаты для `Array.prototype.push === window.Array.prototype.push`?  
> a) Потому что каждый iframe создаёт собственный Realm с независимыми intrinsics  
> b) Потому что Array.prototype копируется при создании iframe  
> c) Потому что прототипы всегда клонируются между окнами  
> d) Потому что window глобально шарит все объекты через event loop
> 
> > [!success]- Answer  
> > a) Потому что каждый iframe создаёт собственный Realm с независимыми intrinsics  
> > Explanation:  
> > Каждый iframe имеет свой Realm, включая собственные встроенные объекты и прототипы. Поэтому ссылки на Array.prototype различаются.  
> > b неверен: копирования нет.  
> > c неверен: прототипы не клонируются.  
> > d неверен: event loop не влияет на identity объектов.

---

> [!question] Какой механизм наиболее точно объясняет поведение TDZ при обращении к переменной до её объявления?  
> a) Переменная существует, но имеет значение undefined  
> b) Binding существует, но находится в неинициализированном состоянии в Environment Record  
> c) Переменная удаляется до выполнения кода оптимизатором  
> d) Identifier Resolution возвращает null вместо ReferenceError
> 
> > [!success]- Answer  
> > b) Binding существует, но находится в неинициализированном состоянии в Environment Record  
> > Explanation:  
> > TDZ — это состояние binding’а до инициализации, не отсутствие переменной.  
> > a неверен: undefined появляется только после инициализации.  
> > c неверен: ничего не удаляется.  
> > d неверен: возникает ReferenceError, а не null.

---

> [!question] Что наиболее точно описывает поведение `return` внутри try/finally?  
> a) finally игнорируется при return  
> b) return немедленно завершает Execution Context без выполнения finally  
> c) finally всегда выполняется, даже если return уже был инициирован  
> d) return преобразуется в throw внутри try блока
> 
> > [!success]- Answer  
> > c) finally всегда выполняется, даже если return уже был инициирован  
> > Explanation:  
> > finally выполняется перед завершением Completion Record, даже при return.  
> > a неверен: finally не игнорируется.  
> > b неверен: Execution Context не завершается мгновенно.  
> > d неверен: return не превращается в throw.

---

> [!question] Почему `Object.create(null)` ведёт себя иначе при доступе к `toString`?  
> a) Потому что prototype chain отсутствует, и lookup не находит Object.prototype  
> b) Потому что toString удаляется оптимизатором V8  
> c) Потому что null автоматически заменяется на undefined  
> d) Потому что Object.create всегда создаёт frozen объект
> 
> > [!success]- Answer  
> > a) Потому что prototype chain отсутствует, и lookup не находит Object.prototype  
> > Explanation:  
> > Object.create(null) создаёт объект без [[Prototype]], поэтому нет доступа к Object.prototype methods.  
> > b неверен: ничего не удаляется движком.  
> > c неверен: null не преобразуется.  
> > d неверен: объект не frozen по умолчанию.

---

> [!question] Какой сценарий наиболее точно иллюстрирует работу Lexical Environment?  
> a) Хранение всех глобальных объектов в одном кеше  
> b) Механизм разрешения идентификаторов через цепочку Environment Records  
> c) Прямое хранение переменных в Execution Context Stack  
> d) Копирование scope при каждой функции в runtime
> 
> > [!success]- Answer  
> > b) Механизм разрешения идентификаторов через цепочку Environment Records  
> > Explanation:  
> > Lexical Environment — это структура для resolution идентификаторов через цепочку scope’ов.  
> > a неверен: нет единого кеша.  
> > c неверен: Execution Context содержит ссылки, но не хранит переменные напрямую.  
> > d неверен: scope не копируется.

---

> [!question] Почему генераторы (`function*`) нельзя считать эквивалентом async/await?  
> a) Потому что генераторы не поддерживают yield в strict mode  
> b) Потому что генераторы управляются вручную через next(), а async управляется event loop  
> c) Потому что генераторы выполняются только в браузере  
> d) Потому что async функции не используют Execution Context Stack
> 
> > [!success]- Answer  
> > b) Потому что генераторы управляются вручную через next(), а async управляется event loop  
> > Explanation:  
> > Генераторы требуют ручного управления итерацией, async/await интегрирован в event loop.  
> > a неверен: yield работает в strict mode.  
> > c неверен: генераторы работают везде.  
> > d неверен: async использует Execution Context Stack.

---

> [!question] Что наиболее точно описывает порядок выполнения microtasks при вложенных Promise.then?  
> a) Каждый then выполняется после полного завершения предыдущего macrotask  
> b) Microtask queue полностью опустошается до перехода к следующему macrotask, включая новые microtasks  
> c) Каждый then создаёт отдельный Event Loop cycle  
> d) then выполняются синхронно внутри текущего Execution Context
> 
> > [!success]- Answer  
> > b) Microtask queue полностью опустошается до перехода к следующему macrotask, включая новые microtasks  
> > Explanation:  
> > Microtask queue flush происходит рекурсивно до полного опустошения.  
> > a неверен: недостаточно точно описывает рекурсию microtasks.  
> > c неверен: нет отдельного цикла на каждый then.  
> > d неверен: then не синхронен.

---

> [!question] Почему `typeof function() {}` возвращает "function", несмотря на то, что функция — это объект?  
> a) Потому что typeof отражает внутренний slot [[Call]]  
> b) Потому что функции не являются объектами в ECMAScript  
> c) Потому что движок переопределяет typeof для функций через Host hooks  
> d) Потому что prototype цепочка функции особая и не учитывается typeof
> 
> > [!success]- Answer  
> > a) Потому что typeof отражает внутренний slot [[Call]]  
> > Explanation:  
> > typeof distinguishes callable objects via internal [[Call]] capability.  
> > b неверен: функции являются объектами.  
> > c неверен: Host не участвует в typeof.  
> > d неверен: prototype chain не влияет на typeof.

---

> [!question] Какой механизм наиболее точно объясняет “замыкание” (closure) в терминах спецификации?  
> a) Копирование переменных в момент создания функции  
> b) Захват ссылки на Lexical Environment, а не значений  
> c) Создание отдельного Execution Context для каждой функции  
> d) Преобразование scope в объект при компиляции
> 
> > [!success]- Answer  
> > b) Захват ссылки на Lexical Environment, а не значений  
> > Explanation:  
> > Closure сохраняет ссылку на Lexical Environment, обеспечивая доступ к переменным.  
> > a неверен: нет копирования значений.  
> > c неверен: Execution Context не дублируется.  
> > d неверен: scope не превращается в объект.

---

> [!question] Почему `Promise.resolve().then(() => console.log(1)); console.log(2);` всегда выводит 2 затем 1?  
> a) Потому что Promise.then выполняется синхронно  
> b) Потому что microtasks выполняются после завершения текущего Execution Context  
> c) Потому что console.log асинхронен  
> d) Потому что Promise создаёт новый Event Loop
> 
> > [!success]- Answer  
> > b) Потому что microtasks выполняются после завершения текущего Execution Context  
> > Explanation:  
> > then callback попадает в microtask queue и выполняется после текущего стека.  
> > a неверен: then не синхронен.  
> > c неверен: console.log синхронен.  
> > d неверен: Promise не создаёт event loop.

---
> [!question] Какое поведение наиболее точно соответствует спецификации при выполнении цепочки микрозадач, создающих новые микрозадачи бесконечно (microtask starvation)?  
> a) Event Loop принудительно прерывает выполнение microtasks после фиксированного числа итераций  
> b) Microtask queue блокирует macrotask queue до тех пор, пока не завершится текущий stack frame  
> c) Microtask queue может теоретически блокировать переход к macrotask indefinitely, если постоянно пополняется новыми microtasks  
> d) Browser автоматически переводит microtasks в macrotasks при переполнении очереди
> 
> > [!success]- Answer  
> > c) Microtask queue может теоретически блокировать переход к macrotask indefinitely, если постоянно пополняется новыми microtasks  
> > Explanation:  
> > Спецификация не ограничивает рост microtask queue. Если она бесконечно пополняется, macrotask никогда не будет выполнен.  
> > a неверен: лимитов в спецификации нет.  
> > b неверен: stack frame уже завершён до microtasks flush.  
> > d неверен: автоматической конвертации нет.

---

> [!question] Почему следующий код может привести к различию поведения между движками?
> 
> ```js
> let obj = {  
>   get x() {  
>     delete this.x;  
>     return 1;  
>   }  
> };  
> console.log(obj.x);  
> ```
> 
> a) Потому что delete внутри getter запрещён спецификацией  
> b) Потому что порядок [[Get]] и [[Delete]] может зависеть от оптимизаций inline cache  
> c) Потому что спецификация допускает разные моменты materialization property descriptor  
> d) Потому что this внутри getter всегда undefined
> 
> > [!success]- Answer  
> > b) Потому что порядок [[Get]] и [[Delete]] может зависеть от оптимизаций inline cache  
> > Explanation:  
> > Поведение связано с тем, как движок реализует property access и кеширование inline caches при вызове getter’ов.  
> > a неверен: delete разрешён.  
> > c неверен: семантика описана строго, различия — в реализации, не в спецификации.  
> > d неверен: this в getter — объект (или undefined в strict call, но здесь метод вызова объекта).

---

> [!question] Что наиболее точно объясняет поведение следующего кода?
> 
> ```js
> const obj = {  
>   a: 1,  
>   get b() {  
>     return this.a;  
>   }  
> };  
> const f = obj.b;  
> console.log(f);  
> ```
> 
> a) getter возвращает ссылку на binding b  
> b) this теряется, потому что происходит unbound [[Get]] without call context  
> c) obj.b компилируется в замыкание  
> d) getter всегда возвращает undefined при деструктуризации
> 
> > [!success]- Answer  
> > b) this теряется, потому что происходит unbound [[Get]] without call context  
> > Explanation:  
> > При извлечении getter как значения теряется receiver, поэтому this не связан.  
> > a неверен: нет binding reference.  
> > c неверен: это не closure.  
> > d неверен: undefined не является правилом.

---

> [!question] Какое утверждение наиболее точно описывает ToPrimitive в контексте объектов с valueOf и Symbol.toPrimitive?  
> a) valueOf всегда имеет приоритет над Symbol.toPrimitive  
> b) Symbol.toPrimitive полностью игнорирует hint и всегда возвращает string  
> c) ToPrimitive сначала проверяет Symbol.toPrimitive, затем valueOf/toString fallback  
> d) ToPrimitive вызывает только toString, если объект не frozen
> 
> > [!success]- Answer  
> > c) ToPrimitive сначала проверяет Symbol.toPrimitive, затем valueOf/toString fallback  
> > Explanation:  
> > Спецификация сначала вызывает @@toPrimitive, затем fallback алгоритм.  
> > a неверен: Symbol.toPrimitive имеет высший приоритет.  
> > b неверен: hint учитывается.  
> > d неверен: frozen не влияет.

---

> [!question] Почему следующий код ведёт себя неожиданно?
> 
> ```js
> const obj = {  
>   [Symbol.toPrimitive](hint) {  
>     return hint === "number" ? 1 : "1";  
>   }  
> };  
> console.log(obj + obj);  
> ```
> 
> a) потому что + всегда приводит к string concatenation  
> b) потому что оба объекта приводятся к primitive с hint "default", но порядок неоднозначен  
> c) потому что сначала вызывается valueOf, затем toString  
> d) потому что Symbol.toPrimitive игнорируется в бинарных операциях
> 
> > [!success]- Answer  
> > b) потому что оба объекта приводятся к primitive с hint "default", но порядок неоднозначен  
> > Explanation:  
> > оператор + вызывает ToPrimitive с hint "default", что может приводить к строковому или числовому поведению.  
> > a неверен: не всегда string concat.  
> > c неверен: valueOf/toString не вызываются первыми.  
> > d неверен: Symbol.toPrimitive имеет приоритет.

---

> [!question] Какое утверждение наиболее точно объясняет поведение Proxy при нарушении invariants?  
> a) Proxy всегда подавляет ошибки и возвращает undefined  
> b) Proxy может выбросить TypeError, если handler нарушает invariants объекта  
> c) Proxy полностью заменяет target и не обязан соблюдать его свойства  
> d) Proxy работает только в strict mode
> 
> > [!success]- Answer  
> > b) Proxy может выбросить TypeError, если handler нарушает invariants объекта  
> > Explanation:  
> > Proxy обязан соблюдать invariants (например, non-configurable properties).  
> > a неверен: ошибки не подавляются.  
> > c неверен: target остаётся источником invariants.  
> > d неверен: Proxy не зависит от strict mode.

---

> [!question] Почему следующий код может вести себя нестабильно при оптимизации движка?
> 
> ```js
> function test(obj) {  
>   return obj.x + obj.x;  
> }  
> ```
> 
> a) потому что obj может менять prototype chain во время выполнения  
> b) потому что inline cache может специализировать доступ к obj.x и нарушить наблюдаемость side effects  
> c) потому что + оператор не ассоциативен  
> d) потому что obj автоматически становится Realm-bound
> 
> > [!success]- Answer  
> > b) потому что inline cache может специализировать access к obj.x и нарушить наблюдаемость side effects  
> > Explanation:  
> > движки оптимизируют property access через inline caches, что может влиять на поведение при изменениях структуры объекта.  
> > a неверен: prototype mutation не основной фактор здесь.  
> > c неверен: + ассоциативен слева направо.  
> > d неверен: Realm binding не влияет.

---

> [!question] Какой механизм наиболее точно объясняет различие между module scripts и classic scripts в ECMAScript?  
> a) module scripts выполняются в отдельном Execution Context без Event Loop  
> b) module scripts имеют strict mode и отдельный lexical environment с live bindings  
> c) classic scripts не имеют lexical environment  
> d) module scripts не используют Realm
> 
> > [!success]- Answer  
> > b) module scripts имеют strict mode и отдельный lexical environment с live bindings  
> > Explanation:  
> > ES modules используют live bindings и отдельную область lexical environment.  
> > a неверен: Event Loop используется одинаково.  
> > c неверен: classic scripts имеют lexical environment.  
> > d неверен: modules используют Realm.

---

> [!question] Почему следующий код ведёт к неожиданному порядку выполнения?
> 
> ```js
> queueMicrotask(() => console.log("A"));  
> Promise.resolve().then(() => console.log("B"));  
> ```
> 
> a) queueMicrotask всегда выполняется позже Promise.then  
> b) порядок гарантирован только внутри одного источника microtask scheduling  
> c) Promise.then и queueMicrotask используют разные Event Loop phases  
> d) queueMicrotask выполняется в следующем macrotask
> 
> > [!success]- Answer  
> > b) порядок гарантирован только внутри одного источника microtask scheduling  
> > Explanation:  
> > оба — microtasks, но спецификация не гарантирует строгий порядок между источниками постановки задач.  
> > a неверен: queueMicrotask не всегда позже.  
> > c неверен: фазы одинаковые (microtask queue).  
> > d неверен: это не macrotask.

---

> [!question] Почему следующая операция может вести себя по-разному в разных движках?
> 
> ```js
> let x = 1;  
> x = x++;  
> console.log(x);  
> ```
> 
> a) потому что post-increment не определён в ECMAScript  
> b) потому что порядок Evaluation Order и side effects допускает различную интерпретацию временных binding snapshot  
> c) потому что let переменные не поддерживают assignment  
> d) потому что x++ всегда возвращает undefined
> 
> > [!success]- Answer  
> > b) потому что порядок Evaluation Order и side effects допускает различную интерпретацию temporal value snapshot  
> > Explanation:  
> > post-increment включает сложную семантику временного значения и assignment order.  
> > a неверен: оператор определён.  
> > c неверен: let поддерживает assignment.  
> > d неверен: x++ возвращает значение до инкремента.

---

> [!question] Что наиболее точно описывает поведение JavaScript при одновременной работе нескольких Realm (например, iframe + main window) с точки зрения globalThis?  
> a) globalThis всегда указывает на один и тот же объект во всех Realm  
> b) globalThis зависит от текущего Execution Context, но разделяет prototype chain между Realm  
> c) Каждый Realm имеет собственный globalThis, не связанный ссылочно с другими Realm  
> d) globalThis является alias для Object.prototype в глобальной области
> 
> > [!success]- Answer  
> > c) Каждый Realm имеет собственный globalThis, не связанный ссылочно с другими Realm  
> > Explanation:  
> > Каждый Realm имеет собственный глобальный объект и свой globalThis. Они изолированы.  
> > a неверен: нет общего globalThis.  
> > b неверен: prototype chain не общий между Realm.  
> > d неверен: globalThis не связан с Object.prototype.

---

> [!question] Почему следующий код может вести себя по-разному при разных реализациях движков?
> 
> ```js
> const obj = {  
>   get a() {  
>     Object.defineProperty(this, "a", { value: 1 });  
>     return this.a;  
>   }  
> };  
> console.log(obj.a);  
> ```
> 
> a) потому что getter запрещён спецификацией  
> b) потому что порядок [[DefineOwnProperty]] и [[Get]] может оптимизироваться движком при кешировании property access  
> c) потому что this в getter всегда frozen object  
> d) потому что defineProperty всегда асинхронен
> 
> > [!success]- Answer  
> > b) потому что порядок [[DefineOwnProperty]] и [[Get]] может оптимизироваться движком при кешировании property access  
> > Explanation:  
> > комбинация getter + defineProperty + повторный get создаёт сложные эффекты оптимизаций inline caches.  
> > a неверен: getter разрешён.  
> > c неверен: this не frozen.  
> > d неверен: defineProperty синхронен.

---

> [!question] Какой механизм наиболее точно объясняет разницу между ordinary object и exotic object (например, Array)?  
> a) exotic objects не имеют prototype  
> b) exotic objects имеют внутренние методы ([[DefineOwnProperty]], [[Get]]) с переопределённой семантикой  
> c) exotic objects существуют только в V8  
> d) exotic objects не участвуют в prototype chain
> 
> > [!success]- Answer  
> > b) exotic objects имеют internal methods ([[DefineOwnProperty]], [[Get]]) с переопределённой семантикой  
> > Explanation:  
> > exotic objects отличаются внутренними методами спецификации (например, Array length behavior).  
> > a неверен: prototype может быть.  
> > c неверен: это ECMAScript концепт, не V8-only.  
> > d неверен: участвуют в prototype chain.

---

> [!question] Почему изменение Array.length может приводить к неожиданному удалению элементов?  
> a) потому что length — это обычное свойство объекта  
> b) потому что [[DefineOwnProperty]] для length имеет side effect truncation semantics  
> c) потому что массивы копируются при каждом изменении length  
> d) потому что length хранится в prototype
> 
> > [!success]- Answer  
> > b) потому что [[DefineOwnProperty]] для length имеет side effect truncation semantics  
> > Explanation:  
> > изменение length триггерит внутреннюю логику обрезки элементов массива.  
> > a неверен: length — exotic property.  
> > c неверен: копирования нет.  
> > d неверен: length не в prototype.

---

> [!question] Что наиболее точно объясняет поведение следующего кода?
> 
> ```js
> const obj = {  
>   valueOf() { return 1; },  
>   toString() { return "2"; }  
> };  
> console.log(obj + obj);  
> ```
> 
> a) всегда вызывается toString  
> b) всегда вызывается valueOf  
> c) выбор зависит от hint и порядка ToPrimitive, определяемого операцией +  
> d) valueOf и toString вызываются параллельно
> 
> > [!success]- Answer  
> > c) выбор зависит от hint и порядка ToPrimitive, определяемого операцией +  
> > Explanation:
> > 
> > - использует ToPrimitive с hint "default", и порядок зависит от спецификации.  
> >     a неверен: не всегда toString.  
> >     b неверен: не всегда valueOf.  
> >     d неверен: нет параллелизма.
> >     

---

> [!question] Почему следующий код может вызвать TypeError?
> 
> ```js
> const p = new Proxy({}, {  
>   getOwnPropertyDescriptor() {  
>     return undefined;  
>   }  
> });  
> Object.keys(p);  
> ```
> 
> a) потому что Proxy всегда запрещён в Object.keys  
> b) потому что нарушены invariants [[GetOwnProperty]] для non-configurable properties  
> c) потому что Object.keys не работает с Proxy  
> d) потому что getOwnPropertyDescriptor не может возвращать undefined
> 
> > [!success]- Answer  
> > b) потому что нарушены invariants [[GetOwnProperty]] для non-configurable properties  
> > Explanation:  
> > Proxy handler обязан соблюдать invariants спецификации, иначе TypeError.  
> > a неверен: Proxy разрешён.  
> > c неверен: Object.keys работает с Proxy.  
> > d неверен: undefined допустим, но с ограничениями invariants.

---

> [!question] Какой механизм наиболее точно описывает module instantiation phase в ECMAScript?  
> a) выполнение всех модулей синхронно до загрузки HTML  
> b) связывание экспортов и импортов через live bindings перед evaluation phase  
> c) копирование экспортируемых значений в импортирующие модули  
> d) выполнение модулей в отдельном Event Loop
> 
> > [!success]- Answer  
> > b) связывание экспортов и импортов через live bindings перед evaluation phase  
> > Explanation:  
> > ES modules сначала проходят linking (live bindings), затем evaluation.  
> > a неверен: не связано с HTML blocking.  
> > c неверен: нет копирования значений.  
> > d неверен: Event Loop не изолирован.

---

> [!question] Почему следующий код демонстрирует “неинтуитивную” наблюдаемость side effects?
> 
> ```js
> let x = {  
>   get a() {  
>     delete this.a;  
>     return this.a;  
>   }  
> };  
> console.log(x.a);  
> ```
> 
> a) потому что delete всегда выполняется после return  
> b) потому что порядок [[Get]] внутри getter может повторно инициировать property lookup после mutation  
> c) потому что getter кешируется движком  
> d) потому что this становится null после delete
> 
> > [!success]- Answer  
> > b) потому что порядок [[Get]] внутри getter может повторно инициировать property lookup после mutation  
> > Explanation:  
> > getter + delete + повторный access вызывает повторное разрешение свойства после изменения объекта.  
> > a неверен: return не блокирует delete.  
> > c неверен: кеширование не определено здесь.  
> > d неверен: this не становится null.

---

> [!question] Какой сценарий наиболее точно демонстрирует разницу между spec-level evaluation order и engine-level optimization?  
> a) изменение порядка инструкций в AST во время выполнения  
> b) одинаковый JS код может иметь разные observable side effects из-за inline caching и JIT optimizations  
> c) ECMAScript допускает случайную перестановку выражений  
> d) Event Loop меняет порядок выполнения синхронного кода
> 
> > [!success]- Answer  
> > b) одинаковый JS код может иметь разные observable side effects из-за inline caching и JIT optimizations  
> > Explanation:  
> > Спека фиксирует семантику, но движки оптимизируют исполнение, сохраняя наблюдаемое поведение.  
> > a неверен: AST не мутирует так.  
> > c неверен: порядок фиксирован спецификацией.  
> > d неверен: event loop не влияет на sync order.

---

> [!question] Почему следующий код считается крайне опасным с точки зрения спецификации Proxy invariants?
> 
> ```js
> const arr = new Proxy([], {  
>   set(target, prop, value) {  
>     target.length = 0;  
>     target[prop] = value;  
>     return true;  
>   }  
> });  
> arr[0] = 1;  
> ```
> 
> a) потому что Proxy запрещает изменение length  
> b) потому что нарушается invariant согласованности [[DefineOwnProperty]] для array exotic objects  
> c) потому что set trap не может изменять target  
> d) потому что массивы immutable в ECMAScript
> 
> > [!success]- Answer  
> > b) потому что нарушается invariant согласованности [[DefineOwnProperty]] для array exotic objects  
> > Explanation:  
> > массивы имеют строгие invariants, и вмешательство Proxy может их нарушить.  
> > a неверен: Proxy разрешает изменения.  
> > c неверен: set trap может изменять target.  
> > d неверен: массивы mutable.

---

