#flashquiz/proga/js/spec/3
> [!question] Почему введение Iterator как глобального объекта с ленивыми операциями (map/filter) принципиально влияет на memory model по сравнению с Array.prototype.map?  
> a) Потому что Array автоматически становится lazy  
> b) Потому что Iterator позволяет избегать materialization промежуточных коллекций и тем самым меняет allocation pressure в runtime  
> c) Потому что Array перестаёт поддерживать элементы  
> d) Потому что Iterator заменяет prototype chain  
>> [!success]- Answer  
>> b) Потому что Iterator позволяет избегать materialization промежуточных коллекций и тем самым меняет allocation pressure в runtime  

> [!question] Почему Set.prototype.union/intersection нельзя считать просто syntactic sugar над Array operations в терминах спецификации?  
> a) Потому что Set всегда отсортирован  
> b) Потому что Set использует SameValueZero семантику и гарантирует уникальность на уровне абстрактной модели данных  
> c) Потому что Array не поддерживает итерацию  
> d) Потому что Set запрещает примитивы  
>> [!success]- Answer  
>> b) Потому что Set использует SameValueZero семантику и гарантирует уникальность на уровне абстрактной модели данных  

> [!question] Какой security-угрозе в первую очередь противодействуют Import Attributes и JSON modules?  
> a) Prototype pollution через Object.create  
> b) MIME-type substitution и подмена интерпретации импортируемого ресурса  
> c) XSS через eval  
> d) Memory leaks в GC  
>> [!success]- Answer  
>> b) MIME-type substitution и подмена интерпретации импортируемого ресурса  

> [!question] Почему Promise.try меняет семантику error propagation по сравнению с Promise.resolve(fn())?  
> a) Потому что Promise.resolve всегда async  
> b) Потому что Promise.try гарантирует захват синхронного throw до перехода в microtask queue  
> c) Потому что Promise.try отключает event loop  
> d) Потому что Promise.try делает функции idempotent  
>> [!success]- Answer  
>> b) Потому что Promise.try гарантирует захват синхронного throw до перехода в microtask queue  

> [!question] Почему Float16Array критически влияет на performance моделей машинного обучения в ECMAScript-экосистеме?  
> a) Потому что увеличивает точность до double precision  
> b) Потому что снижает memory bandwidth за счёт уменьшения размера представления чисел  
> c) Потому что заменяет BigInt  
> d) Потому что отключает IEEE 754  
>> [!success]- Answer  
>> b) Потому что снижает memory bandwidth за счёт уменьшения размера представления чисел  

> [!question] Какое утверждение наиболее точно описывает роль Host vs Implementation разделения?  
> a) Host является частью движка  
> b) Implementation и Host вместе определяют все internal methods  
> c) Host предоставляет внешние hooks, которые могут влиять на observable semantics вне спецификации ядра  
> d) Host всегда строго детерминирован  
>> [!success]- Answer  
>> c) Host предоставляет внешние hooks, которые могут влиять на observable semantics вне спецификации ядра  

> [!question] Почему классификация Legacy / Normative Optional важна для conformance проверки движков?  
> a) Потому что Legacy запрещено реализовывать  
> b) Потому что реализация может быть формально compliant даже без всех optional features  
> c) Потому что Normative Optional не влияет на поведение  
> d) Потому что Legacy относится только к синтаксису  
>> [!success]- Answer  
>> b) Потому что реализация может быть формально compliant даже без всех optional features  

> [!question] Почему String в ECMAScript допускает ill-formed UTF-16 последовательности?  
> a) Потому что JS использует UTF-8  
> b) Потому что спецификация оперирует 16-bit code units без enforcement validity constraints  
> c) Потому что строки всегда нормализуются автоматически  
> d) Потому что Unicode запрещён  
>> [!success]- Answer  
>> b) Потому что спецификация оперирует 16-bit code units без enforcement validity constraints  

> [!question] Почему различие Number (𝔽) и BigInt (ℤ) критично для финансовых систем?  
> a) Потому что BigInt медленнее  
> b) Потому что Number теряет точность вне safe integer range из-за IEEE 754 представления  
> c) Потому что BigInt не поддерживает арифметику  
> d) Потому что Number является строкой  
>> [!success]- Answer  
>> b) Потому что Number теряет точность вне safe integer range из-за IEEE 754 представления  

> [!question] Почему изменение CF.prototype после создания экземпляров влияет на уже созданные объекты?  
> a) Потому что prototype копируется в объект  
> b) Потому что [[Prototype]] lookup происходит динамически через chain, а не при инстанцировании  
> c) Потому что new пересоздаёт все объекты  
> d) Потому что JS кеширует методы навсегда  
>> [!success]- Answer  
>> b) Потому что [[Prototype]] lookup происходит динамически через chain, а не при инстанцировании  

> [!question] Почему Early Error Rules критичны для архитектуры ECMAScript engine?  
> a) Потому что они выполняются во время runtime execution  
> b) Потому что они позволяют отклонить программу до начала выполнения, гарантируя отсутствие частичных эффектов  
> c) Потому что они заменяют GC  
> d) Потому что они относятся только к TypeScript  
>> [!success]- Answer  
>> b) Потому что они позволяют отклонить программу до начала выполнения, гарантируя отсутствие частичных эффектов  

> [!question] Почему Completion Records являются ключевыми для понимания поведения try/finally?  
> a) Потому что они описывают только ошибки  
> b) Потому что они моделируют propagation нормальных и abrupt completion через все уровни интерпретации  
> c) Потому что они отключают return  
> d) Потому что они используются только в async functions  
>> [!success]- Answer  
>> b) Потому что они моделируют propagation нормальных и abrupt completion через все уровни интерпретации  

> [!question] Почему операция clamping критична для typed arrays и buffer-based APIs?  
> a) Потому что она увеличивает precision  
> b) Потому что она гарантирует попадание значений в допустимый диапазон индексов/байтовых представлений  
> c) Потому что она заменяет Math.floor  
> d) Потому что она отключает overflow  
>> [!success]- Answer  
>> b) Потому что она гарантирует попадание значений в допустимый диапазон индексов/байтовых представлений  

> [!question] Почему ленивые итераторы (Iterator with map/filter) принципиально меняют поведение оптимизаций GC по сравнению с eager Array transformations?  
> a) Потому что GC перестаёт работать с итераторами  
> b) Потому что уменьшается количество промежуточных аллокаций и меняется lifetime объектов в цепочке вычислений  
> c) Потому что Array становится immutable  
> d) Потому что Iterator требует manual memory management  
>> [!success]- Answer  
>> b) Потому что уменьшается количество промежуточных аллокаций и меняется lifetime объектов в цепочке вычислений  

> [!question] Почему Set операции (union/intersection/difference) не могут быть безопасно JIT-оптимизированы как Array operations без guard checks?  
> a) Потому что Set всегда отсортирован  
> b) Потому что Set использует SameValueZero и может иметь произвольный order insertion semantics  
> c) Потому что Array не iterable  
> d) Потому что Set не имеет prototype  
>> [!success]- Answer  
>> b) Потому что Set использует SameValueZero и может иметь произвольный order insertion semantics  

> [!question] Какой инвариант нарушается при попытке заменить RegExp.escape на naive string replacement в runtime оптимизации?  
> a) Unicode normalization invariant  
> b) Regex grammar correctness and meta-character escaping completeness  
> c) Prototype chain consistency  
> d) Event loop ordering  
>> [!success]- Answer  
>> b) Regex grammar correctness and meta-character escaping completeness  

> [!question] Почему Import Attributes принципиально важны для module resolution pipeline?  
> a) Потому что они ускоряют parsing  
> b) Потому что они добавляют type-level metadata в resolution phase, влияя на loader decision before execution  
> c) Потому что они отключают dynamic imports  
> d) Потому что они заменяют JSON.parse  
>> [!success]- Answer  
>> b) Потому что они добавляют type-level metadata в resolution phase, влияя на loader decision before execution  

> [!question] Почему Float16Array нельзя рассматривать как просто “memory optimization” без влияния на numerical stability?  
> a) Потому что он всегда округляет до zero  
> b) Потому что он меняет error propagation in floating point accumulation chains  
> c) Потому что он заменяет BigInt arithmetic  
> d) Потому что он отключает IEEE 754 rules  
>> [!success]- Answer  
>> b) Потому что он меняет error propagation in floating point accumulation chains  

> [!question] Почему Host hooks создают фундаментальную недоказуемость полной формальной эквивалентности ECMAScript программ между средами?  
> a) Потому что они запрещают strict mode  
> b) Потому что они определяют observable semantics вне формализованного ядра языка  
> c) Потому что они изменяют syntax grammar  
> d) Потому что они отключают Promise microtasks  
>> [!success]- Answer  
>> b) Потому что они определяют observable semantics вне формализованного ядра языка  

> [!question] Почему Normative Optional разделы являются критичными для эволюции ECMAScript без breaking changes?  
> a) Потому что они обязательны для всех engines  
> b) Потому что они позволяют расширять стандарт без изменения conformance baseline  
> c) Потому что они заменяют legacy code  
> d) Потому что они отключают backward compatibility  
>> [!success]- Answer  
>> b) Потому что они позволяют расширять стандарт без изменения conformance baseline  

> [!question] Почему ill-formed UTF-16 strings в ECMAScript требуют методов isWellFormed/toWellFormed вместо автоматической нормализации?  
> a) Потому что Unicode запрещает normalization  
> b) Потому что сохранение raw code unit representation важно для backward compatibility and binary fidelity  
> c) Потому что JS использует UTF-32  
> d) Потому что strings are immutable in spec  
>> [!success]- Answer  
>> b) Потому что сохранение raw code unit representation важно для backward compatibility and binary fidelity  

> [!question] Почему Number (IEEE 754) не может быть заменён BigInt даже при “lossless arithmetic” требованиях?  
> a) Потому что BigInt не поддерживает division  
> b) Потому что floating-point semantics необходимы для performance-sensitive numeric operations (NaN, Infinity, fractions)  
> c) Потому что Number является string-based  
> d) Потому что BigInt не iterable  
>> [!success]- Answer  
>> b) Потому что floating-point semantics необходимы для performance-sensitive numeric operations (NaN, Infinity, fractions)  

> [!question] Почему prototype mutation invalidates inline caches в современных JS engines?  
> a) Потому что inline cache зависит от event loop  
> b) Потому что cached property access assumes stable shape + stable prototype chain  
> c) Потому что GC deletes caches on mutation  
> d) Потому что Proxy disables caching entirely  
>> [!success]- Answer  
>> b) Потому что cached property access assumes stable shape + stable prototype chain  

> [!question] Почему Early Error Rules нельзя считать runtime checks и почему это важно для engine design?  
> a) Потому что они выполняются после execution  
> b) Потому что они выполняются на parse/validation phase и предотвращают creation of execution context  
> c) Потому что они зависят от GC  
> d) Потому что они относятся только к strict mode  
>> [!success]- Answer  
>> b) Потому что они выполняются на parse/validation phase и предотвращают creation of execution context  

> [!question] Почему Completion Records формально объясняют поведение return внутри try/finally лучше, чем императивная модель?  
> a) Потому что они описывают только exceptions  
> b) Потому что они моделируют propagation of control flow states including override of abrupt completion by finally  
> c) Потому что они отключают async execution  
> d) Потому что они используются только в generators  
>> [!success]- Answer  
>> b) Потому что они моделируют propagation of control flow states including override of abrupt completion by finally  

> [!question] Почему Iterator-based lazy pipeline (map/filter/take) меняет границу между “data structure” и “control flow” в ECMAScript модели вычислений?  
> a) Потому что Iterator запрещает использование функций высшего порядка  
> b) Потому что вычисление становится pull-based, перенося часть control flow в момент потребления значений  
> c) Потому что Array перестаёт быть iterable  
> d) Потому что lazy evaluation отключает prototype chain  
>> [!success]- Answer  
>> b) Потому что вычисление становится pull-based, перенося часть control flow в момент потребления значений  

> [!question] Почему Set.prototype operations требуют SameValueZero семантики и не могут быть эквивалентны === comparison без изменения поведения?  
> a) Потому что === не поддерживает NaN  
> b) Потому что SameValueZero определяет равенство иначе, включая NaN === NaN и +0/-0 унификацию  
> c) Потому что Set хранит только строки  
> d) Потому что Array использует SameValueZero  
>> [!success]- Answer  
>> b) Потому что SameValueZero определяет равенство иначе, включая NaN === NaN и +0/-0 унификацию  

> [!question] Почему Import Attributes влияют на безопасность module resolution сильнее, чем runtime sanitization импортируемых данных?  
> a) Потому что они заменяют JSON.parse  
> b) Потому что они участвуют в фазе resolution до execution, предотвращая неверную интерпретацию ресурса  
> c) Потому что они отключают dynamic imports  
> d) Потому что они изменяют event loop  
>> [!success]- Answer  
>> b) Потому что они участвуют в фазе resolution до execution, предотвращая неверную интерпретацию ресурса  

> [!question] Почему Float16Array особенно чувствителен к накоплению ошибок в iterative numeric algorithms (например gradient descent)?  
> a) Потому что он всегда округляет до integer  
> b) Потому что reduced mantissa leads to faster precision loss propagation in long computation chains  
> c) Потому что он не поддерживает отрицательные числа  
> d) Потому что он отключает IEEE 754 compliance  
>> [!success]- Answer  
>> b) Потому что reduced mantissa leads to faster precision loss propagation in long computation chains  

> [!question] Почему Host-defined hooks делают невозможной полную формальную equivalence between Node.js and browser ECMAScript execution?  
> a) Потому что Node.js не поддерживает modules  
> b) Потому что Host-defined operations introduce environment-specific observable semantics outside ECMAScript core  
> c) Потому что browsers не поддерживают BigInt  
> d) Потому что event loop отсутствует в Node.js  
>> [!success]- Answer  
>> b) Потому что Host-defined operations introduce environment-specific observable semantics outside ECMAScript core  

> [!question] Почему Normative Optional features не нарушают conformance, даже если отсутствуют в реализации?  
> a) Потому что они deprecated  
> b) Потому что conformance определяется subset of mandatory semantics, not full feature parity  
> c) Потому что они заменяются polyfills  
> d) Потому что они относятся только к legacy code  
>> [!success]- Answer  
>> b) Потому что conformance определяется subset of mandatory semantics, not full feature parity  

> [!question] Почему String “ill-formed UTF-16” модель важна для бинарной совместимости и engine internals?  
> a) Потому что JS использует UTF-8 internally  
> b) Потому что string is raw 16-bit code unit sequence without enforcement of Unicode validity  
> c) Потому что strings are compressed at runtime  
> d) Потому что Unicode normalization is mandatory in spec  
>> [!success]- Answer  
>> b) Потому что string is raw 16-bit code unit sequence without enforcement of Unicode validity  

> [!question] Почему IEEE 754 Number semantics нельзя заменить exact arithmetic model без потери performance guarantees в ECMAScript runtime?  
> a) Потому что BigInt быстрее  
> b) Потому что floating-point hardware acceleration and NaN/Infinity semantics are fundamental for engine performance model  
> c) Потому что Number запрещён в spec  
> d) Потому что JS не поддерживает decimals  
>> [!success]- Answer  
>> b) Потому что floating-point hardware acceleration and NaN/Infinity semantics are fundamental for engine performance model  

> [!question] Почему inline caches требуют invalidation при prototype mutation даже если object shape остаётся стабильным?  
> a) Потому что GC очищает cache  
> b) Потому что property resolution depends on prototype chain stability, not only object shape  
> c) Потому что Proxy always invalidates cache  
> d) Потому что IC работает только с primitives  
>> [!success]- Answer  
>> b) Потому что property resolution depends on prototype chain stability, not only object shape  

> [!question] Почему Early Error phase критичен для предотвращения partially executed ECMAScript programs?  
> a) Потому что errors are handled at runtime  
> b) Потому что validation occurs before execution context creation, preventing side effects entirely  
> c) Потому что it only affects syntax highlighting  
> d) Потому что it runs after GC  
>> [!success]- Answer  
>> b) Потому что validation occurs before execution context creation, preventing side effects entirely  

> [!question] Почему Completion Records являются фундаментальной моделью для understanding control-flow override (e.g. return inside try/finally)?  
> a) Потому что они only track exceptions  
> b) Потому что they model propagation and override of normal and abrupt completion states through nested execution contexts  
> c) Потому что they disable async functions  
> d) Потому что they are only used in loops  
>> [!success]- Answer  
>> b) Потому что they model propagation and override of normal and abrupt completion states through nested execution contexts  

> [!question] Почему Iterator pipeline (map/filter/take) принципиально сложнее для JIT-оптимизации, чем Array chain, даже при одинаковой логике вычислений?  
> a) Потому что Iterator всегда использует heap allocation  
> b) Потому что Iterator вводит indirection и late-bound execution order, размывая static call graph  
> c) Потому что Array не поддерживает функции высшего порядка  
> d) Потому что Iterator отключает prototype chain  
>> [!success]- Answer  
>> b) Потому что Iterator вводит indirection и late-bound execution order, размывая static call graph  

> [!question] Почему Set semantics (union/intersection) нельзя корректно свести к sorting + dedup pipeline без изменения observable behavior?  
> a) Потому что Set всегда отсортирован  
> b) Потому что SameValueZero equality + insertion order semantics не эквивалентны сортировке  
> c) Потому что Array не поддерживает NaN  
> d) Потому что Set запрещает итерацию  
>> [!success]- Answer  
>> b) Потому что SameValueZero equality + insertion order semantics не эквивалентны сортировке  

> [!question] Почему Import Attributes влияют на безопасность ещё до выполнения module code?  
> a) Потому что они выполняются в runtime phase  
> b) Потому что они участвуют в module resolution phase, до evaluation, влияя на loader decision  
> c) Потому что они заменяют HTTP headers  
> d) Потому что они отключают JSON parsing  
>> [!success]- Answer  
>> b) Потому что они участвуют в module resolution phase, до evaluation, влияя на loader decision  

> [!question] Почему Float16Array особенно проблематичен для numerical stability в iterative ML algorithms?  
> a) Потому что он увеличивает precision  
> b) Потому что reduced exponent/mantissa space усиливает накопление rounding errors в длинных цепочках вычислений  
> c) Потому что он отключает NaN  
> d) Потому что он не поддерживает multiplication  
>> [!success]- Answer  
>> b) Потому что reduced exponent/mantissa space усиливает накопление rounding errors в длинных цепочках вычислений  

> [!question] Почему Host hooks делают невозможным доказательство полного behavioral equivalence между ECMAScript реализациями?  
> a) Потому что они запрещают async/await  
> b) Потому что они определяют observable semantics вне ECMAScript core, зависящие от среды  
> c) Потому что они отключают GC  
> d) Потому что они меняют grammar  
>> [!success]- Answer  
>> b) Потому что они определяют observable semantics вне ECMAScript core, зависящие от среды  

> [!question] Почему Normative Optional features не нарушают conformance, даже если отсутствуют в движке?  
> a) Потому что они deprecated  
> b) Потому что conformance определяется обязательным ядром, а optional расширения не входят в baseline  
> c) Потому что они всегда polyfilled  
> d) Потому что они относятся только к legacy  
>> [!success]- Answer  
>> b) Потому что conformance определяется обязательным ядром, а optional расширения не входят в baseline  

> [!question] Почему ill-formed UTF-16 strings нельзя автоматически нормализовать без изменения observable semantics?  
> a) Потому что Unicode требует normalization  
> b) Потому что ECMAScript оперирует raw 16-bit code units, и нормализация изменяет битовую идентичность данных  
> c) Потому что strings immutable  
> d) Потому что UTF-16 запрещён  
>> [!success]- Answer  
>> b) Потому что ECMAScript оперирует raw 16-bit code units, и нормализация изменяет битовую идентичность данных  

> [!question] Почему IEEE 754 Number model критичен для JIT hardware-level optimization?  
> a) Потому что BigInt быстрее на CPU  
> b) Потому что он соответствует native floating-point instructions (FPU/SIMD), позволяя агрессивные оптимизации  
> c) Потому что Number — это string  
> d) Потому что JS не использует числа  
>> [!success]- Answer  
>> b) Потому что он соответствует native floating-point instructions (FPU/SIMD), позволяя агрессивные оптимизации  

> [!question] Почему prototype chain mutation ломает assumptions hidden class based optimization even if object shape is unchanged?  
> a) Потому что hidden classes depend only on GC  
> b) Потому что property lookup semantics include prototype chain resolution, invalidating cached lookup paths  
> c) Потому что Proxy disables caching always  
> d) Потому что Array becomes invalid  
>> [!success]- Answer  
>> b) Потому что property lookup semantics include prototype chain resolution, invalidating cached lookup paths  

> [!question] Почему Early Error phase нельзя моделировать как runtime validation?  
> a) Потому что it runs after execution  
> b) Потому что it occurs before execution context creation, preventing any observable side effects  
> c) Потому что it depends on GC  
> d) Потому что it is optional  
>> [!success]- Answer  
>> b) Потому что it occurs before execution context creation, preventing any observable side effects  

> [!question] Почему Completion Records являются фундаментом для формального описания control flow override (например return в try/finally)?  
> a) Потому что они only track exceptions  
> b) Потому что они encode propagation + override semantics for normal and abrupt completions across nested evaluation steps  
> c) Потому что они disable async  
> d) Потому что они only apply to loops  
>> [!success]- Answer  
>> b) Потому что они encode propagation + override semantics for normal and abrupt completions across nested evaluation steps  


> [!question] Почему Iterator-based lazy evaluation может ухудшить JIT-предсказуемость по сравнению с Array-based eager evaluation, даже если итоговый результат идентичен?  
> a) Потому что Iterator всегда аллоцирует больше памяти  
> b) Потому что порядок исполнения и точка вычисления значений становятся runtime-dependent и размывают оптимизируемые горячие пути  
> c) Потому что Array не поддерживает функции высшего порядка  
> d) Потому что Iterator отключает inline caching  
>> [!success]- Answer  
>> b) Потому что порядок исполнения и точка вычисления значений становятся runtime-dependent и размывают оптимизируемые горячие пути  

> [!question] Почему Set.prototype operations нельзя свести к deterministic sort + dedup без изменения спецификационного поведения?  
> a) Потому что Set всегда отсортирован  
> b) Потому что Set использует SameValueZero и сохраняет insertion order, а не sorted order  
> c) Потому что Array не поддерживает NaN  
> d) Потому что Set не iterable  
>> [!success]- Answer  
>> b) Потому что Set использует SameValueZero и сохраняет insertion order, а не sorted order  

> [!question] Почему Import Attributes меняют security model module loading, а не только parsing behavior?  
> a) Потому что они добавляют runtime validation  
> b) Потому что они влияют на resolution phase, до execution, определяя интерпретацию ресурса loader’ом  
> c) Потому что они заменяют HTTP  
> d) Потому что они отключают dynamic import  
>> [!success]- Answer  
>> b) Потому что они влияют на resolution phase, до execution, определяя интерпретацию ресурса loader’ом  

> [!question] Почему Float16Array увеличивает риск silent numerical instability в long-running numerical pipelines?  
> a) Потому что он всегда приводит к overflow  
> b) Потому что уменьшенная точность ускоряет накопление rounding errors в цепочках зависимых вычислений  
> c) Потому что он отключает floating point arithmetic  
> d) Потому что он не поддерживает multiplication  
>> [!success]- Answer  
>> b) Потому что уменьшенная точность ускоряет накопление rounding errors в цепочках зависимых вычислений  

> [!question] Почему Host-defined operations нарушают возможность формального equivalence между ECMAScript средами даже при одинаковом bytecode?  
> a) Потому что они запрещают JIT  
> b) Потому что они вводят external observable semantics, не фиксированные спецификацией  
> c) Потому что они отключают GC  
> d) Потому что они меняют grammar  
>> [!success]- Answer  
>> b) Потому что они вводят external observable semantics, не фиксированные спецификацией  

> [!question] Почему Normative Optional разделы не влияют на conformance baseline?  
> a) Потому что они deprecated  
> b) Потому что conformance определяется минимальным обязательным ядром спецификации  
> c) Потому что они всегда polyfilled  
> d) Потому что они относятся только к legacy браузерам  
>> [!success]- Answer  
>> b) Потому что conformance определяется минимальным обязательным ядром спецификации  

> [!question] Почему ill-formed UTF-16 strings сохраняются в ECMAScript вместо автоматической коррекции?  
> a) Потому что Unicode требует strict normalization  
> b) Потому что сохранение raw code units важно для backward compatibility и binary fidelity операций  
> c) Потому что strings are mutable  
> d) Потому что UTF-16 deprecated  
>> [!success]- Answer  
>> b) Потому что сохранение raw code units важно для backward compatibility и binary fidelity операций  

> [!question] Почему IEEE 754 модель чисел критична для engine-level optimization strategies?  
> a) Потому что BigInt быстрее  
> b) Потому что она напрямую отображается на hardware FP units (SIMD/FPU), позволяя эффективную компиляцию  
> c) Потому что Number — это string  
> d) Потому что JS не использует числа  
>> [!success]- Answer  
>> b) Потому что она напрямую отображается на hardware FP units (SIMD/FPU), позволяя эффективную компиляцию  

> [!question] Почему prototype chain mutation invalidates hidden class / inline cache assumptions даже при неизменной shape объекта?  
> a) Потому что hidden class зависит от GC  
> b) Потому что property resolution semantics включают prototype chain traversal, ломая cached lookup paths  
> c) Потому что Proxy всегда отключает IC  
> d) Потому что Array становится invalid  
>> [!success]- Answer  
>> b) Потому что property resolution semantics включают prototype chain traversal, ломая cached lookup paths  

> [!question] Почему Early Error phase нельзя рассматривать как runtime validation без изменения модели исполнения?  
> a) Потому что он выполняется после выполнения кода  
> b) Потому что он предотвращает создание execution context и исключает любые observable side effects  
> c) Потому что он зависит от GC  
> d) Потому что он опционален  
>> [!success]- Answer  
>> b) Потому что он предотвращает создание execution context и исключает любые observable side effects  

> [!question] Почему Completion Records являются фундаментом formal semantics control flow в ECMAScript?  
> a) Потому что они описывают только exceptions  
> b) Потому что они моделируют propagation и override нормальных и abrupt completion через все уровни evaluation  
> c) Потому что они отключают async execution  
> d) Потому что они используются только в loops  
>> [!success]- Answer  
>> b) Потому что они моделируют propagation и override нормальных и abrupt completion через все уровни evaluation  


> [!question] Почему Iterator-based pipelines (map/filter/take) усложняют escape analysis и lifetime prediction в JIT по сравнению с Array chains?  
> a) Потому что Iterator всегда требует heap allocation для каждого step  
> b) Потому что lazy pull-based execution переносит точки использования значений, делая lifetime объектов менее статически предсказуемым  
> c) Потому что Array запрещает inline caching  
> d) Потому что Iterator отключает prototype chain optimization  
>> [!success]- Answer  
>> b) Потому что lazy pull-based execution переносит точки использования значений, делая lifetime объектов менее статически предсказуемым  

> [!question] Почему Set semantics не могут быть безопасно заменены на Map-based implementation без изменения observable behavior?  
> a) Потому что Map не поддерживает NaN  
> b) Потому что Set требует uniqueness semantics на уровне SameValueZero без key-value separation  
> c) Потому что Set не iterable  
> d) Потому что Map всегда отсортирован  
>> [!success]- Answer  
>> b) Потому что Set требует uniqueness semantics на уровне SameValueZero без key-value separation  

> [!question] Почему Import Attributes влияют на module security model ещё до parsing phase module body?  
> a) Потому что они выполняются после execution  
> b) Потому что они участвуют в module resolution и loader selection до evaluation  
> c) Потому что они заменяют MIME headers полностью  
> d) Потому что они отключают dynamic import  
>> [!success]- Answer  
>> b) Потому что они участвуют в module resolution и loader selection до evaluation  

> [!question] Почему Float16Array особенно опасен для gradient-based optimization algorithms при длительном обучении моделей?  
> a) Потому что он делает gradients integer-only  
> b) Потому что reduced precision ускоряет drift накопления ошибок в последовательных обновлениях параметров  
> c) Потому что он отключает multiplication  
> d) Потому что он несовместим с SIMD  
>> [!success]- Answer  
>> b) Потому что reduced precision ускоряет drift накопления ошибок в последовательных обновлениях параметров  

> [!question] Почему Host-defined hooks делают невозможным доказательство полного semantic equivalence даже между compliant ECMAScript engines?  
> a) Потому что они запрещают strict mode  
> b) Потому что они вводят observable behavior outside ECMAScript core specification  
> c) Потому что они отключают GC  
> d) Потому что они меняют syntax grammar  
>> [!success]- Answer  
>> b) Потому что они вводят observable behavior outside ECMAScript core specification  

> [!question] Почему Normative Optional features не могут считаться частью strict conformance baseline?  
> a) Потому что они deprecated  
> b) Потому что baseline conformance определяется mandatory semantics subset, а optional features не обязательны  
> c) Потому что они всегда polyfilled  
> d) Потому что они относятся только к legacy browsers  
>> [!success]- Answer  
>> b) Потому что baseline conformance определяется mandatory semantics subset, а optional features не обязательны  

> [!question] Почему ill-formed UTF-16 strings нельзя автоматически исправлять на этапе runtime без breaking backward compatibility?  
> a) Потому что UTF-16 запрещён в ECMAScript  
> b) Потому что raw code unit representation является observable и используется в существующих алгоритмах сравнения и сериализации  
> c) Потому что strings are mutable  
> d) Потому что Unicode требует normalization  
>> [!success]- Answer  
>> b) Потому что raw code unit representation является observable и используется в существующих алгоритмах сравнения и сериализации  

> [!question] Почему IEEE 754 Number model остаётся основой ECMAScript despite known precision issues?  
> a) Потому что BigInt заменяет все числа  
> b) Потому что hardware FP units provide performance and deterministic low-level behavior across platforms  
> c) Потому что Number is deprecated  
> d) Потому что JS does not support decimals  
>> [!success]- Answer  
>> b) Потому что hardware FP units provide performance and deterministic low-level behavior across platforms  

> [!question] Почему prototype chain mutation invalidates inline cache assumptions даже при стабильной hidden class?  
> a) Потому что hidden class depends on GC state  
> b) Потому что property lookup resolution depends on prototype traversal, invalidating cached access paths  
> c) Потому что Proxy always disables caching  
> d) Потому что Array becomes immutable  
>> [!success]- Answer  
>> b) Потому что property lookup resolution depends on prototype traversal, invalidating cached access paths  

> [!question] Почему Early Error phase является ключевым элементом static safety в ECMAScript execution model?  
> a) Потому что it runs after execution  
> b) Потому что it prevents execution context creation, ensuring no partial evaluation occurs  
> c) Потому что it depends on GC  
> d) Потому что it is optional runtime feature  
>> [!success]- Answer  
>> b) Потому что it prevents execution context creation, ensuring no partial evaluation occurs  

> [!question] Почему Completion Records являются фундаментальной абстракцией для формализации try/finally semantics?  
> a) Потому что they only represent exceptions  
> b) Потому что they encode propagation and override of normal and abrupt completions through nested evaluation rules  
> c) Потому что they disable async execution  
> d) Потому что they are used only in loops  
>> [!success]- Answer  
>> b) Потому что they encode propagation and override of normal and abrupt completions through nested evaluation rules  

> [!question] Почему Iterator-based lazy evaluation делает невозможной полноценную static whole-program optimization в JIT, даже при отсутствии side effects в пользовательском коде?  
> a) Потому что Iterator всегда использует heap memory  
> b) Потому что порядок вычислений становится demand-driven и зависит от consumer context, а не producer graph  
> c) Потому что Array запрещает оптимизации  
> d) Потому что Iterator отключает inline caches  
>> [!success]- Answer  
>> b) Потому что порядок вычислений становится demand-driven и зависит от consumer context, а не producer graph  

> [!question] Почему Set.prototype methods (union/intersection/difference) нельзя без потерь заменить на sorted array merge algorithms?  
> a) Потому что Set всегда отсортирован  
> b) Потому что Set использует SameValueZero semantics и сохраняет insertion order, а не sorted order  
> c) Потому что Array не поддерживает NaN  
> d) Потому что Set не iterable  
>> [!success]- Answer  
>> b) Потому что Set использует SameValueZero semantics и сохраняет insertion order, а не sorted order  

> [!question] Почему Import Attributes изменяют trust boundary module loading system?  
> a) Потому что они заменяют HTTP headers  
> b) Потому что они участвуют в resolution phase и влияют на выбор loader before evaluation  
> c) Потому что они отключают dynamic import  
> d) Потому что они выполняются в runtime phase  
>> [!success]- Answer  
>> b) Потому что они участвуют в resolution phase и влияют на выбор loader before evaluation  

> [!question] Почему Float16Array усиливает numerical instability в deep computational graphs?  
> a) Потому что он превращает float в integer  
> b) Потому что уменьшенная precision ускоряет accumulation of rounding errors across dependency chains  
> c) Потому что он отключает multiplication  
> d) Потому что он несовместим с SIMD  
>> [!success]- Answer  
>> b) Потому что уменьшенная precision ускоряет accumulation of rounding errors across dependency chains  

> [!question] Почему Host-defined hooks фундаментально нарушают assumption of platform independence in ECMAScript?  
> a) Потому что они запрещают strict mode  
> b) Потому что они вводят observable behavior outside ECMAScript core semantics  
> c) Потому что они отключают GC  
> d) Потому что они изменяют syntax grammar  
>> [!success]- Answer  
>> b) Потому что они вводят observable behavior outside ECMAScript core semantics  

> [!question] Почему Normative Optional features не могут быть использованы как основание для cross-engine behavioral guarantees?  
> a) Потому что они deprecated  
> b) Потому что они не входят в mandatory conformance baseline  
> c) Потому что они всегда polyfilled  
> d) Потому что они относятся только к legacy systems  
>> [!success]- Answer  
>> b) Потому что они не входят в mandatory conformance baseline  

> [!question] Почему ill-formed UTF-16 strings сохраняются как first-class values в ECMAScript?  
> a) Потому что Unicode требует strict validation  
> b) Потому что raw 16-bit code unit representation is observable and must remain stable for backward compatibility  
> c) Потому что strings are mutable  
> d) Потому что UTF-16 is deprecated  
>> [!success]- Answer  
>> b) Потому что raw 16-bit code unit representation is observable and must remain stable for backward compatibility  

> [!question] Почему IEEE 754 Number model остаётся core numeric type despite precision loss issues?  
> a) Потому что BigInt replaces all arithmetic  
> b) Потому что it maps directly to hardware floating-point units enabling efficient execution  
> c) Потому что Number is string-based  
> d) Потому что JS does not support arithmetic  
>> [!success]- Answer  
>> b) Потому что it maps directly to hardware floating-point units enabling efficient execution  

> [!question] Почему prototype chain mutation invalidates inline cache even при стабильной object shape?  
> a) Потому что GC clears cache on mutation  
> b) Потому что property resolution includes dynamic prototype traversal affecting cached lookup assumptions  
> c) Потому что Proxy always disables IC  
> d) Потому что Array becomes immutable  
>> [!success]- Answer  
>> b) Потому что property resolution includes dynamic prototype traversal affecting cached lookup assumptions  

> [!question] Почему Early Error phase является критическим элементом static validation pipeline ECMAScript?  
> a) Потому что it runs after execution  
> b) Потому что it prevents execution context creation, eliminating partial observable state  
> c) Потому что it depends on runtime GC  
> d) Потому что it is optional optimization  
>> [!success]- Answer  
>> b) Потому что it prevents execution context creation, eliminating partial observable state  

> [!question] Почему Completion Records являются основой formal semantics for control-flow override?  
> a) Потому что they only represent exceptions  
> b) Потому что they encode propagation and override rules for normal and abrupt completion across nested evaluation contexts  
> c) Потому что they disable async execution  
> d) Потому что they are used only in loops  
>> [!success]- Answer  
>> b) Потому что they encode propagation and override rules for normal and abrupt completion across nested evaluation contexts  

> [!question] Почему комбинация Iterator-based lazy evaluation + side-effectful host hooks делает невозможной global reasoning о program execution order в ECMAScript engine optimizations?  
> a) Потому что Iterator всегда выполняется в microtask queue  
> b) Потому что evaluation becomes demand-driven while side effects may be triggered at unpredictable consumption points across host boundary  
> c) Потому что Array blocks optimization  
> d) Потому что GC forces reordering of execution  
>> [!success]- Answer  
>> b) Потому что evaluation becomes demand-driven while side effects may be triggered at unpredictable consumption points across host boundary  

> [!question] Почему Set semantics (SameValueZero + insertion order) создают фундаментальный конфликт с попытками JIT-спекуляции на sorted or hash-stable representations?  
> a) Потому что Set is always hash-based internally  
> b) Because observable iteration order is insertion-based and must remain stable under mutation semantics  
> c) Because Set forbids mutation  
> d) Because Array cannot represent NaN  
>> [!success]- Answer  
>> b) Because observable iteration order is insertion-based and must remain stable under mutation semantics  

> [!question] Почему Import Attributes fundamentally break assumption that module identity depends only on URL string?  
> a) Because they replace module cache  
> b) Because they introduce metadata into resolution key space, changing identity beyond specifier string  
> c) Because they disable dynamic import  
> d) Because they modify syntax tree only  
>> [!success]- Answer  
>> b) Because they introduce metadata into resolution key space, changing identity beyond specifier string  

> [!question] Почему Float16Array introduces non-linear error accumulation that invalidates naive assumptions about associative property in numerical pipelines?  
> a) Because float16 is exact arithmetic  
> b) Because reduced precision causes non-associative rounding behavior across chained operations  
> c) Because it disables SIMD  
> d) Because it converts numbers to BigInt  
>> [!success]- Answer  
>> b) Because reduced precision causes non-associative rounding behavior across chained operations  

> [!question] Почему Host-defined IO hooks делают невозможной formal equivalence даже при identical ECMAScript semantics and identical bytecode execution?  
> a) Because bytecode is non-deterministic  
> b) Because observable effects depend on external environment state not governed by ECMAScript abstract machine  
> c) Because GC is randomized  
> d) Because event loop is undefined  
>> [!success]- Answer  
>> b) Because observable effects depend on external environment state not governed by ECMAScript abstract machine  

> [!question] Почему Normative Optional features создают проблему для static conformance verification tools?  
> a) Because they are mandatory  
> b) Because conformance cannot assume presence or absence, requiring bifurcation of semantic model space  
> c) Because they replace core syntax  
> d) Because they disable parsing  
>> [!success]- Answer  
>> b) Because conformance cannot assume presence or absence, requiring bifurcation of semantic model space  

> [!question] Почему ill-formed UTF-16 representation is essential for reversible string transformations in low-level APIs?  
> a) Because Unicode requires normalization  
> b) Because preserving raw code units ensures lossless round-tripping in binary/string boundary operations  
> c) Because strings are mutable  
> d) Because UTF-16 is compressed  
>> [!success]- Answer  
>> b) Because preserving raw code units ensures lossless round-tripping in binary/string boundary operations  

> [!question] Почему IEEE 754 Number semantics are tightly coupled with JIT speculative optimization strategies?  
> a) Because BigInt is faster  
> b) Because FP hardware allows predictable low-level execution patterns for vectorization and constant folding  
> c) Because Number is string-based  
> d) Because JS disables arithmetic optimization  
>> [!success]- Answer  
>> b) Because FP hardware allows predictable low-level execution patterns for vectorization and constant folding  

> [!question] Почему prototype chain mutation breaks inline cache assumptions even in fully shape-stable objects?  
> a) Because shape includes prototype  
> b) Because property lookup semantics depend on dynamic prototype traversal, invalidating cached resolution paths  
> c) Because Proxy disables JIT entirely  
> d) Because GC resets IC  
>> [!success]- Answer  
>> b) Because property lookup semantics depend on dynamic prototype traversal, invalidating cached resolution paths  

> [!question] Почему Early Error phase является фундаментальным separation boundary between parsing and execution semantics?  
> a) Because it runs during execution  
> b) Because it ensures invalid programs never enter execution context creation stage, eliminating partial evaluation states  
> c) Because it depends on runtime values  
> d) Because it is optional optimization layer  
>> [!success]- Answer  
>> b) Because it ensures invalid programs never enter execution context creation stage, eliminating partial evaluation states  

> [!question] Почему Completion Records являются достаточной формализацией для modeling abrupt control-flow override across nested evaluators?  
> a) Because they only represent exceptions  
> b) Because they unify normal and abrupt completion propagation and define deterministic override semantics (e.g. finally overriding return)  
> c) Because they disable async execution  
> d) Because they are only used in loops  
>> [!success]- Answer  
>> b) Because they unify normal and abrupt completion propagation and define deterministic override semantics (e.g. finally overriding return)  


