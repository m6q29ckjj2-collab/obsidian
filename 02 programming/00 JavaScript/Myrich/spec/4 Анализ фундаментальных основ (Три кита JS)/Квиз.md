#flashquiz/proga/js/spec/4

> [!question] Разработчик пишет библиотеку, которая должна одинаково работать в браузере, Node.js и минималистичной встроенной среде, где отсутствуют DOM, console и Event Loop. Какой архитектурный подход наиболее соответствует модели ECMAScript?
> a) Использовать только стандартные объекты ECMAScript, а взаимодействие со средой вынести через адаптеры хоста.
> b) Использовать console.log как универсальный механизм взаимодействия между компонентами.
> c) Использовать setTimeout для синхронизации всех операций, поскольку он является частью JavaScript.
> d) Использовать DOM API как минимальный стандарт взаимодействия между средами.
>> [!success]- Answer
>> a) Использовать только стандартные объекты ECMAScript, а взаимодействие со средой вынести через адаптеры хоста.

> [!question] Код успешно работает в Chrome, но сразу завершается с ошибкой в D8 после вызова setTimeout(). Какое объяснение наиболее соответствует архитектуре ECMAScript?
> a) D8 реализует устаревшую версию языка.
> b) setTimeout относится к API хоста, а не является частью языка ECMAScript.
> c) JavaScript запрещает асинхронность вне браузера.
> d) D8 поддерживает только синхронный JavaScript.
>> [!success]- Answer
>> b) setTimeout относится к API хоста, а не является частью языка ECMAScript.

> [!question] Почему утверждение «JavaScript умеет выводить текст на экран» архитектурно некорректно?
> a) Потому что вывод текста всегда реализуется средствами конкретной хост-среды.
> b) Потому что вывод возможен только через console.log.
> c) Потому что JavaScript запрещает операции ввода-вывода.
> d) Потому что вывод реализуется сборщиком мусора.
>> [!success]- Answer
>> a) Потому что вывод текста всегда реализуется средствами конкретной хост-среды.

> [!question] Разработчик использует print() как универсальную функцию вывода после успешного тестирования в D8. Какой фундаментальный просчет он допустил?
> a) Он смешал возможности конкретного хоста со спецификацией языка.
> b) Он использовал устаревший синтаксис ECMAScript.
> c) print существует только в строгом режиме.
> d) print является внутренним методом объекта Global.
>> [!success]- Answer
>> a) Он смешал возможности конкретного хоста со спецификацией языка.

> [!question] Почему спецификация ECMAScript принципиально не включает средства ввода-вывода?
> a) Потому что язык проектируется как встраиваемый механизм вычислений, а не как самостоятельная операционная среда.
> b) Потому что ввод-вывод реализуется только через Unicode.
> c) Потому что I/O запрещён стандартом IEEE 754.
> d) Потому что реализация I/O нарушает работу прототипов.
>> [!success]- Answer
>> a) Потому что язык проектируется как встраиваемый механизм вычислений, а не как самостоятельная операционная среда.

> [!question] Если одна и та же функция print() в разных средах приводит к совершенно разному поведению, какой архитектурный вывод является наиболее корректным?
> a) Семантика подобных функций определяется хостом, а не ECMAScript.
> b) Спецификация ECMAScript допускает случайное изменение поведения функций.
> c) JavaScript автоматически адаптирует функции под операционную систему.
> d) Такое различие нарушает соответствие стандарту.
>> [!success]- Answer
>> a) Семантика подобных функций определяется хостом, а не ECMAScript.

> [!question] Согласно модели «Конфета», какую роль выполняет непосредственно язык ECMAScript?
> a) Выполняет алгоритмическое преобразование данных между входом и выходом среды.
> b) Управляет файловой системой.
> c) Планирует выполнение задач Event Loop.
> d) Предоставляет сетевой стек.
>> [!success]- Answer
>> a) Выполняет алгоритмическое преобразование данных между входом и выходом среды.

> [!question] Почему переносимость алгоритмов ECMAScript обычно значительно выше переносимости приложений?
> a) Потому что алгоритмы языка не зависят от конкретных Host API.
> b) Потому что алгоритмы всегда используют DOM.
> c) Потому что приложения автоматически компилируются под любую среду.
> d) Потому что спецификация гарантирует одинаковые API во всех хостах.
>> [!success]- Answer
>> a) Потому что алгоритмы языка не зависят от конкретных Host API.

> [!question] Архитектор предлагает полностью изолировать бизнес-логику от Browser API. Какую фундаментальную идею ECMAScript реализует подобная архитектура?
> a) Разделение трансформации данных и взаимодействия с хостом.
> b) Использование прототипного наследования.
> c) Применение строгого режима.
> d) Использование сборщика мусора.
>> [!success]- Answer
>> a) Разделение трансформации данных и взаимодействия с хостом.

> [!question] Почему утверждение «Event Loop — часть JavaScript» является архитектурной ошибкой?
> a) Потому что механизм планирования задач реализуется конкретной хост-средой, а не спецификацией языка.
> b) Потому что Event Loop существует только в браузерах.
> c) Потому что Event Loop входит в стандарт Unicode.
> d) Потому что Event Loop реализуется внутри синтаксического анализатора.
>> [!success]- Answer
>> a) Потому что механизм планирования задач реализуется конкретной хост-средой, а не спецификацией языка.

> [!question] Во время технического интервью кандидат подробно объясняет очереди microtask и macrotask, но не уточняет, о какой среде выполнения идет речь. Почему такой ответ нельзя считать архитектурно полным?
> a) Потому что модель обработки задач определяется конкретной хост-средой, а не самим ECMAScript.
> b) Потому что microtask существуют только в браузерах.
> c) Потому что очередь задач определяется движком JavaScript независимо от хоста.
> d) Потому что Event Loop является частью синтаксиса языка.
>> [!success]- Answer
>> a) Потому что модель обработки задач определяется конкретной хост-средой, а не самим ECMAScript.

> [!question] Библиотека использует только объекты ECMAScript и не содержит ни одного обращения к DOM, console, fetch или setTimeout. Какое преимущество такая архитектура получает в первую очередь?
> a) Максимальную переносимость между различными хост-средами.
> b) Автоматическое ускорение работы JIT-компилятора.
> c) Полную независимость от реализации движка JavaScript.
> d) Возможность работать без runtime.
>> [!success]- Answer
>> a) Максимальную переносимость между различными хост-средами.

> [!question] Почему console.log считается примером плохого критерия при определении возможностей языка?
> a) Потому что его наличие определяется соглашениями конкретной среды, а не спецификацией ECMAScript.
> b) Потому что console.log является устаревшей возможностью языка.
> c) Потому что console.log запрещен в строгом режиме.
> d) Потому что console.log реализован через прототип Object.
>> [!success]- Answer
>> a) Потому что его наличие определяется соглашениями конкретной среды, а не спецификацией ECMAScript.

> [!question] Представьте новую встроенную платформу, которая предоставляет ECMAScript без таймеров, DOM и консоли. Какое утверждение наиболее корректно?
> a) Такая среда полностью соответствует архитектуре ECMAScript, если реализует требования спецификации языка.
> b) Такая реализация нарушает стандарт, поскольку отсутствует Event Loop.
> c) Такая реализация невозможна без console.log.
> d) Такая реализация перестает быть JavaScript.
>> [!success]- Answer
>> a) Такая среда полностью соответствует архитектуре ECMAScript, если реализует требования спецификации языка.

> [!question] Почему сравнение JavaScript с двигателем автомобиля лучше отражает архитектуру языка, чем сравнение с полноценной операционной системой?
> a) Потому что язык предназначен для работы внутри внешней системы, которая определяет способы взаимодействия с окружающим миром.
> b) Потому что ECMAScript реализует аппаратную часть процессора.
> c) Потому что JavaScript управляет операционной системой напрямую.
> d) Потому что браузер является частью спецификации ECMAScript.
>> [!success]- Answer
>> a) Потому что язык предназначен для работы внутри внешней системы, которая определяет способы взаимодействия с окружающим миром.

> [!question] Архитектор утверждает: «Если функция обращается к файловой системе, значит это функциональность JavaScript». Какое возражение наиболее точно следует из модели Host/Runtime?
> a) Работа с файловой системой предоставляется хостом и находится за пределами спецификации ECMAScript.
> b) Работа с файлами определяется стандартом Unicode.
> c) Работа с файлами является частью синтаксиса языка.
> d) Работа с файлами реализуется механизмом прототипов.
>> [!success]- Answer
>> a) Работа с файловой системой предоставляется хостом и находится за пределами спецификации ECMAScript.

> [!question] Почему попытка изучать JavaScript исключительно через Browser API приводит к фундаментальным пробелам в понимании языка?
> a) Потому что разработчик начинает воспринимать возможности конкретного хоста как свойства самого ECMAScript.
> b) Потому что браузер использует другой синтаксис языка.
> c) Потому что Browser API запрещает использование объектов.
> d) Потому что браузер не поддерживает стандарт ECMAScript.
>> [!success]- Answer
>> a) Потому что разработчик начинает воспринимать возможности конкретного хоста как свойства самого ECMAScript.

> [!question] При переносе вычислительного алгоритма между Browser, Node.js и встроенным устройством изменения потребовались только в коде взаимодействия с внешним миром. Какой принцип архитектуры это подтверждает?
> a) ECMAScript отвечает за вычисления, а хост — за взаимодействие с окружением.
> b) Все алгоритмы автоматически переписываются движком.
> c) Runtime самостоятельно реализует отсутствующие API.
> d) ECMAScript динамически адаптирует Host API.
>> [!success]- Answer
>> a) ECMAScript отвечает за вычисления, а хост — за взаимодействие с окружением.

> [!question] Какой вывод наиболее точно следует из существования различных реализаций print(), alert() и process.stdout.write()?
> a) Спецификация языка не определяет универсального механизма пользовательского ввода-вывода.
> b) ECMAScript поддерживает несколько конкурирующих стандартов вывода.
> c) Все реализации обязаны поддерживать все перечисленные функции одновременно.
> d) Эти функции являются встроенными объектами ECMAScript.
>> [!success]- Answer
>> a) Спецификация языка не определяет универсального механизма пользовательского ввода-вывода.

> [!question] Какой вопрос архитектор должен задать первым, если код, использующий асинхронные механизмы, начинает вести себя по-разному на двух платформах?
> a) Какая именно хост-среда предоставляет механизм выполнения асинхронных операций?
> b) Какая версия Unicode используется?
> c) Какой сборщик мусора используется движком?
> d) Какой прототип имеет глобальный объект?
>> [!success]- Answer
>> a) Какая именно хост-среда предоставляет механизм выполнения асинхронных операций?

> [!question] Архитектор проектирует ядро приложения так, чтобы оно могло работать как в браузере, так и в Node.js, D8 и новой встраиваемой платформе. Какое решение наиболее соответствует фундаментальной архитектуре ECMAScript?
> a) Все зависимости от Host API должны находиться исключительно на границе системы.
> b) Все вызовы console.log должны быть заменены на print().
> c) Необходимо реализовать собственный Event Loop внутри приложения.
> d) Следует отказаться от стандартных объектов ECMAScript в пользу API браузера.
>> [!success]- Answer
>> a) Все зависимости от Host API должны находиться исключительно на границе системы.

> [!question] Почему переносимость JavaScript-кода определяется не только соответствием спецификации ECMAScript?
> a) Потому что наблюдаемое поведение может зависеть от API, предоставляемых конкретным хостом.
> b) Потому что синтаксис языка отличается между браузерами.
> c) Потому что Unicode реализуется каждым движком по-разному.
> d) Потому что объекты существуют только в браузерах.
>> [!success]- Answer
>> a) Потому что наблюдаемое поведение может зависеть от API, предоставляемых конкретным хостом.

> [!question] В новой платформе отсутствует console, но полностью реализован стандарт ECMAScript. Какой вывод наиболее корректен?
> a) Такая платформа остается полностью совместимой с ECMAScript.
> b) Такая реализация нарушает требования стандарта.
> c) console является обязательным встроенным объектом языка.
> d) Runtime не может существовать без console.
>> [!success]- Answer
>> a) Такая платформа остается полностью совместимой с ECMAScript.

> [!question] Почему профессиональный инженер при анализе поведения программы сначала определяет, где заканчивается ECMAScript и начинается Host API?
> a) Потому что это позволяет локализовать источник проблемы между логикой языка и механизмами среды.
> b) Потому что это ускоряет работу JIT-компилятора.
> c) Потому что это влияет на работу сборщика мусора.
> d) Потому что так требует строгий режим.
>> [!success]- Answer
>> a) Потому что это позволяет локализовать источник проблемы между логикой языка и механизмами среды.

> [!question] Если одинаковый алгоритм ECMAScript работает корректно, но взаимодействие с пользователем отличается между платформами, какой компонент архитектуры, вероятнее всего, является причиной?
> a) Хост-среда.
> b) Прототипная модель.
> c) Парсер ECMAScript.
> d) Лексическая грамматика.
>> [!success]- Answer
>> a) Хост-среда.

> [!question] Почему использование Browser API внутри бизнес-логики увеличивает архитектурный долг системы?
> a) Потому что вычислительная логика становится зависимой от конкретной среды выполнения.
> b) Потому что Browser API работают медленнее объектов ECMAScript.
> c) Потому что DOM запрещает использование функций.
> d) Потому что браузер использует другую спецификацию языка.
>> [!success]- Answer
>> a) Потому что вычислительная логика становится зависимой от конкретной среды выполнения.

> [!question] Разработчик утверждает, что Runtime и Host — это одно и то же. Какая архитектурная проблема возникает из такого предположения?
> a) Исчезает различие между механизмом исполнения языка и внешней средой, предоставляющей API.
> b) Перестают существовать прототипы объектов.
> c) Нарушается работа сборщика мусора.
> d) ECMAScript становится интерпретируемым языком.
>> [!success]- Answer
>> a) Исчезает различие между механизмом исполнения языка и внешней средой, предоставляющей API.

> [!question] Почему модель «JavaScript как трансформатор данных» считается более фундаментальной, чем модель «JavaScript как язык браузера»?
> a) Потому что она остается истинной независимо от конкретной среды исполнения.
> b) Потому что браузер больше не использует JavaScript.
> c) Потому что DOM входит в ECMAScript.
> d) Потому что трансформация данных выполняется Event Loop.
>> [!success]- Answer
>> a) Потому что она остается истинной независимо от конкретной среды исполнения.

> [!question] Какое свойство архитектуры ECMAScript позволяет использовать один и тот же движок как внутри браузера, так и внутри встроенного устройства?
> a) Язык не определяет способы взаимодействия с внешним миром и делегирует их хосту.
> b) Язык автоматически обнаруживает оборудование.
> c) Спецификация содержит драйверы устройств.
> d) Event Loop входит в ядро ECMAScript.
>> [!success]- Answer
>> a) Язык не определяет способы взаимодействия с внешним миром и делегирует их хосту.

> [!question] Какой вывод наиболее соответствует философии «первого кита» ECMAScript?
> a) Поведение программы следует анализировать сначала через границы ответственности между ECMAScript и Host API, а уже затем искать ошибки в алгоритмах.
> b) Любая проблема JavaScript связана с синтаксисом языка.
> c) Любая среда обязана реализовывать одинаковый набор API.
> d) Главной задачей инженера является знание Browser API.
>> [!success]- Answer
>> a) Поведение программы следует анализировать сначала через границы ответственности между ECMAScript и Host API, а уже затем искать ошибки в алгоритмах.

> [!question] Представьте гипотетический хост, который предоставляет только механизм загрузки скрипта и передачу входных данных, но не имеет ни консоли, ни таймеров, ни сетевых API. Какой вывод наиболее точно соответствует архитектуре ECMAScript?
> a) Такой хост может быть полностью совместим со спецификацией, если корректно реализует язык.
> b) Такой хост нарушает стандарт из-за отсутствия Event Loop.
> c) Такой хост обязан реализовать console.log для соответствия стандарту.
> d) Такой хост не способен выполнять ECMAScript-программы.
>> [!success]- Answer
>> a) Такой хост может быть полностью совместим со спецификацией, если корректно реализует язык.

> [!question] Архитектор утверждает: «Если библиотека зависит только от ECMAScript, то её можно перенести в любую среду без изменения вычислительной логики». При каком условии это утверждение является корректным?
> a) Если все взаимодействия с окружением изолированы через слой адаптации к Host API.
> b) Если библиотека использует только console.log.
> c) Если библиотека написана в строгом режиме.
> d) Если библиотека не использует объекты.
>> [!success]- Answer
>> a) Если все взаимодействия с окружением изолированы через слой адаптации к Host API.

> [!question] Почему вопрос «Как работает Event Loop?» считается неполным без уточнения среды выполнения?
> a) Потому что спецификация ECMAScript не определяет конкретную модель реализации Event Loop.
> b) Потому что Event Loop существует только в браузерах Chromium.
> c) Потому что Event Loop реализуется исключительно внутри движка V8.
> d) Потому что очередь задач является частью синтаксиса ECMAScript.
>> [!success]- Answer
>> a) Потому что спецификация ECMAScript не определяет конкретную модель реализации Event Loop.

> [!question] Какой архитектурный принцип нарушает библиотека, если её алгоритмы напрямую используют DOM вместо абстрактного интерфейса взаимодействия со средой?
> a) Разделение вычислительной логики и зависимостей от хоста.
> b) Прототипное наследование.
> c) Иммутабельность примитивов.
> d) Правила ранних ошибок.
>> [!success]- Answer
>> a) Разделение вычислительной логики и зависимостей от хоста.

> [!question] Почему утверждение «JavaScript — это язык браузера» является архитектурным упрощением?
> a) Потому что браузер является лишь одной из возможных хост-сред, в которой реализуется ECMAScript.
> b) Потому что браузеры используют собственный язык, отличный от ECMAScript.
> c) Потому что JavaScript существует только внутри V8.
> d) Потому что браузер реализует исключительно DOM.
>> [!success]- Answer
>> a) Потому что браузер является лишь одной из возможных хост-сред, в которой реализуется ECMAScript.

> [!question] При проектировании переносимого ядра приложения какая зависимость считается наиболее опасной с точки зрения архитектуры?
> a) Прямая зависимость бизнес-логики от конкретного Host API.
> b) Использование объектов ECMAScript.
> c) Использование операторов языка.
> d) Использование литералов.
>> [!success]- Answer
>> a) Прямая зависимость бизнес-логики от конкретного Host API.

> [!question] Почему понимание различий между Runtime и Host особенно важно при переносе приложения на новую платформу?
> a) Потому что движок языка и инфраструктура взаимодействия с внешним миром могут изменяться независимо друг от друга.
> b) Потому что Runtime определяет синтаксис ECMAScript.
> c) Потому что Host отвечает за работу прототипов.
> d) Потому что Runtime заменяет спецификацию языка.
>> [!success]- Answer
>> a) Потому что движок языка и инфраструктура взаимодействия с внешним миром могут изменяться независимо друг от друга.

> [!question] Архитектор анализирует систему и утверждает: «Если алгоритм вычислений не зависит от источника входных данных и способа выдачи результата, значит он реализован на правильном уровне абстракции». Какая идея лежит в основе этого утверждения?
> a) ECMAScript должен выполнять роль универсального трансформатора данных, а не механизма взаимодействия с окружением.
> b) Все данные должны храниться в DOM.
> c) Любая программа обязана использовать Event Loop.
> d) Runtime должен реализовывать сетевой стек.
>> [!success]- Answer
>> a) ECMAScript должен выполнять роль универсального трансформатора данных, а не механизма взаимодействия с окружением.

> [!question] Какой вопрос наиболее полезно задать первым при расследовании различий поведения одной и той же программы на двух различных платформах?
> a) Какие возможности и ограничения предоставляет каждая хост-среда поверх одинаковой реализации ECMAScript?
> b) Какой редактор использовался при написании кода?
> c) Какой стиль форматирования применяется в проекте?
> d) Какая кодировка используется в исходном файле?
>> [!success]- Answer
>> a) Какие возможности и ограничения предоставляет каждая хост-среда поверх одинаковой реализации ECMAScript?

> [!question] Какой главный архитектурный навык формирует понимание «первого кита»?
> a) Умение проводить границу между возможностями языка ECMAScript и обязанностями хост-среды при анализе любой проблемы.
> b) Умение запоминать Browser API.
> c) Умение пользоваться DevTools.
> d) Умение писать сложные регулярные выражения.
>> [!success]- Answer
>> a) Умение проводить границу между возможностями языка ECMAScript и обязанностями хост-среды при анализе любой проблемы.


