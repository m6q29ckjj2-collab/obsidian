#flashquiz/proga/js/spec/5

> [!question] Архитектор утверждает: «Если убрать Host, JavaScript останется полноценным языком программирования общего назначения». Какой контраргумент наиболее точно соответствует архитектуре ECMAScript?
> a) Без Host язык сохраняет вычислительную модель, но теряет возможность взаимодействия с внешним миром.
> b) Без Host перестают существовать объекты ECMAScript.
> c) Без Host перестает работать прототипное наследование.
> d) Без Host спецификация ECMAScript становится недействительной.
>> [!success]- Answer
>> a) Без Host язык сохраняет вычислительную модель, но теряет возможность взаимодействия с внешним миром.

> [!question] Почему модель «Ключ → Значение» рассматривается как фундаментальная архитектурная абстракция, а не просто как способ хранения данных?
> a) Потому что более сложные структуры языка являются композицией таких пар, а не отдельными фундаментальными сущностями.
> b) Потому что ECMAScript поддерживает только объекты.
> c) Потому что массивы не используют пары ключ-значение.
> d) Потому что спецификация запрещает вложенные структуры.
>> [!success]- Answer
>> a) Потому что более сложные структуры языка являются композицией таких пар, а не отдельными фундаментальными сущностями.

> [!question] Если алгоритм поиска свойства начинается не с прототипа, а с самого объекта, какое фундаментальное свойство объектной модели этим обеспечивается?
> a) Собственные свойства всегда имеют приоритет над делегированными.
> b) Прототип полностью заменяет содержимое объекта.
> c) Все свойства обязаны храниться в прототипе.
> d) Поиск всегда начинается с глобального объекта.
>> [!success]- Answer
>> a) Собственные свойства всегда имеют приоритет над делегированными.

> [!question] Почему Internal Slots принципиально отличаются от обычных свойств объекта?
> a) Они являются частью внутренней модели спецификации и не участвуют в обычном механизме доступа к свойствам.
> b) Они существуют только в браузерах.
> c) Их можно получить через Object.keys().
> d) Они автоматически сериализуются в JSON.
>> [!success]- Answer
>> a) Они являются частью внутренней модели спецификации и не участвуют в обычном механизме доступа к свойствам.

> [!question] Какое архитектурное преимущество дает наличие только одного [[Prototype]] у каждого объекта?
> a) Поиск свойства остается линейным и детерминированным без необходимости разрешать конфликты множественного наследования.
> b) Это позволяет хранить больше собственных свойств.
> c) Это делает поиск свойств константным.
> d) Это позволяет полностью отказаться от Internal Slots.
>> [!success]- Answer
>> a) Поиск свойства остается линейным и детерминированным без необходимости разрешать конфликты множественного наследования.

> [!question] Почему модель «Матрешки» считается архитектурно менее точной, чем модель «Стрелочек»?
> a) Потому что физическая вложенность не отражает возможность динамически изменять маршруты делегирования во время выполнения программы.
> b) Потому что объекты никогда не располагаются в памяти отдельно.
> c) Потому что прототипы являются массивами.
> d) Потому что ECMAScript использует исключительно деревья.
>> [!success]- Answer
>> a) Потому что физическая вложенность не отражает возможность динамически изменять маршруты делегирования во время выполнения программы.

> [!question] Если во время выполнения изменить ссылку [[Prototype]] объекта, какое свойство системы остается неизменным?
> a) Алгоритм поиска продолжит следовать новой цепочке делегирования, а не старой.
> b) Все существующие ссылки на объект автоматически исчезнут.
> c) Все собственные свойства будут удалены.
> d) Объект станет недоступен сборщику мусора.
>> [!success]- Answer
>> a) Алгоритм поиска продолжит следовать новой цепочке делегирования, а не старой.

> [!question] Почему движок запрещает создание циклической цепочки прототипов?
> a) Потому что иначе алгоритм поиска свойства потенциально перестанет иметь условие завершения.
> b) Потому что циклы запрещены стандартом Unicode.
> c) Потому что это нарушает работу Event Loop.
> d) Потому что объекты не могут ссылаться друг на друга.
>> [!success]- Answer
>> a) Потому что иначе алгоритм поиска свойства потенциально перестанет иметь условие завершения.

> [!question] Архитектор описывает поиск свойства как «маршрутизацию запроса». Какая особенность спецификации делает подобную аналогию наиболее точной?
> a) Каждый следующий этап поиска определяется внутренней ссылкой делегирования, а не физической вложенностью объектов.
> b) Все объекты располагаются последовательно в памяти.
> c) Поиск осуществляется только среди собственных свойств.
> d) Поиск всегда начинается с глобального объекта.
>> [!success]- Answer
>> a) Каждый следующий этап поиска определяется внутренней ссылкой делегирования, а не физической вложенностью объектов.

> [!question] Почему понимание Internal Slots считается более фундаментальным, чем знание синтаксиса обращения к свойствам?
> a) Потому что именно Internal Slots определяют алгоритмы поведения объектов, тогда как синтаксис является лишь внешней формой доступа.
> b) Потому что синтаксис постоянно меняется между реализациями.
> c) Потому что Internal Slots являются частью DOM.
> d) Потому что свойства объектов вычисляются исключительно Event Loop.
>> [!success]- Answer
>> a) Потому что именно Internal Slots определяют алгоритмы поведения объектов, тогда как синтаксис является лишь внешней формой доступа.

> [!question] Два объекта имеют одинаковый набор собственных свойств, но разные значения [[Prototype]]. Какое утверждение наиболее точно описывает возможное различие их поведения?
> a) Поведение при поиске отсутствующих свойств может различаться, несмотря на идентичные собственные свойства.
> b) Такие объекты считаются полностью эквивалентными спецификации.
> c) Различаться может только порядок сериализации JSON.
> d) Различаться могут только примитивные значения свойств.
>> [!success]- Answer
>> a) Поведение при поиске отсутствующих свойств может различаться, несмотря на идентичные собственные свойства.

> [!question] Почему делегирование через [[Prototype]] считается более масштабируемой моделью, чем копирование свойств между объектами?
> a) Потому что изменение общего источника поведения автоматически становится доступным всем объектам, использующим делегирование.
> b) Потому что копирование запрещено спецификацией ECMAScript.
> c) Потому что прототип содержит только примитивы.
> d) Потому что делегирование исключает поиск свойств.
>> [!success]- Answer
>> a) Потому что изменение общего источника поведения автоматически становится доступным всем объектам, использующим делегирование.

> [!question] Архитектор называет [[Prototype]] «маршрутизатором поиска», а не «хранилищем данных». Почему это определение точнее?
> a) Потому что слот определяет направление продолжения алгоритма поиска, а не хранит искомое значение.
> b) Потому что [[Prototype]] всегда содержит функции.
> c) Потому что [[Prototype]] принадлежит только массивам.
> d) Потому что [[Prototype]] хранит очередь задач.
>> [!success]- Answer
>> a) Потому что слот определяет направление продолжения алгоритма поиска, а не хранит искомое значение.

> [!question] Почему возможность изменения цепочки прототипов во время выполнения усложняет оптимизацию движка?
> a) Потому что ранее корректный маршрут поиска может стать неверным после изменения ссылки делегирования.
> b) Потому что после изменения прототипа объект становится неизменяемым.
> c) Потому что все свойства автоматически копируются.
> d) Потому что ECMAScript запрещает повторный поиск свойств.
>> [!success]- Answer
>> a) Потому что ранее корректный маршрут поиска может стать неверным после изменения ссылки делегирования.

> [!question] Почему модель «стрелочек» лучше объясняет работу памяти, чем модель вложенных объектов?
> a) Потому что объекты существуют независимо, а связи между ними представлены ссылками, которые можно изменять.
> b) Потому что объекты физически располагаются внутри друг друга.
> c) Потому что спецификация требует непрерывного размещения объектов в памяти.
> d) Потому что все объекты находятся в глобальной области памяти.
>> [!success]- Answer
>> a) Потому что объекты существуют независимо, а связи между ними представлены ссылками, которые можно изменять.

> [!question] В системе существует длинная цепочка делегирования. Какое свойство алгоритма гарантирует завершение поиска?
> a) Последним элементом цепочки является null, который завершает алгоритм поиска.
> b) Последним элементом всегда становится глобальный объект.
> c) Поиск автоматически ограничивается пятью переходами.
> d) Движок прекращает поиск после первого Internal Slot.
>> [!success]- Answer
>> a) Последним элементом цепочки является null, который завершает алгоритм поиска.

> [!question] Почему запрет циклических ссылок в цепочке прототипов относится к корректности алгоритма, а не только к производительности?
> a) Потому что цикл нарушает фундаментальное свойство завершимости алгоритма поиска.
> b) Потому что цикл делает объекты неизменяемыми.
> c) Потому что цикл запрещает создание новых свойств.
> d) Потому что цикл нарушает работу Event Loop.
>> [!success]- Answer
>> a) Потому что цикл нарушает фундаментальное свойство завершимости алгоритма поиска.

> [!question] Почему спецификация описывает внутренние слоты отдельно от свойств объекта?
> a) Потому что они являются частью внутреннего состояния алгоритмов языка, а не пользовательской модели данных.
> b) Потому что они принадлежат исключительно браузеру.
> c) Потому что они автоматически сериализуются вместе с объектом.
> d) Потому что они создаются оператором new.
>> [!success]- Answer
>> a) Потому что они являются частью внутреннего состояния алгоритмов языка, а не пользовательской модели данных.

> [!question] В чем заключается главное архитектурное отличие поиска идентификатора от поиска значения в обычной ассоциативной таблице?
> a) Поиск способен автоматически продолжаться по цепочке делегирования через Internal Slots.
> b) Поиск всегда выполняется бинарным алгоритмом.
> c) Поиск никогда не выходит за пределы текущего объекта.
> d) Поиск всегда завершается глобальным объектом.
>> [!success]- Answer
>> a) Поиск способен автоматически продолжаться по цепочке делегирования через Internal Slots.

> [!question] Какой общий принцип объединяет объектную модель JavaScript и объяснение через «записную книжку»?
> a) Сложные структуры представляют собой композицию простых механизмов поиска по парам «ключ-значение».
> b) Все объекты обязаны иметь одинаковый набор свойств.
> c) Все свойства являются внутренними слотами.
> d) Каждое значение обязано быть объектом.
>> [!success]- Answer
>> a) Сложные структуры представляют собой композицию простых механизмов поиска по парам «ключ-значение».


> [!question] Если два объекта имеют одинаковые собственные свойства, но один из них использует null-прототип, какое поведение наиболее вероятно при вызове метода, отсутствующего в объекте?
> a) Объект с null-прототипом не сможет найти метод в цепочке делегирования и завершит поиск сразу.
> b) Объект с null-прототипом автоматически наследует методы Object.prototype.
> c) Оба объекта всегда ведут себя одинаково, так как собственные свойства доминируют.
> d) Объект с null-прототипом перенаправит поиск в глобальный объект.
>> [!success]- Answer
>> a) Объект с null-прототипом не сможет найти метод в цепочке делегирования и завершит поиск сразу.

> [!question] Почему изменение [[Prototype]] во время выполнения считается «динамическим изменением маршрута», а не просто заменой ссылки?
> a) Потому что это немедленно влияет на алгоритм последующих поисков свойств без изменения самих объектов.
> b) Потому что это приводит к пересборке всего runtime.
> c) Потому что это изменяет тип объекта на примитив.
> d) Потому что это запрещено спецификацией ECMAScript.
>> [!success]- Answer
>> a) Потому что это немедленно влияет на алгоритм последующих поисков свойств без изменения самих объектов.

> [!question] Какой вывод следует из того, что Internal Slots недоступны через обычный механизм свойств?
> a) Они определяют поведение алгоритмов, а не участвуют в модели данных, доступной разработчику.
> b) Они могут быть изменены через Object.keys().
> c) Они существуют только для массивов.
> d) Они являются синтаксическим сахаром над свойствами.
>> [!success]- Answer
>> a) Они определяют поведение алгоритмов, а не участвуют в модели данных, доступной разработчику.

> [!question] Почему модель «стрелочек» более точно описывает прототипное наследование, чем модель вложенных объектов?
> a) Потому что она отражает независимость объектов и наличие изменяемых ссылок между ними.
> b) Потому что объекты физически находятся внутри друг друга в памяти.
> c) Потому что прототипы копируются при создании объекта.
> d) Потому что цепочка прототипов является статической.
>> [!success]- Answer
>> a) Потому что она отражает независимость объектов и наличие изменяемых ссылок между ними.

> [!question] Что произойдет с алгоритмом поиска свойства при циклической ссылке в цепочке прототипов?
> a) Алгоритм может не завершиться, поэтому спецификация запрещает такие структуры.
> b) Поиск автоматически перейдет в режим FIFO.
> c) Цикл будет проигнорирован и заменен на null.
> d) Движок создаст копию объекта для разрыва цикла.
>> [!success]- Answer
>> a) Алгоритм может не завершиться, поэтому спецификация запрещает такие структуры.

> [!question] Почему «матрешечная» модель приводит к неверному пониманию памяти в JavaScript?
> a) Потому что она предполагает физическую вложенность объектов, которой в реальности нет.
> b) Потому что она описывает только primitive values.
> c) Потому что она исключает возможность прототипов.
> d) Потому что она запрещена спецификацией ECMAScript.
>> [!success]- Answer
>> a) Потому что она предполагает физическую вложенность объектов, которой в реальности нет.

> [!question] Какой архитектурный смысл у правила «поиск останавливается на null»?
> a) Это формальное условие завершения алгоритма делегирования.
> b) Это ограничение памяти движка.
> c) Это оптимизация сериализации объектов.
> d) Это поведение только в браузерах.
>> [!success]- Answer
>> a) Это формальное условие завершения алгоритма делегирования.

> [!question] Почему Internal Slots считаются критически важными для понимания поведения объектов?
> a) Потому что они описывают внутреннее состояние и алгоритмы, которые не доступны через обычный API.
> b) Потому что они заменяют свойства объектов.
> c) Потому что они существуют только в Node.js.
> d) Потому что они являются частью DOM.
>> [!success]- Answer
>> a) Потому что они описывают внутреннее состояние и алгоритмы, которые не доступны через обычный API.

> [!question] Какой механизм объясняет возможность «на лету» изменить поведение всех экземпляров объекта?
> a) Делегирование через общий [[Prototype]]-объект.
> b) Копирование методов при создании объекта.
> c) Глобальная мутация всех функций.
> d) Замена Event Loop.
>> [!success]- Answer
>> a) Делегирование через общий [[Prototype]]-объект.

> [!question] Почему поиск свойства в JS нельзя полностью свести к работе хэш-таблицы?
> a) Потому что поиск может продолжаться по цепочке делегирования через Internal Slots.
> b) Потому что ключи всегда сортируются.
> c) Потому что значения всегда уникальны.
> d) Потому что объект всегда имеет фиксированный размер.
>> [!success]- Answer
>> a) Потому что поиск может продолжаться по цепочке делегирования через Internal Slots.

> [!question] Почему модель «записной книжки» лучше объясняет объектную систему JavaScript, чем классическая табличная модель ключ-значение?
> a) Потому что она позволяет интуитивно представить вложенные структуры как композицию простых ассоциативных связей.
> b) Потому что она предполагает физическое хранение данных в виде списка.
> c) Потому что она исключает необходимость прототипов.
> d) Потому что она описывает только работу Event Loop.
>> [!success]- Answer
>> a) Потому что она позволяет интуитивно представить вложенные структуры как композицию простых ассоциативных связей.

> [!question] Если объект A имеет [[Prototype]] → B, а B имеет [[Prototype]] → null, что произойдет при поиске несуществующего свойства?
> a) Поиск завершится на B и вернет undefined после полного обхода цепочки.
> b) Поиск продолжится в глобальном объекте.
> c) Поиск переключится на Event Loop.
> d) Поиск будет кэширован в A.
>> [!success]- Answer
>> a) Поиск завершится на B и вернет undefined после полного обхода цепочки.

> [!question] Почему Internal Slots нельзя рассматривать как обычные свойства объекта?
> a) Потому что они являются частью внутренней спецификационной модели и не участвуют в стандартной модели доступа.
> b) Потому что они автоматически сериализуются в JSON.
> c) Потому что они доступны через for...in.
> d) Потому что они существуют только у функций.
>> [!success]- Answer
>> a) Потому что они являются частью внутренней спецификационной модели и не участвуют в стандартной модели доступа.

> [!question] Какой эффект дает возможность динамически менять [[Prototype]] объекта во время выполнения?
> a) Изменяется маршрут поиска свойств без изменения самих объектов или их собственных свойств.
> b) Объект полностью пересоздается.
> c) Все свойства копируются в новый объект.
> d) Объект становится примитивом.
>> [!success]- Answer
>> a) Изменяется маршрут поиска свойств без изменения самих объектов или их собственных свойств.

> [!question] Почему «стрелочная модель» лучше отражает поведение прототипов, чем модель вложенных объектов?
> a) Потому что связи между объектами являются ссылочными и могут изменяться во время выполнения.
> b) Потому что объекты физически вложены друг в друга.
> c) Потому что прототипы являются статическими структурами.
> d) Потому что цепочка всегда копируется при создании объекта.
>> [!success]- Answer
>> a) Потому что связи между объектами являются ссылочными и могут изменяться во время выполнения.

> [!question] Почему циклическая ссылка в цепочке прототипов считается критической ошибкой модели?
> a) Потому что она нарушает условие завершимости алгоритма поиска свойства.
> b) Потому что она запрещает создание объектов через new.
> c) Потому что она изменяет тип данных объекта.
> d) Потому что она блокирует Event Loop.
>> [!success]- Answer
>> a) Потому что она нарушает условие завершимости алгоритма поиска свойства.

> [!question] Что является главным отличием прототипного делегирования от копирования методов?
> a) При делегировании изменения в прототипе автоматически отражаются на всех связанных объектах.
> b) При делегировании создается копия всех методов.
> c) При копировании сохраняется ссылка на оригинал прототипа.
> d) При копировании исчезает возможность наследования.
>> [!success]- Answer
>> a) При делегировании изменения в прототипе автоматически отражаются на всех связанных объектах.

> [!question] Почему поиск свойства нельзя считать чисто локальной операцией объекта?
> a) Потому что он может продолжаться по цепочке прототипов через Internal Slots.
> b) Потому что он зависит от Event Loop.
> c) Потому что он всегда обращается к DOM.
> d) Потому что он выполняется асинхронно.
>> [!success]- Answer
>> a) Потому что он может продолжаться по цепочке прототипов через Internal Slots.

> [!question] Какое утверждение наиболее точно отражает смысл Internal Slots в архитектуре языка?
> a) Это скрытые механизмы, определяющие поведение объектов на уровне спецификации, недоступные напрямую разработчику.
> b) Это альтернативный синтаксис объектов.
> c) Это способ сериализации объектов в JSON.
> d) Это часть стандартных библиотек ECMAScript.
>> [!success]- Answer
>> a) Это скрытые механизмы, определяющие поведение объектов на уровне спецификации, недоступные напрямую разработчику.

> [!question] Почему модель «стрелочек» критична для понимания производительности и оптимизации движка?
> a) Потому что изменение ссылок может инвалидировать ранее построенные пути поиска свойств.
> b) Потому что она влияет на синтаксис языка.
> c) Потому что она запрещает использование объектов.
> d) Потому что она фиксирует структуру памяти.
>> [!success]- Answer
>> a) Потому что изменение ссылок может инвалидировать ранее построенные пути поиска свойств.


> [!question] Архитектор утверждает: «Если понимать JavaScript как вложенную структуру объектов, можно полностью предсказать поведение программы». Почему это утверждение архитектурно неверно?
> a) Потому что модель вложенности игнорирует динамические изменения связей через [[Prototype]].
> b) Потому что объекты не могут хранить значения.
> c) Потому что JavaScript не использует объекты.
> d) Потому что Event Loop изменяет синтаксис языка.
>> [!success]- Answer
>> a) Потому что модель вложенности игнорирует динамические изменения связей через [[Prototype]].

> [!question] Какой фундаментальный принцип нарушается при попытке моделировать прототипную систему как иерархию физических вложенных объектов?
> a) Разделение между физической структурой памяти и логическими ссылочными связями.
> b) Ограничение на количество объектов в памяти.
> c) Запрет на использование объектов как значений.
> d) Требование наличия DOM.
>> [!success]- Answer
>> a) Разделение между физической структурой памяти и логическими ссылочными связями.

> [!question] Почему изменение [[Prototype]] нельзя считать операцией изменения содержимого объекта?
> a) Потому что изменяется маршрут поиска свойств, а не набор собственных свойств объекта.
> b) Потому что объект удаляется и создается заново.
> c) Потому что свойства автоматически копируются.
> d) Потому что объект становится примитивом.
>> [!success]- Answer
>> a) Потому что изменяется маршрут поиска свойств, а не набор собственных свойств объекта.

> [!question] Если два объекта имеют одинаковые собственные свойства, но разные цепочки прототипов, какое утверждение является корректным?
> a) Их поведение может различаться при доступе к отсутствующим свойствам.
> b) Они всегда ведут себя одинаково в любых условиях.
> c) Они различаются только при сериализации.
> d) Они считаются идентичными на уровне спецификации.
>> [!success]- Answer
>> a) Их поведение может различаться при доступе к отсутствующим свойствам.

> [!question] Почему Internal Slots являются ключевым элементом архитектуры, а не просто деталью реализации?
> a) Потому что они определяют поведение алгоритмов языка на уровне спецификации, а не синтаксиса.
> b) Потому что они доступны через Object.keys().
> c) Потому что они являются частью DOM API.
> d) Потому что они заменяют прототипы.
>> [!success]- Answer
>> a) Потому что они определяют поведение алгоритмов языка на уровне спецификации, а не синтаксиса.

> [!question] Какой эффект оказывает цепочка прототипов на сложность поиска свойства?
> a) Поиск может продолжаться через несколько объектов, пока не будет найдено значение или достигнут null.
> b) Поиск всегда выполняется за O(1).
> c) Поиск выполняется только в текущем объекте.
> d) Поиск выполняется через Event Loop.
>> [!success]- Answer
>> a) Поиск может продолжаться через несколько объектов, пока не будет найдено значение или достигнут null.

> [!question] Почему модель «стрелочек» является более точной ментальной моделью, чем «матрешка»?
> a) Потому что она отражает изменяемые ссылочные связи между независимыми объектами.
> b) Потому что она описывает физическую вложенность объектов.
> c) Потому что она исключает возможность прототипов.
> d) Потому что она фиксирует структуру памяти.
>> [!success]- Answer
>> a) Потому что она отражает изменяемые ссылочные связи между независимыми объектами.

> [!question] Что происходит при достижении null в цепочке [[Prototype]]?
> a) Алгоритм поиска завершается без нахождения свойства.
> b) Происходит переход в глобальный объект.
> c) Начинается повторный поиск с корня цепочки.
> d) Объект автоматически клонируется.
>> [!success]- Answer
>> a) Алгоритм поиска завершается без нахождения свойства.

> [!question] Почему возможность циклических ссылок в цепочке прототипов является критической для корректности алгоритма, а не только для производительности?
> a) Потому что она нарушает гарантию завершения алгоритма поиска.
> b) Потому что она изменяет синтаксис языка.
> c) Потому что она запрещает создание объектов.
> d) Потому что она отключает Event Loop.
>> [!success]- Answer
>> a) Потому что она нарушает гарантию завершения алгоритма поиска.



