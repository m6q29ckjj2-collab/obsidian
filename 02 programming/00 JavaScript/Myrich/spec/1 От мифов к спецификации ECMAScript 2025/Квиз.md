
> [!question] Разработчик утверждает: «Я протестировал поведение в Chrome, значит теперь знаю, как работает JavaScript». Какое возражение наиболее точно соответствует философии ECMA-262?
> a) Chrome реализует только часть стандарта ECMAScript.
> b) Наблюдение описывает конкретную реализацию, а не нормативное поведение языка.
> c) Chrome автоматически включает экспериментальные возможности.
> d) Поведение JavaScript зависит исключительно от операционной системы.
>> [!success]- Answer
>> b) Наблюдение описывает конкретную реализацию, а не нормативное поведение языка.
>> Explanation:
>> Спецификация описывает язык нормативно, а эксперимент — лишь конкретную реализацию.
>> a) Современные движки стремятся соответствовать стандарту полностью.
>> c) Это не объясняет фундаментальную проблему вывода.
>> d) Поведение языка определяется спецификацией, а не ОС.

> [!question] Два движка корректно проходят Test262, но в некоторых математических вычислениях возвращают немного разные результаты. Какое объяснение наиболее соответствует ECMA-262?
> a) Один из движков нарушает спецификацию.
> b) Для данной операции допускается implementation-approximated поведение.
> c) Различия допустимы только между браузером и Node.js.
> d) Такое невозможно для conforming implementation.
>> [!success]- Answer
>> b) Для данной операции допускается implementation-approximated поведение.
>> Explanation:
>> Для некоторых математических функций задается идеал, к которому реализации должны максимально приблизиться.
>> a) Различия не всегда означают нарушение.
>> c) Node.js здесь не имеет значения.
>> d) Спецификация допускает подобные случаи.

> [!question] Представьте, что ECMA-262 внезапно начала подробно описывать DOM и Fetch API. Какой принцип был бы нарушен?
> a) Принцип обратной совместимости.
> b) Разделение между языком и хост-окружением.
> c) Требования Unicode.
> d) Conformance.
>> [!success]- Answer
>> b) Разделение между языком и хост-окружением.
>> Explanation:
>> ECMA-262 описывает язык, а не API среды исполнения.
>> Остальные варианты не относятся к разграничению обязанностей спецификации.

> [!question] Почему аналогия «переменная — коробочка» особенно опасна именно при работе с объектами?
> a) Она перестает объяснять существование нескольких идентификаторов, разделяющих один объект.
> b) Она неверно объясняет работу сборщика мусора.
> c) Она запрещена спецификацией.
> d) Она работает только для массивов.
>> [!success]- Answer
>> a) Она перестает объяснять существование нескольких идентификаторов, разделяющих один объект.
>> Explanation:
>> Именно ссылочная семантика разрушает такую модель.
>> Остальные ответы либо вторичны, либо неверны.

> [!question] Разработчик предлагает изменить поведение встроенной функции, которая НЕ помечена implementation-defined, чтобы ускорить движок. Соответствует ли это ECMA-262?
> a) Да, если производительность возрастет.
> b) Да, если остальные движки поступят аналогично.
> c) Нет.
> d) Только если функция относится к Legacy.
>> [!success]- Answer
>> c) Нет.
>> Explanation:
>> Conforming implementation обязана соблюдать нормативное поведение.
>> Производительность не дает права менять семантику.

> [!question] Почему знание спецификации обладает большей предсказательной силой, чем большое количество экспериментов в консоли?
> a) Потому что спецификация описывает фундаментальные правила, а не отдельные наблюдения.
> b) Потому что консоль не исполняет настоящий JavaScript.
> c) Потому что спецификация содержит исходный код V8.
> d) Потому что браузеры всегда содержат ошибки.
>> [!success]- Answer
>> a) Потому что спецификация описывает фундаментальные правила, а не отдельные наблюдения.
>> Explanation:
>> Именно инварианты языка позволяют предсказывать поведение в новых ситуациях.

> [!question] Что наиболее вероятно означает тот факт, что некоторое поведение объявлено implementation-defined?
> a) Реализация обязана максимально приблизиться к математическому идеалу.
> b) Разные conforming implementation могут вести себя по-разному в рамках требований стандарта.
> c) Такое поведение запрещено использовать.
> d) Такое поведение возможно только в Legacy.
>> [!success]- Answer
>> b) Разные conforming implementation могут вести себя по-разному в рамках требований стандарта.
>> Explanation:
>> implementation-defined оставляет выбор реализации за движком.
>> a) Это implementation-approximated.
>> c) Неверно.
>> d) Legacy не связано с implementation-defined.

> [!question] Разработчик утверждает: «DOM — часть JavaScript, потому что он доступен из JavaScript». Что лучше всего отвечает спецификации?
> a) Верно, поскольку DOM описан в ECMA-262.
> b) DOM предоставляет хост, а не язык ECMAScript.
> c) DOM является частью движка JavaScript.
> d) DOM существует только в браузерах благодаря strict mode.
>> [!success]- Answer
>> b) DOM предоставляет хост, а не язык ECMAScript.
>> Explanation:
>> DOM определяется спецификациями хост-среды.
>> Остальные варианты смешивают язык и окружение.

> [!question] Почему conforming implementation обязана следовать актуальной версии Unicode?
> a) Чтобы обеспечить единообразную интерпретацию исходного текста ECMAScript.
> b) Чтобы ускорить работу строк.
> c) Чтобы гарантировать одинаковую кодировку файлов.
> d) Чтобы отказаться от ASCII.
>> [!success]- Answer
>> a) Чтобы обеспечить единообразную интерпретацию исходного текста ECMAScript.
>> Explanation:
>> Это необходимо для одинакового понимания исходного кода всеми реализациями.

> [!question] Почему раздел Normative Optional отличается от Legacy с точки зрения архитектуры языка?
> a) Normative Optional определяет устаревшие возможности.
> b) Legacy можно игнорировать полностью.
> c) Normative Optional — необязательная возможность, но при реализации должна строго соответствовать стандарту; Legacy сохраняется ради обратной совместимости.
> d) Между ними нет принципиальной разницы.
>> [!success]- Answer
>> c) Normative Optional — необязательная возможность, но при реализации должна строго соответствовать стандарту; Legacy сохраняется ради обратной совместимости.
>> Explanation:
>> Это принципиально разные категории.
>> a) Это описание Legacy.
>> b) Legacy обязателен для веб-совместимости.
>> d) Это неверно.

> [!question] Представьте, что разработчик объясняет ссылочную семантику объектов словами: «В коробочке лежит стрелочка на другую коробочку». Почему такое объяснение все равно считается слабой моделью?
> a) Потому что спецификация вообще не использует ссылки.
> b) Потому что модель продолжает описывать вымышленные сущности вместо нормативных абстракций спецификации.
> c) Потому что стрелка всегда копируется.
> d) Потому что объекты не имеют адресов памяти.
>> [!success]- Answer
>> b) Потому что модель продолжает описывать вымышленные сущности вместо нормативных абстракций спецификации.
>> Explanation:
>> Аналогия может помочь интуитивно, но она перестает быть предсказательной и не отражает модель спецификации.

### Quiz 2
> [!question] Разработчик утверждает: «Если изменить свойство `prototype` функции-конструктора, существующие экземпляры автоматически начнут использовать новый прототип». Какое утверждение наиболее корректно?
> a) Верно, поскольку экземпляры всегда читают prototype конструктора динамически.
> b) Неверно, поскольку экземпляр хранит собственную ссылку [[Prototype]], установленную при создании.
> c) Верно только в strict mode.
> d) Верно только для классов.
>> [!success]- Answer
>> b) Неверно, поскольку экземпляр хранит собственную ссылку [[Prototype]], установленную при создании.
>> Explanation:
>> [[Prototype]] устанавливается при создании объекта.
>> a) prototype конструктора больше не участвует в уже созданных экземплярах.
>> c) Strict mode здесь не влияет.
>> d) Классы используют тот же механизм.

> [!question] Почему спецификация разделяет понятия "prototype" и [[Prototype]], несмотря на схожие названия?
> a) Потому что одно существует только в браузерах.
> b) Потому что prototype — обычное свойство объекта-функции, а [[Prototype]] — внутренний механизм наследования любого объекта.
> c) Потому что prototype существует только в strict mode.
> d) Потому что [[Prototype]] используется исключительно массивами.
>> [!success]- Answer
>> b) Потому что prototype — обычное свойство объекта-функции, а [[Prototype]] — внутренний механизм наследования любого объекта.
>> Explanation:
>> Это два принципиально разных понятия.
>> Остальные варианты неверно описывают их назначение.

> [!question] Если свойство отсутствует у объекта, какой следующий шаг алгоритма согласно спецификации?
> a) Немедленно вернуть undefined.
> b) Проверить prototype конструктора.
> c) Перейти к объекту, находящемуся в [[Prototype]].
> d) Выполнить поиск среди всех объектов программы.
>> [!success]- Answer
>> c) Перейти к объекту, находящемуся в [[Prototype]].
>> Explanation:
>> Именно так работает Prototype Chain.
>> a) undefined возвращается только после завершения поиска.
>> b) prototype конструктора не участвует.
>> d) Такого алгоритма не существует.

> [!question] Почему поиск свойства через цепочку прототипов не может продолжаться бесконечно?
> a) Потому что движок ограничивает глубину поиска.
> b) Потому что цепочка обязана завершиться значением null.
> c) Потому что prototype всегда равен Object.prototype.
> d) Потому что strict mode запрещает длинные цепочки.
>> [!success]- Answer
>> b) Потому что цепочка обязана завершиться значением null.
>> Explanation:
>> Именно null завершает поиск.

> [!question] Какое утверждение наиболее точно отражает отличие объектной модели ECMAScript от Java?
> a) Объекты в ECMAScript не имеют типов.
> b) Наследование строится вокруг цепочек прототипов, а свойства могут динамически изменяться во время выполнения.
> c) ECMAScript использует множественное наследование.
> d) ECMAScript не поддерживает наследование.
>> [!success]- Answer
>> b) Наследование строится вокруг цепочек прототипов, а свойства могут динамически изменяться во время выполнения.
>> Explanation:
>> Именно динамическая объектная модель отличает ECMAScript.
>> Остальные варианты неверны.

> [!question] Почему спецификация описывает объект как коллекцию свойств, а не полей класса?
> a) Потому что объект может менять свою структуру во время выполнения.
> b) Потому что классов в ECMAScript нет.
> c) Потому что свойства существуют только у функций.
> d) Потому что это связано исключительно с производительностью.
>> [!success]- Answer
>> a) Потому что объект может менять свою структуру во время выполнения.
>> Explanation:
>> Структура объекта не фиксирована.
>> b) Классы существуют как синтаксический сахар.
>> c) Неверно.
>> d) Производительность не является причиной определения.

> [!question] Какой вывод следует из существования атрибута Writable?
> a) Он определяет возможность изменения значения свойства.
> b) Он определяет возможность удаления свойства.
> c) Он определяет возможность наследования свойства.
> d) Он определяет положение свойства в памяти.
>> [!success]- Answer
>> a) Он определяет возможность изменения значения свойства.
>> Explanation:
>> Writable отвечает только за запись.

> [!question] Какой атрибут отвечает за участие свойства в перечислении?
> a) Writable
> b) Enumerable
> c) Configurable
> d) Prototype
>> [!success]- Answer
>> b) Enumerable
>> Explanation:
>> Именно Enumerable определяет участие в перечислении.

> [!question] Если свойство найдено в самом объекте, но то же имя присутствует и выше по цепочке прототипов, какое значение будет использовано?
> a) Значение из ближайшего прототипа.
> b) Значение собственного свойства объекта.
> c) Значения объединяются.
> d) Это implementation-defined.
>> [!success]- Answer
>> b) Значение собственного свойства объекта.
>> Explanation:
>> Own property всегда имеет приоритет.

> [!question] Почему изменение свойства объекта-прототипа способно изменить поведение множества объектов одновременно?
> a) Потому что при каждом обращении поиск выполняется динамически по цепочке [[Prototype]].
> b) Потому что свойства автоматически копируются во все объекты.
> c) Потому что движок перестраивает все объекты после изменения.
> d) Потому что prototype является глобальным объектом.
>> [!success]- Answer
>> a) Потому что при каждом обращении поиск выполняется динамически по цепочке [[Prototype]].
>> Explanation:
>> Объекты разделяют один и тот же прототип.
>> Остальные варианты описывают несуществующие механизмы.

> [!question] Разработчик говорит: «Класс определяет окончательный набор свойств объекта». Почему это утверждение плохо применимо к ECMAScript?
> a) Потому что свойства могут свободно добавляться и удаляться во время выполнения.
> b) Потому что классы не создают объекты.
> c) Потому что классы существуют только в TypeScript.
> d) Потому что свойства хранятся только в прототипах.
>> [!success]- Answer
>> a) Потому что свойства могут свободно добавляться и удаляться во время выполнения.
>> Explanation:
>> Динамическая структура объектов — фундаментальная особенность ECMAScript.

> [!question] Почему ES2015 считается не просто набором новых возможностей, а архитектурным переломом в ECMAScript?
> a) Потому что он добавил поддержку классов и убрал прототипы.
> b) Потому что он формализовал масштабируемую модель языка (модули, lexical scope, promises, iterators).
> c) Потому что он изменил работу движков на уровне байткода.
> d) Потому что он ввел обязательную типизацию.
>> [!success]- Answer
>> b) Потому что он формализовал масштабируемую модель языка (модули, lexical scope, promises, iterators).
>> Explanation:
>> ES2015 изменил архитектуру языка, а не только API.
>> a) Прототипы не исчезли.
>> c) Байткод — деталь реализации движков.
>> d) Типизация не появилась.

> [!question] Какое из следующих утверждений лучше всего отражает общую тенденцию развития ECMAScript после 2017 года?
> a) Язык постепенно удаляет динамические возможности.
> b) Язык расширяется в сторону асинхронности и удобства работы с данными.
> c) Язык переходит к статической компиляции.
> d) Язык становится строго объектно-ориентированным.
>> [!success]- Answer
>> b) Язык расширяется в сторону асинхронности и удобства работы с данными.
>> Explanation:
>> Async, iterators, data helpers — ключевая линия развития.
>> Остальные варианты не соответствуют реальной эволюции.

> [!question] Почему появление async/await в ES2017 нельзя рассматривать как добавление «просто синтаксического сахара»?
> a) Потому что оно изменило модель выполнения кода в движке.
> b) Потому что оно заменило промисы.
> c) Потому что оно добавило новые типы данных.
> d) Потому что оно отключило генераторы.
>> [!success]- Answer
>> a) Потому что оно изменило модель выполнения кода в движке.
>> Explanation:
>> Async/await влияет на композицию асинхронных операций.
>> b) Promises остались основой.
>> c) Новых типов не добавлено.
>> d) Generators продолжают существовать.

> [!question] Какое утверждение наиболее точно описывает роль SharedArrayBuffer и Atomics (ES2017)?
> a) Они позволяют синхронно выполнять JS в нескольких потоках.
> b) Они вводят модель разделяемой памяти между потоками.
> c) Они заменяют event loop.
> d) Они отключают асинхронность.
>> [!success]- Answer
>> b) Они вводят модель разделяемой памяти между потоками.
>> Explanation:
>> Это фундаментальный сдвиг в модели конкурентности.
>> a) Выполнение все равно кооперативное.
>> c) Event loop не заменяется.
>> d) Асинхронность не исчезает.

> [!question] Какое изменение ES2018 наиболее важно с точки зрения модели асинхронного кода?
> a) Object.groupBy
> b) Async iterators
> c) RegExp improvements
> d) flatMap
>> [!success]- Answer
>> b) Async iterators
>> Explanation:
>> Async iteration напрямую влияет на поток данных во времени.
>> Остальные — утилитарные улучшения.

> [!question] Почему введение Optional Chaining (?.) нельзя рассматривать только как синтаксическое удобство?
> a) Потому что оно изменяет правила вычисления выражений с short-circuit semantics.
> b) Потому что оно добавляет новый тип.
> c) Потому что оно заменяет null.
> d) Потому что оно делает все значения truthy.
>> [!success]- Answer
>> a) Потому что оно изменяет правила вычисления выражений с short-circuit semantics.
>> Explanation:
>> Меняется семантика доступа к свойствам.
>> b) Типы не добавлялись.
>> c) null остается.
>> d) Boolean coercion не меняется.

> [!question] Какое из нововведений ES2020 наиболее сильно влияет на модульную систему?
> a) BigInt
> b) Dynamic import
> c) Nullish coalescing
> d) WeakRef
>> [!success]- Answer
>> b) Dynamic import
>> Explanation:
>> Это изменение модели загрузки модулей во время выполнения.
>> Остальные фичи не связаны с модулями.

> [!question] Почему globalThis считается важным шагом стандартизации, а не просто утилитарным API?
> a) Он вводит новый глобальный объект.
> b) Он унифицирует доступ к глобальному контексту во всех средах выполнения.
> c) Он заменяет window и global.
> d) Он отключает strict mode.
>> [!success]- Answer
>> b) Он унифицирует доступ к глобальному контексту во всех средах выполнения.
>> Explanation:
>> Проблема была именно в различиях сред.
>> a) Глобальный объект уже существовал.
>> c) Он их не заменяет.
>> d) Не связан со strict mode.

> [!question] Какое из утверждений лучше всего описывает WeakRef и FinalizationRegistry?
> a) Они позволяют управлять памятью вручную.
> b) Они дают возможность наблюдать за сборкой мусора косвенно.
> c) Они отключают garbage collector.
> d) Они делают объекты неизменяемыми.
>> [!success]- Answer
>> b) Они дают возможность наблюдать за сборкой мусора косвенно.
>> Explanation:
>> GC остается недетерминированным.
>> a) Ручного управления нет.
>> c) GC не отключается.
>> d) Immutability не связана.

> [!question] Почему методы массивов типа toSorted / toSpliced (ES2023–2024) важны концептуально, а не только практически?
> a) Они ускоряют выполнение массивов.
> b) Они продвигают иммутабельный стиль работы с данными.
> c) Они заменяют старые методы.
> d) Они делают массивы потокобезопасными.
>> [!success]- Answer
>> b) Они продвигают иммутабельный стиль работы с данными.
>> Explanation:
>> Это изменение парадигмы работы с данными.
>> a) Производительность не является целью.
>> c) Старые методы сохраняются.
>> d) Потокобезопасность не гарантируется.

> [!question] Какой общий принцип объединяет изменения ES2020–ES2022?
> a) Упрощение синтаксиса и работа с асинхронностью и структурой данных.
> b) Удаление устаревших возможностей.
> c) Переход к статической типизации.
> d) Отказ от прототипов.
>> [!success]- Answer
>> a) Упрощение синтаксиса и работа с асинхронностью и структурой данных.
>> Explanation:
>> Optional chaining, nullish coalescing, top-level await — одна линия развития.
>> 
>> 


> [!question] Какое утверждение наиболее точно описывает роль strict mode в ECMAScript?
> a) Он включает более строгую типизацию значений.
> b) Он изменяет семантику некоторых операций и запрещает часть небезопасных конструкций.
> c) Он ускоряет выполнение кода во всех движках.
> d) Он отключает prototype chain.
>> [!success]- Answer
>> b) Он изменяет семантику некоторых операций и запрещает часть небезопасных конструкций.
>> Explanation:
>> Strict mode влияет на семантику, а не на типизацию или модель наследования.
>> a) Типизация не меняется.
>> c) Производительность не гарантируется.
>> d) Prototype chain остается.

> [!question] Почему strict mode применяется на уровне отдельных script/функциональных единиц, а не глобально по умолчанию?
> a) Чтобы сохранить обратную совместимость с существующим вебом.
> b) Чтобы ускорить парсинг кода.
> c) Чтобы отключить legacy API.
> d) Чтобы упростить работу GC.
>> [!success]- Answer
>> a) Чтобы сохранить обратную совместимость с существующим вебом.
>> Explanation:
>> ECMAScript обязан сохранять совместимость с огромным количеством legacy-кода.
>> Остальные варианты не являются причиной дизайна.

> [!question] Какое утверждение наиболее точно описывает host environment в ECMAScript?
> a) Это часть спецификации ECMA-262.
> b) Это среда, предоставляющая дополнительные API (DOM, timers, I/O) поверх движка.
> c) Это альтернативный режим strict mode.
> d) Это слой оптимизации движка.
>> [!success]- Answer
>> b) Это среда, предоставляющая дополнительные API (DOM, timers, I/O) поверх движка.
>> Explanation:
>> Host — это внешняя среда, не часть языка.
>> a) Host не входит в ECMA-262.
>> c) Неверно.
>> d) Не связано с оптимизацией.

> [!question] В чем ключевое различие между implementation-defined и implementation-approximated поведением?
> a) Implementation-defined всегда быстрее.
> b) Implementation-defined оставляет выбор реализации, implementation-approximated задает математический идеал.
> c) Они означают одно и то же.
> d) Implementation-approximated применяется только в strict mode.
>> [!success]- Answer
>> b) Implementation-defined оставляет выбор реализации, implementation-approximated задает математический идеал.
>> Explanation:
>> Это разные уровни требований к точности и свободе реализации.
>> Остальные варианты неверны.

> [!question] Что гарантирует conforming implementation в ECMAScript?
> a) Идентичное поведение во всех движках.
> b) Следование спецификации ECMA-262 с допустимыми отклонениями там, где это разрешено.
> c) Использование одинакового JIT-компилятора.
> d) Полную совместимость с DOM.
>> [!success]- Answer
>> b) Следование спецификации ECMA-262 с допустимыми отклонениями там, где это разрешено.
>> Explanation:
>> Конформность не означает идентичность реализации.
>> a) Поведение может различаться в разрешенных местах.
>> c) JIT не стандартизирован.
>> d) DOM не входит в ECMAScript.

> [!question] Почему host hooks являются важной частью модели исполнения ECMAScript?
> a) Потому что они заменяют функции языка.
> b) Потому что они позволяют делегировать операции внешней среде выполнения.
> c) Потому что они ускоряют выполнение кода.
> d) Потому что они управляют памятью напрямую.
>> [!success]- Answer
>> b) Потому что они позволяют делегировать операции внешней среде выполнения.
>> Explanation:
>> Host hooks — это точки интеграции с окружением.
>> Остальные варианты неверно описывают их роль.

> [!question] Какое утверждение наиболее точно описывает взаимодействие ECMAScript и Unicode?
> a) ECMAScript использует Unicode только для строк.
> b) ECMAScript полагается на Unicode для интерпретации исходного текста и строковых данных.
> c) Unicode используется только в браузерах.
> d) Unicode влияет только на регулярные выражения.
>> [!success]- Answer
>> b) ECMAScript полагается на Unicode для интерпретации исходного текста и строковых данных.
>> Explanation:
>> Unicode затрагивает как парсинг, так и runtime-строки.
>> Остальные варианты слишком узкие.

> [!question] Почему спецификация не описывает механизмы ввода-вывода (I/O)?
> a) Потому что I/O слишком медленный для стандартизации.
> b) Потому что I/O относится к host environment, а не к языку.
> c) Потому что I/O запрещен в JavaScript.
> d) Потому что I/O реализуется только через WebAssembly.
>> [!success]- Answer
>> b) Потому что I/O относится к host environment, а не к языку.
>> Explanation:
>> ECMA-262 описывает язык, а не платформу.
>> Остальные варианты неверны.

> [!question] Какое утверждение лучше всего объясняет роль abstract operations в спецификации?
> a) Это реальные функции движка.
> b) Это формальные шаги алгоритмов, описывающие поведение языка независимо от реализации.
> c) Это функции JavaScript runtime API.
> d) Это оптимизации JIT-компилятора.
>> [!success]- Answer
>> b) Это формальные шаги алгоритмов, описывающие поведение языка независимо от реализации.
>> Explanation:
>> Abstract operations — это часть формальной модели, а не код движка.
>> Остальные варианты смешивают уровни абстракции.

> [!question] Что произойдет, если host environment изменит поведение, противоречащее ECMAScript specification?
> a) Поведение станет implementation-defined.
> b) Реализация перестает быть conforming.
> c) ECMAScript автоматически адаптируется.
> d) Strict mode отключит конфликт.
>> [!success]- Answer
>> b) Реализация перестает быть conforming.
>> Explanation:
>> Несоответствие спецификации нарушает conformance.
>> Остальные варианты неверны.

> [!question] Почему разделение “engine vs host” является критически важным для архитектуры JavaScript?
> a) Потому что engine отвечает за DOM.
> b) Потому что оно отделяет язык от среды выполнения и ее API.
> c) Потому что host управляет синтаксисом языка.
> d) Потому что engine зависит от браузера.
>> [!success]- Answer
>> b) Потому что оно отделяет язык от среды выполнения и ее API.
>> Explanation:
>> Это фундаментальная архитектурная граница ECMAScript.
>> Остальные варианты перепутывают роли.

