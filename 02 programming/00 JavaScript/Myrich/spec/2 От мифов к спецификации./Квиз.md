#flashquiz/proga/js/spec/2
> [!question] В ECMAScript различие между implementation-defined, implementation-approximated и host-defined влияет на переносимость кода. Какой вариант корректен?
> a) Math.exp относится к host-defined, а console.log — к implementation-approximated
> b) Math.exp допускает численную аппроксимацию, а I/O определяется хост-средой
> c) Все числовые операции полностью host-defined
> d) Host-defined означает одинаковое поведение во всех реализациях
>> [!success]- Answer
>> b) Math.exp допускает численную аппроксимацию, а I/O определяется хост-средой

> [!question] Почему ECMAScript считается computationally non-self-sufficient?
> a) Потому что отсутствует event loop
> b) Потому что язык не определяет ввод-вывод
> c) Потому что нет асинхронности
> d) Потому что он работает только в браузере
>> [!success]- Answer
>> b) Потому что язык не определяет ввод-вывод

> [!question] Какое утверждение точнее всего описывает exotic objects?
> a) Объекты без прототипа
> b) Объекты с нестандартным поведением внутренних методов
> c) Только встроенные объекты
> d) Неизменяемые объекты
>> [!success]- Answer
>> b) Объекты с нестандартным поведением внутренних методов

> [!question] Какой эффект даёт Iterator API (map/filter/take/drop)?
> a) Замена массивов
> b) Унификация обработки последовательностей
> c) Запрет ленивости
> d) Привязка к Promise
>> [!success]- Answer
>> b) Унификация обработки последовательностей

> [!question] Зачем Set.prototype.union/intersection/difference?
> a) Для оптимизации GC
> b) Для стандартизации операций множеств
> c) Для замены Map
> d) Для сортировки Set
>> [!success]- Answer
>> b) Для стандартизации операций множеств

> [!question] Почему Float16Array важен?
> a) Повышает точность
> b) Уменьшает память и bandwidth
> c) Делает вычисления детерминированными
> d) Заменяет Float64Array
>> [!success]- Answer
>> b) Уменьшает память и bandwidth

> [!question] Что делает RegExp.escape?
> a) Ускоряет regex
> b) Экранирует строки
> c) Заменяет regex
> d) Делает потокобезопасным
>> [!success]- Answer
>> b) Экранирует строки

> [!question] Что делает Promise.try?
> a) Всегда async
> b) Оборачивает результат в Promise
> c) Заменяет async/await
> d) Запрещает ошибки
>> [!success]- Answer
>> b) Оборачивает результат в Promise

> [!question] Как работает prototype chain в памяти?
> a) Копирует методы
> b) Хранит глобально
> c) Делит поведение между объектами
> d) Убирает объекты
>> [!success]- Answer
>> c) Делит поведение между объектами

> [!question] Роль [[Get]] и [[Set]]?
> a) Создание объектов
> b) Доступ к свойствам
> c) Сериализация
> d) Только Proxy
>> [!success]- Answer
>> b) Доступ к свойствам

> [!question] Смысл Strict Mode на уровне source text unit?
> a) Глобально
> b) Можно смешивать режимы
> c) Отключает прототипы
> d) Запрещает классы
>> [!success]- Answer
>> b) Можно смешивать режимы

> [!question] В движке ECMAScript объект X имеет следующий профиль: он обычный object, но у него переопределён [[Get]] и [[Set]] через Proxy. Какой вывод корректен о его классификации?  
> a) Он перестаёт быть ordinary object и становится exotic object  
> b) Он остаётся ordinary object, потому что Proxy не меняет базовый target  
> c) Он становится built-in object  
> d) Он автоматически теряет прототип  
>> [!success]- Answer  
>> a) Он перестаёт быть ordinary object и становится exotic object  

> [!question] Почему prototype lookup не может быть полностью статически оптимизирован?  
> a) Потому что свойства всегда immutable  
> b) Потому что [[Get]] может быть переопределён Proxy  
> c) Потому что prototype chain не существует  
> d) Потому что Object.create запрещает наследование  
>> [!success]- Answer  
>> b) Потому что [[Get]] может быть переопределён Proxy  

> [!question] Когда разница implementation-approximated и implementation-defined критична?  
> a) JSON.parse  
> b) Math.exp в softmax  
> c) console.log  
> d) BigInt serialization  
>> [!success]- Answer  
>> b) Math.exp в softmax  


> [!question] SharedArrayBuffer + Atomics означает:  
> a) Sequential consistency  
> b) Data races always safe  
> c) Limited synchronization without global ordering  
> d) Disables event loop  
>> [!success]- Answer  
>> c) Limited synchronization without global ordering  

> [!question] Почему Set.prototype.union нельзя заменить Array методами?  
> a) Set ordered  
> b) Set enforces uniqueness semantics  
> c) Array no BigInt  
> d) Set immutable  
>> [!success]- Answer  
>> b) Set enforces uniqueness semantics  

> [!question] Strict mode на уровне source text unit означает:  
> a) Whole program strict  
> b) Mixed strict/non-strict possible  
> c) No prototypes  
> d) Only syntax changes  
>> [!success]- Answer  
>> b) Mixed strict/non-strict possible  

> [!question] Почему exotic objects требуют override internal methods?  
> a) String-indexed properties  
> b) No prototype chain  
> c) Strict mode restriction  
> d) No properties  
>> [!success]- Answer  
>> a) String-indexed properties  

> [!question] Когда Promise.try лучше Promise.resolve(fn())?  
> a) async fn  
> b) sync throw possible  
> c) await usage  
> d) after settlement  
>> [!success]- Answer  
>> b) sync throw possible  

> [!question] Почему host-defined ломает portability?  
> a) CPU dependency  
> b) Defined outside spec, varies per environment  
> c) No functions allowed  
> d) Disables GC  
>> [!success]- Answer  
>> b) Defined outside spec, varies per environment  


> [!question] В ECMAScript Proxy перехватывает операции [[Get]] и [[Set]] у target-объекта. Какой вывод о семантике prototype chain наиболее корректен?  
> a) Prototype chain становится неактуальной, потому что Proxy заменяет наследование  
> b) Prototype chain продолжает существовать, но операция доступа может быть перехвачена на уровне абстрактной операции [[Get]]  
> c) Proxy полностью отключает lookup по prototype chain  
> d) Proxy влияет только на собственные свойства объекта, не затрагивая прототипы  
>> [!success]- Answer  
>> b) Prototype chain продолжает существовать, но операция доступа может быть перехвачена на уровне абстрактной операции [[Get]]  

> [!question] Почему движки не могут полностью агрессивно inline-ить доступ к свойствам объекта в ECMAScript?  
> a) Потому что все объекты могут быть frozen  
> b) Потому что [[Get]] может быть заменён Proxy, изменяя поведение доступа  
> c) Потому что prototype chain отсутствует в runtime  
> d) Потому что свойства всегда динамические по стандарту  
>> [!success]- Answer  
>> b) Потому что [[Get]] может быть заменён Proxy, изменяя поведение доступа  

> [!question] Какой случай лучше всего иллюстрирует различие между ℝ и 𝔽 в спецификации ECMAScript?  
> a) BigInt операции всегда работают в ℝ  
> b) Математическая функция определяется над ℝ, но реализуется через IEEE 754 числа 𝔽  
> c) ℝ используется только для строк  
> d) 𝔽 применяется только в JSON  
>> [!success]- Answer  
>> b) Математическая функция определяется над ℝ, но реализуется через IEEE 754 числа 𝔽  

> [!question] В модели памяти ECMAScript, почему SharedArrayBuffer требует использования Atomics для корректной синхронизации?  
> a) Потому что без Atomics запрещены все операции записи  
> b) Потому что SharedArrayBuffer автоматически делает операции атомарными  
> c) Потому что без Atomics нет гарантий порядка видимости между агентами  
> d) Потому что event loop блокируется при использовании SAB  
>> [!success]- Answer  
>> c) Потому что без Atomics нет гарантий порядка видимости между агентами  

> [!question] Что наиболее точно описывает влияние host-defined операций на детерминированность программы?  
> a) Они всегда детерминированы, если используются в браузере  
> b) Они могут иметь разное поведение в разных хост-средах при одинаковом ECMAScript коде  
> c) Они запрещены в strict mode  
> d) Они влияют только на синтаксический анализ  
>> [!success]- Answer  
>> b) Они могут иметь разное поведение в разных хост-средах при одинаковом ECMAScript коде  

> [!question] Почему exotic objects (например Array) требуют специальных internal method overrides вместо стандартного object behavior?  
> a) Потому что они не используют prototype chain  
> b) Потому что они должны поддерживать семантику индексированных свойств и length-инвариант  
> c) Потому что они всегда immutable  
> d) Потому что они не являются объектами в строгом смысле  
>> [!success]- Answer  
>> b) Потому что они должны поддерживать семантику индексированных свойств и length-инвариант  

> [!question] Какой эффект даёт Promise.try в сравнении с Promise.resolve(fn()) с точки зрения error handling?  
> a) Он запрещает синхронные ошибки  
> b) Он гарантирует захват синхронных исключений в Promise-цепочку  
> c) Он делает выполнение строго асинхронным  
> d) Он заменяет необходимость try/catch  
>> [!success]- Answer  
>> b) Он гарантирует захват синхронных исключений в Promise-цепочку  

> [!question] Почему Set в ECMAScript нельзя рассматривать как просто “Array без дубликатов”?  
> a) Потому что Set хранит элементы только по ссылке  
> b) Потому что Set имеет другую семантику операций и проверки уникальности на уровне спецификации  
> c) Потому что Set всегда отсортирован  
> d) Потому что Set не поддерживает примитивы  
>> [!success]- Answer  
>> b) Потому что Set имеет другую семантику операций и проверки уникальности на уровне спецификации  

> [!question] В чем ключевое отличие implementation-defined и implementation-approximated в контексте Math.* функций?  
> a) implementation-defined всегда детерминирован  
> b) implementation-approximated допускает численные отклонения от идеальной математической модели  
> c) implementation-defined относится только к браузеру  
> d) implementation-approximated запрещает использование IEEE 754  
>> [!success]- Answer  
>> b) implementation-approximated допускает численные отклонения от идеальной математической модели  


> [!question] В ECMAScript объект создаётся через new CF(). CF имеет собственные свойства и prototype CF.prototype. Какое утверждение наиболее точно описывает разделение ответственности между CF и CF.prototype?  
> a) CF.prototype используется только для хранения внутренних методов движка  
> b) CF содержит состояние конструктора, а CF.prototype определяет разделяемое поведение экземпляров  
> c) CF.prototype копируется в каждый экземпляр при создании  
> d) CF и CF.prototype объединяются в один объект на этапе выполнения  
>> [!success]- Answer  
>> b) CF содержит состояние конструктора, а CF.prototype определяет разделяемое поведение экземпляров  

> [!question] Почему механизм prototype chain считается memory-efficient в ECMAScript?  
> a) Потому что объекты не хранят собственные свойства  
> b) Потому что методы и общие данные размещаются в prototype и не дублируются в экземплярах  
> c) Потому что GC автоматически удаляет prototype после создания объектов  
> d) Потому что prototype хранится в регистре CPU  
>> [!success]- Answer  
>> b) Потому что методы и общие данные размещаются в prototype и не дублируются в экземплярах  

> [!question] Какой эффект имеет изменение prototype объекта CF.prototype после создания экземпляров?  
> a) Изменения не влияют на уже созданные экземпляры  
> b) Все экземпляры начинают использовать обновлённый prototype при lookup  
> c) Экземпляры пересоздаются автоматически  
> d) Prototype chain фиксируется на момент new CF()  
>> [!success]- Answer  
>> b) Все экземпляры начинают использовать обновлённый prototype при lookup  

> [!question] В чем ключевая причина существования internal methods ([[Get]], [[Set]]) вместо прямого доступа к свойствам в спецификации?  
> a) Для оптимизации синтаксиса JavaScript  
> b) Для абстракции поведения объектов и возможности переопределения через exotic objects  
> c) Для запрета динамических свойств  
> d) Для устранения prototype chain  
>> [!success]- Answer  
>> b) Для абстракции поведения объектов и возможности переопределения через exotic objects  

> [!question] Почему strict mode не является глобальной настройкой в ECMAScript?  
> a) Потому что он зависит от engine  
> b) Потому что он применяется на уровне source text unit и может различаться внутри одного runtime  
> c) Потому что он отключает prototype chain  
> d) Потому что он влияет только на синтаксис, а не на semantics  
>> [!success]- Answer  
>> b) Потому что он применяется на уровне source text unit и может различаться внутри одного runtime  

> [!question] Какой сценарий лучше всего иллюстрирует различие между mathematical ℝ и IEEE 754 𝔽 в спецификации?  
> a) Использование BigInt в арифметике  
> b) Применение Math.sin, определённой математически над ℝ, но реализуемой через 𝔽  
> c) Работа JSON.parse с числами  
> d) Использование String в арифметике  
>> [!success]- Answer  
>> b) Применение Math.sin, определённой математически над ℝ, но реализуемой через 𝔽  

> [!question] Почему exotic objects (например Array) нельзя реализовать только через обычные properties без internal method overrides?  
> a) Потому что они не поддерживают prototype chain  
> b) Потому что они требуют специальных инвариантов (например length и индексированного доступа)  
> c) Потому что они всегда frozen  
> d) Потому что они не являются объектами спецификации  
>> [!success]- Answer  
>> b) Потому что они требуют специальных инвариантов (например length и индексированного доступа)  

> [!question] Какой эффект даёт Host-defined поведение в разных JavaScript средах?  
> a) Полная унификация поведения между браузером и Node.js  
> b) Возможность различий в поведении при одинаковом ECMAScript коде  
> c) Запрет использования стандартных библиотек  
> d) Отключение асинхронности  
>> [!success]- Answer  
>> b) Возможность различий в поведении при одинаковом ECMAScript коде  

> [!question] Почему Atomics необходимы для корректной работы с SharedArrayBuffer?  
> a) Потому что они ускоряют доступ к памяти  
> b) Потому что без них нет гарантии порядка и видимости изменений между агентами  
> c) Потому что SharedArrayBuffer не поддерживает запись без Atomics  
> d) Потому что они отключают GC конкуренцию  
>> [!success]- Answer  
>> b) Потому что без них нет гарантии порядка и видимости изменений между агентами  


> [!question] В ECMAScript Proxy перехватывает операции [[Get]] и [[Set]]. Почему это делает невозможной полную статическую оптимизацию property access в JIT без деоптимизации?  
> a) Потому что Proxy отключает prototype chain  
> b) Потому что поведение [[Get]] может быть произвольно изменено handler'ом  
> c) Потому что свойства объектов всегда удаляются после первого доступа  
> d) Потому что JIT не поддерживает объекты  
>> [!success]- Answer  
>> b) Потому что поведение [[Get]] может быть произвольно изменено handler'ом  

> [!question] Какое утверждение наиболее точно описывает связь между ordinary objects и exotic objects?  
> a) Exotic objects не являются объектами ECMAScript  
> b) Ordinary objects используют стандартные internal methods, exotic — переопределяют хотя бы один  
> c) Exotic objects всегда являются built-in  
> d) Ordinary objects не поддерживают prototype chain  
>> [!success]- Answer  
>> b) Ordinary objects используют стандартные internal methods, exotic — переопределяют хотя бы один  

> [!question] Почему Set semantics нельзя выразить через Array.map/filter без потери корректности?  
> a) Потому что Array не поддерживает primitive values  
> b) Потому что Set опирается на математическую уникальность элементов, а Array — на позиционную индексацию  
> c) Потому что Set всегда отсортирован  
> d) Потому что Array не является iterable  
>> [!success]- Answer  
>> b) Потому что Set опирается на математическую уникальность элементов, а Array — на позиционную индексацию  

> [!question] В чем ключевое отличие implementation-defined и host-defined в ECMAScript архитектуре?  
> a) implementation-defined всегда одинаково везде  
> b) host-defined полностью вне контроля спецификации и определяется средой выполнения  
> c) implementation-defined относится только к числам  
> d) host-defined запрещает использование стандартной библиотеки  
>> [!success]- Answer  
>> b) host-defined полностью вне контроля спецификации и определяется средой выполнения  

> [!question] Почему Promise.try считается более семантически корректным, чем прямой вызов Promise.resolve(fn()) в случае синхронных ошибок?  
> a) Потому что он ускоряет выполнение промисов  
> b) Потому что он гарантирует захват sync throw в Promise pipeline  
> c) Потому что он делает fn асинхронной  
> d) Потому что он отключает event loop  
>> [!success]- Answer  
>> b) Потому что он гарантирует захват sync throw в Promise pipeline  

> [!question] Какой сценарий лучше всего демонстрирует необходимость разделения ℝ и 𝔽 в спецификации ECMAScript?  
> a) Работа с BigInt  
> b) Формальное описание математических функций независимо от IEEE 754 реализации  
> c) JSON сериализация чисел  
> d) Преобразование string → number  
>> [!success]- Answer  
>> b) Формальное описание математических функций независимо от IEEE 754 реализации  

> [!question] Почему Array является exotic object в ECMAScript?  
> a) Потому что он не поддерживает iteration  
> b) Потому что он имеет переопределённое поведение для индексированных свойств и length-инварианта  
> c) Потому что он immutable  
> d) Потому что он не использует prototype chain  
>> [!success]- Answer  
>> b) Потому что он имеет переопределённое поведение для индексированных свойств и length-инварианта  

> [!question] Какое последствие имеет наличие host-defined операций для детерминизма ECMAScript программ?  
> a) Полная детерминированность поведения между средами  
> b) Возможны различия поведения при одинаковом коде в разных хостах  
> c) Запрет использования Promise  
> d) Отключение GC  
>> [!success]- Answer  
>> b) Возможны различия поведения при одинаковом коде в разных хостах  

> [!question] Почему Atomics не превращают SharedArrayBuffer в полностью безопасную модель конкурентности?  
> a) Потому что Atomics запрещены в strict mode  
> b) Потому что они не обеспечивают глобальную последовательную консистентность памяти  
> c) Потому что они отключают parallel execution  
> d) Потому что они работают только в браузере  
>> [!success]- Answer  
>> b) Потому что они не обеспечивают глобальную последовательную консистентность памяти  


> [!question] Почему JIT-движки вынуждены учитывать возможность изменения prototype chain при оптимизации доступа к свойствам объекта?  
> a) Потому что prototype chain может быть удалён во время выполнения  
> b) Потому что изменения в prototype влияют на результат [[Get]] lookup в рантайме  
> c) Потому что объекты могут перестать быть объектами  
> d) Потому что JavaScript не имеет фиксированных типов  
>> [!success]- Answer  
>> b) Потому что изменения в prototype влияют на результат [[Get]] lookup в рантайме  

> [!question] Какое утверждение наиболее точно описывает связь between ordinary objects and internal methods abstraction?  
> a) Internal methods существуют только в документации и не влияют на runtime  
> b) Они задают формальную модель поведения объектов, независимую от реализации движка  
> c) Они запрещают использование Proxy  
> d) Они заменяют prototype chain  
>> [!success]- Answer  
>> b) Они задают формальную модель поведения объектов, независимую от реализации движка  

> [!question] Почему Set в ECMAScript не может гарантировать идентичное поведение через Array.reduce даже при ручной дедупликации?  
> a) Потому что Set хранит элементы только по ссылке  
> b) Потому что спецификация Set определяет уникальность на уровне SameValueZero, а Array — нет  
> c) Потому что Array не поддерживает итерацию  
> d) Потому что Set всегда отсортирован  
>> [!success]- Answer  
>> b) Потому что спецификация Set определяет уникальность на уровне SameValueZero, а Array — нет  

> [!question] В чем ключевая причина разделения execution model ECMAScript на agent-based систему?  
> a) Для упрощения синтаксиса async/await  
> b) Для поддержки параллелизма и изоляции памяти между execution contexts  
> c) Для отключения event loop  
> d) Для ускорения garbage collection  
>> [!success]- Answer  
>> b) Для поддержки параллелизма и изоляции памяти между execution contexts  

> [!question] Почему implementation-approximated операции (например Math.sin) не могут считаться pure deterministic functions в строгом математическом смысле?  
> a) Потому что они используют BigInt  
> b) Потому что допускают различия реализации при одинаковых входных данных  
> c) Потому что они зависят от prototype chain  
> d) Потому что они изменяют global state  
>> [!success]- Answer  
>> b) Потому что допускают различия реализации при одинаковых входных данных  

> [!question] Какое последствие имеет возможность Proxy перехватывать [[Get]] для оптимизаций inline caching?  
> a) Inline cache всегда становится invalid  
> b) JIT должен использовать guard checks или deopt при возможном Proxy presence  
> c) Proxy запрещает использование property access  
> d) Prototype chain перестаёт существовать  
>> [!success]- Answer  
>> b) JIT должен использовать guard checks или deopt при возможном Proxy presence  

> [!question] Почему Array exotic behavior нельзя выразить через обычный Object без изменения спецификации?  
> a) Потому что Array не является объектом  
> b) Потому что он требует специальных правил для length и индексированных свойств  
> c) Потому что Array не имеет prototype  
> d) Потому что Array всегда immutable  
>> [!success]- Answer  
>> b) Потому что он требует специальных правил для length и индексированных свойств  

> [!question] Какой эффект имеет host-defined IO на воспроизводимость ECMAScript программ?  
> a) Полная воспроизводимость между средами  
> b) Возможность различий результата при одинаковом коде  
> c) Запрет использования console  
> d) Отключение Promise microtasks  
>> [!success]- Answer  
>> b) Возможность различий результата при одинаковом коде  

> [!question] Почему Atomics необходимы, но недостаточны для полноценной concurrency safety?  
> a) Потому что они заменяют event loop  
> b) Потому что они не гарантируют глобальный memory ordering между всеми операциями  
> c) Потому что они работают только с Number  
> d) Потому что они отключают shared memory  
>> [!success]- Answer  
>> b) Потому что они не гарантируют глобальный memory ordering между всеми операциями  

> [!question] В JIT-компиляторе ECMAScript доступ к свойству obj.x был ранее стабильно inline-оптимизирован через hidden class (shape). Что произойдёт при появлении Proxy между объектом и доступом к этому свойству?  
> a) Inline cache продолжит работать без изменений, так как Proxy не влияет на shape  
> b) JIT обязан добавить guard на наличие Proxy и потенциально деоптимизировать access  
> c) Hidden class становится нестабильным и удаляется из объекта  
> d) Prototype chain полностью отключается для этого объекта  
>> [!success]- Answer  
>> b) JIT обязан добавить guard на наличие Proxy и потенциально деоптимизировать access  

> [!question] Почему в ECMAScript модель internal methods нельзя напрямую маппить на методы JavaScript-объектов в runtime?  
> a) Потому что internal methods существуют только в TypeScript  
> b) Потому что они описывают абстрактную семантику, независимую от observable JS-level API  
> c) Потому что они запрещают prototype inheritance  
> d) Потому что они выполняются только на этапе парсинга  
>> [!success]- Answer  
>> b) Потому что они описывают абстрактную семантику, независимую от observable JS-level API  

> [!question] Какой эффект имеет возможность изменения prototype chain в runtime на speculative optimization?  
> a) Ускоряет JIT за счёт фиксированного lookup  
> b) Требует invalidation inline caches и возможного deopt при prototype mutation  
> c) Делает объекты immutable  
> d) Отключает garbage collector  
>> [!success]- Answer  
>> b) Требует invalidation inline caches и возможного deopt при prototype mutation  

> [!question] Почему Set/Map операции в ECMAScript не могут быть полностью свёрнуты в Array-based оптимизации без нарушения спецификации?  
> a) Потому что Set не является iterable  
> b) Потому что они используют SameValueZero semantics и не индексную модель хранения  
> c) Потому что Array не поддерживает heap allocation  
> d) Потому что Map всегда упорядочен по insertion sort  
>> [!success]- Answer  
>> b) Потому что они используют SameValueZero semantics и не индексную модель хранения  

> [!question] В agent-based execution model ECMAScript, почему SharedArrayBuffer не даёт deterministic ordering даже при использовании Atomics?  
> a) Потому что Atomics отключаются в strict mode  
> b) Потому что порядок между агентами не глобально сериализуется спецификацией  
> c) Потому что event loop всегда переупорядочивает операции  
> d) Потому что memory всегда копируется при доступе  
>> [!success]- Answer  
>> b) Потому что порядок между агентами не глобально сериализуется спецификацией  

> [!question] Почему Math.* функции не могут быть формально гарантированно reproducible между различными ECMAScript engines?  
> a) Потому что они используют BigInt  
> b) Потому что спецификация допускает implementation-approximated поведение  
> c) Потому что они зависят от prototype chain  
> d) Потому что они являются host-defined  
>> [!success]- Answer  
>> b) Потому что спецификация допускает implementation-approximated поведение  

> [!question] Какой JIT-инвариант ломается при динамическом изменении shape объекта через добавление новых свойств?  
> a) Garbage collection consistency  
> b) Hidden class stability assumption for inline caching  
> c) Event loop ordering  
> d) Promise microtask queue ordering  
>> [!success]- Answer  
>> b) Hidden class stability assumption for inline caching  

> [!question] Почему Array exotic semantics (особенно length property) критичны для correctness JIT оптимизаций?  
> a) Потому что length всегда readonly  
> b) Потому что length может быть side-effectful и влияет на indexed property semantics  
> c) Потому что Array не имеет prototype  
> d) Потому что Array запрещает numeric keys  
>> [!success]- Answer  
>> b) Потому что length может быть side-effectful и влияет на indexed property semantics  

> [!question] Какой фундаментальный риск создаёт host-defined IO для formal verification ECMAScript программ?  
> a) Он делает код синтаксически некорректным  
> b) Он вводит недетерминированное поведение вне спецификации языка  
> c) Он отключает type system  
> d) Он ломает prototype chain invariants  
>> [!success]- Answer  
>> b) Он вводит недетерминированное поведение вне спецификации языка  

> [!question] Почему Atomics operations не эквивалентны sequential consistency memory model?  
> a) Потому что они работают только в Node.js  
> b) Потому что они гарантируют атомарность операций, но не глобальный total order всех memory operations  
> c) Потому что они отключают concurrency  
> d) Потому что они работают только с float16  
>> [!success]- Answer  
>> b) Потому что они гарантируют атомарность операций, но не глобальный total order всех memory operations  



