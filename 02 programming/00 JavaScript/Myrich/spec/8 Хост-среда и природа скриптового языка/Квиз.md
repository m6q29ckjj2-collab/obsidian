#flashquiz/proga/js/spec/8 

> [!question] При выполнении кода внутри блока `{}` разработчик ожидает, что будет создан новый независимый lexical scope с отдельным Environment Record. Однако в реальности движок оптимизирует это поведение. Какое утверждение наиболее точно описывает компромисс между спецификацией и реализацией?  
> a) Движок всегда полностью игнорирует блоковые Environment Records и использует только глобальный  
> b) Движок сохраняет семантику изоляции, но может переиспользовать одну структуру хранения с индексным разделением  
> c) Каждый блок всегда создаёт отдельный физический объект в памяти без исключений  
> d) Block scope существует только для let, а var полностью отключает Environment Records
> 
> > > [!success]- Answer  
> > > b) Движок сохраняет семантику изоляции, но может переиспользовать одну структуру хранения с индексным разделением  
> > > Explanation:  
> > > Правильный ответ отражает идею оптимизаций (как в V8), где семантика блоков сохраняется, но физическая реализация может быть упрощена.  
> > > a неверен: блоки не игнорируются, иначе ломается область видимости.  
> > > c неверен: спецификация не требует отдельного объекта на каждый блок.  
> > > d неверен: var тоже участвует в Environment Records, просто с function/global scope.

> [!question] При поиске идентификатора в цепочке окружений (outer environment link), что является наиболее точным описанием механизма резолвинга?  
> a) Итеративный поиск по таблице символов компилятора до конца файла  
> b) Рекурсивное движение по ссылкам внешних окружений до null  
> c) Прямое обращение к глобальному объекту без промежуточных проверок  
> d) Поиск выполняется только в текущем Execution Context без выхода наружу
> 
> > > [!success]- Answer  
> > > b) Рекурсивное движение по ссылкам внешних окружений до null  
> > > Explanation:  
> > > Это соответствует цепочке Environment Records с outer link.  
> > > a неверен: нет «таблицы символов компилятора» в runtime модели.  
> > > c неверен: глобальный объект используется только как крайняя точка.  
> > > d неверен: поиск явно выходит за пределы текущего окружения.

> [!question] Почему Execution Context Stack (ECS) в спецификационной модели нельзя полностью свести к классическому Call Stack из архитектуры x86?  
> a) Потому что ECS хранит только адреса возврата функций  
> b) Потому что ECS может изменять порядок контекстов и хранит состояния выполнения  
> c) Потому что ECS существует только в браузере и отсутствует в Node.js  
> d) Потому что ECS не связан с функциями вообще
> 
> > > [!success]- Answer  
> > > b) Потому что ECS может изменять порядок контекстов и хранит состояния выполнения  
> > > Explanation:  
> > > ECS оперирует полноценными состояниями Execution Context, а не просто адресами.  
> > > a неверен: это характеристика x86 stack, а не ECS.  
> > > c неверен: модель ECMAScript не зависит от конкретного хоста.  
> > > d неверен: функции — основной источник functional execution contexts.

> [!question] Что наиболее точно объясняет поведение `await` внутри async function с точки зрения Execution Context?  
> a) Контекст уничтожается и создаётся заново при каждом await  
> b) Контекст сериализуется в глобальный объект  
> c) Контекст сохраняется (suspend) и позже возобновляется (resume) с тем же состоянием  
> d) Await блокирует весь Agent до завершения Promise
> 
> > > [!success]- Answer  
> > > c) Контекст сохраняется (suspend) и позже возобновляется (resume) с тем же состоянием  
> > > Explanation:  
> > > Async/await работает через приостановку Execution Context.  
> > > a неверен: контекст не пересоздаётся.  
> > > b неверен: нет сериализации в глобальный объект.  
> > > d неверен: блокировки агента не происходит.

> [!question] Какое утверждение наиболее корректно отражает роль Global Object в современной модели ECMAScript?  
> a) Это единственный обязательный глобальный singleton во всех средах  
> b) Это объект, который может быть создан несколькими экземплярами в разных Realms  
> c) Это исключительно часть языка ECMAScript без участия хоста  
> d) Это статическая структура, жестко зафиксированная в спецификации без вариаций
> 
> > > [!success]- Answer  
> > > b) Это объект, который может быть создан несколькими экземплярами в разных Realms  
> > > Explanation:  
> > > Global Object зависит от Realm и может различаться между контекстами.  
> > > a неверен: не обязателен singleton.  
> > > c неверен: хост определяет его реализацию.  
> > > d неверен: структура вариативна.

> [!question] Почему утверждение "Event Loop — часть ECMAScript" является некорректным с точки зрения архитектуры исполнения?  
> a) Потому что Event Loop реализован исключительно в WebAssembly  
> b) Потому что ECMAScript определяет только Jobs, а цикл задаётся хостом  
> c) Потому что Event Loop устарел и удалён из спецификации  
> d) Потому что Event Loop используется только в многопоточных системах
> 
> > > [!success]- Answer  
> > > b) Потому что ECMAScript определяет только Jobs, а цикл задаётся хостом  
> > > Explanation:  
> > > Event Loop относится к HTML/host environment, а не к ECMAScript.  
> > > a неверен: WebAssembly не управляет event loop.  
> > > c неверен: он не был «удалён», он вне ECMAScript.  
> > > d неверен: используется и в однопоточном JS.

> [!question] Что наиболее точно описывает Job Queue в ECMAScript?  
> a) Это единая очередь всех задач включая I/O и таймеры  
> b) Это набор очередей с различными правилами порядка выполнения, включая PromiseJobs  
> c) Это очередь, управляемая исключительно Event Loop браузера  
> d) Это синхронный стек вызовов функций
> 
> > > [!success]- Answer  
> > > b) Это набор очередей с различными правилами порядка выполнения, включая PromiseJobs  
> > > Explanation:  
> > > Job Queue включает разные типы jobs, включая microtasks (PromiseJobs).  
> > > a неверен: I/O и timers — host-level queues.  
> > > c неверен: не только браузер и не только event loop.  
> > > d неверен: это не стек вызовов.

> [!question] В каком случае Agent Cluster и SharedArrayBuffer становятся критически важны?  
> a) При синхронном выполнении однопоточного кода  
> b) При работе с разделяемой памятью между агентами и предотвращении race conditions  
> c) При создании глобального объекта  
> d) При оптимизации парсинга JavaScript кода
> 
> > > [!success]- Answer  
> > > b) При работе с разделяемой памятью между агентами и предотвращении race conditions  
> > > Explanation:  
> > > Agent Cluster + SharedArrayBuffer + Atomics решают concurrency.  
> > > a неверен: не требуется.  
> > > c неверен: не связано.  
> > > d неверен: парсинг не зависит от этого механизма.

> [!question] Какое утверждение наиболее точно отражает различие между Realm и Execution Context?  
> a) Realm — это стек вызовов функций, Execution Context — глобальная среда  
> b) Realm задаёт изоляцию окружений и встроенных объектов, Execution Context — текущее состояние выполнения  
> c) Realm и Execution Context — полностью взаимозаменяемые термины  
> d) Realm существует только в браузере, Execution Context — только в Node.js
> 
> > > [!success]- Answer  
> > > b) Realm задаёт изоляцию окружений и встроенных объектов, Execution Context — текущее состояние выполнения  
> > > Explanation:  
> > > Realm — окружение, Execution Context — runtime состояние.  
> > > a неверен: перепутаны уровни модели.  
> > > c неверен: это разные сущности.  
> > > d неверен: оба существуют в разных хостах.

> [!question] Почему утверждение "Call Stack в DevTools полностью отражает реальную Execution Context Stack" является некорректным?  
> a) Потому что DevTools отображает только синтаксис кода  
> b) Потому что DevTools показывает упрощённую модель вызовов, скрывая асинхронные переключения контекстов  
> c) Потому что ECS не существует в реальности  
> d) Потому что Call Stack и ECS идентичны по спецификации
> 
> > > [!success]- Answer  
> > > b) Потому что DevTools показывает упрощённую модель вызовов, скрывая асинхронные переключения контекстов  
> > > Explanation:  
> > > DevTools визуализирует упрощённый call stack.  
> > > a неверен: это не просто синтаксис.  
> > > c неверен: ECS существует как модель.  
> > > d неверен: они не идентичны.

> [!question] Что наиболее точно описывает поведение TimeOutJobs в контексте спецификации?  
> a) Это очередь исключительно для setTimeout и setInterval  
> b) Это механизм синхронизации в многопоточности через Atomics, не связанный напрямую с setTimeout  
> c) Это часть Promise microtask queue  
> d) Это внутренняя оптимизация браузера для рендера
> 
> > > [!success]- Answer  
> > > b) Это механизм синхронизации в многопоточности через Atomics, не связанный напрямую с setTimeout  
> > > Explanation:  
> > > TimeOutJobs связаны с Atomics.wait и concurrency.  
> > > a неверен: это распространённый миф.  
> > > c неверен: это не microtasks.  
> > > d неверен: не про рендер.

> [!question] Какой эффект имеет «outer link» в цепочке Environment Records при лексическом поиске?  
> a) Останавливает поиск на уровне текущего блока  
> b) Создаёт копию переменных родительского окружения  
> c) Позволяет последовательно подниматься по цепочке окружений  
> d) Заменяет глобальный объект при каждом вызове функции
> 
> > > [!success]- Answer  
> > > c) Позволяет последовательно подниматься по цепочке окружений  
> > > Explanation:  
> > > Outer link — механизм lexical scope chain.  
> > > a неверен: наоборот, поиск продолжается.  
> > > b неверен: копирование не происходит.  
> > > d неверен: глобальный объект не заменяется.

> [!question] Какое утверждение лучше всего объясняет идею "Agent canBlock"?  
> a) Агент всегда блокирует все другие агенты  
> b) Агент может останавливать выполнение других потоков в определённых условиях  
> c) Агент управляет только DOM рендерингом  
> d) Агент существует только в однопоточном режиме
> 
> > > [!success]- Answer  
> > > b) Агент может останавливать выполнение других потоков в определённых условиях  
> > > Explanation:  
> > > canBlock — характеристика возможностей агента в concurrency модели.  
> > > a неверен: не всегда блокирует.  
> > > c неверен: не связан только с DOM.  
> > > d неверен: агенты применяются в многопоточности.

> [!question] Почему утверждение "var игнорирует Environment Records" является неверным?  
> a) Потому что var не существует в ECMAScript  
> b) Потому что var обрабатывается через function/global scope внутри Environment Records  
> c) Потому что var создаёт отдельный Realm  
> d) Потому что var работает только в strict mode
> 
> > > [!success]- Answer  
> > > b) Потому что var обрабатывается через function/global scope внутри Environment Records  
> > > Explanation:  
> > > var интегрирован в Environment Records, просто с другим scope rules.  
> > > a неверен: var существует.  
> > > c неверен: Realm не создаётся.  
> > > d неверен: strict mode влияет, но не определяет существование.

> [!question] В чём ключевая архитектурная разница между "PromiseJobs" и "GenericJobs"?  
> a) PromiseJobs выполняются только синхронно  
> b) PromiseJobs имеют строгий порядок и приоритет microtask-like очереди  
> c) GenericJobs всегда выполняются раньше PromiseJobs  
> d) PromiseJobs относятся к хосту, а GenericJobs к CPU
> 
> > > [!success]- Answer  
> > > b) PromiseJobs имеют строгий порядок и приоритет microtask-like очереди  
> > > Explanation:  
> > > PromiseJobs — упорядоченная очередь microtasks.  
> > > a неверен: они асинхронны.  
> > > c неверен: наоборот приоритет у PromiseJobs выше.  
> > > d неверен: обе — часть ECMAScript модели.

> [!question] Какое утверждение наиболее точно описывает отношение между JavaScript и Host Environment?  
> a) JS полностью контролирует I/O и рендеринг  
> b) JS является изолированной системой без внешних API  
> c) JS предоставляет логику, а хост реализует API и выполнение системных операций  
> d) JS и хост — это один и тот же уровень спецификации
> 
> > > [!success]- Answer  
> > > c) JS предоставляет логику, а хост реализует API и выполнение системных операций  
> > > Explanation:  
> > > JS (ECMAScript) не включает I/O, он зависит от хоста.  
> > > a неверен: это делает хост.  
> > > b неверен: JS всегда встраиваемый.  
> > > d неверен: уровни разделены.

> [!question] Почему утверждение "global code — это просто последовательность инструкций верхнего уровня без собственного Execution Context" является некорректным с точки зрения спецификации?  
> a) Потому что global code выполняется внутри неявного Execution Context, аналогичного функции main  
> b) Потому что global code выполняется только внутри Event Loop  
> c) Потому что global code не может содержать переменные  
> d) Потому что global code компилируется отдельно от остальных модулей
> 
> > > [!success]- Answer  
> > > a) Потому что global code выполняется внутри неявного Execution Context, аналогичного функции main  
> > > Explanation:  
> > > Global code запускается в глобальном Execution Context, который по сути является неявной точкой входа выполнения.  
> > > b неверен: Event Loop не определяет первичный запуск кода.  
> > > c неверен: global scope содержит переменные.  
> > > d неверен: нет отдельной «компиляции глобального кода».

> [!question] Какое утверждение наиболее точно описывает поведение вложенных Execution Context при рекурсивных вызовах функций?  
> a) Каждый вызов перезаписывает предыдущий Execution Context в одном и том же слоте памяти  
> b) Каждый вызов создаёт новый Execution Context и помещает его в стек ECS  
> c) Рекурсия не создаёт новых Execution Context, а переиспользует текущий  
> d) Execution Context создаётся только один раз на функцию, независимо от числа вызовов
> 
> > > [!success]- Answer  
> > > b) Каждый вызов создаёт новый Execution Context и помещает его в стек ECS  
> > > Explanation:  
> > > Рекурсивные вызовы создают отдельные контексты, сохраняемые в ECS.  
> > > a неверен: это ломало бы рекурсию.  
> > > c неверен: переиспользование невозможно без потери состояния.  
> > > d неверен: Execution Context создаётся на каждый вызов.

> [!question] Какой механизм наиболее точно объясняет, почему переменные `let/const` недоступны до инициализации (temporal dead zone)?  
> a) Они не существуют в Environment Record до выполнения строки объявления  
> b) Они существуют, но находятся в состоянии неинициализированного binding в Environment Record  
> c) Они хранятся в глобальном объекте, но скрыты от доступа  
> d) Они создаются только после завершения всего Execution Context
> 
> > > [!success]- Answer  
> > > b) Они существуют, но находятся в состоянии неинициализированного binding в Environment Record  
> > > Explanation:  
> > > TDZ — это не отсутствие переменной, а наличие uninitialized binding.  
> > > a неверен: binding уже существует.  
> > > c неверен: let/const не в global object.  
> > > d неверен: они создаются при создании контекста, а не после его завершения.

> [!question] Почему утверждение "Execution Context можно приостановить и возобновить в любой точке без ограничений" является неточным?  
> a) Потому что только async/await и generators поддерживают suspend/resume с сохранением состояния  
> b) Потому что Execution Context нельзя приостанавливать вообще  
> c) Потому что приостановка возможна только в глобальном контексте  
> d) Потому что браузер запрещает любые формы паузы исполнения
> 
> > > [!success]- Answer  
> > > a) Потому что только async/await и generators поддерживают suspend/resume с сохранением состояния  
> > > Explanation:  
> > > Suspend/resume — ограниченный механизм, реализованный через async/generator semantics.  
> > > b неверен: приостановка существует.  
> > > c неверен: работает внутри функций.  
> > > d неверен: это не ограничение браузера как такового.

> [!question] Что наиболее точно описывает разницу между Agent и Execution Context?  
> a) Agent — это состояние выполнения кода, Execution Context — поток ОС  
> b) Agent — это исполняющая сущность (worker), Execution Context — состояние конкретного выполнения  
> c) Agent и Execution Context — полностью идентичные понятия  
> d) Agent существует только в браузере, Execution Context только в Node.js
> 
> > > [!success]- Answer  
> > > b) Agent — это исполняющая сущность (worker), Execution Context — состояние конкретного выполнения  
> > > Explanation:  
> > > Agent — выполняющий субъект, Execution Context — его внутреннее состояние.  
> > > a неверен: перепутаны уровни абстракции.  
> > > c неверен: это разные уровни модели.  
> > > d неверен: оба существуют в разных средах.

> [!question] Почему утверждение "Realm создаётся для каждой функции при вызове" является ошибочным?  
> a) Потому что Realm создаётся один раз на глобальную среду выполнения  
> b) Потому что Realm создаётся на каждый Execution Context  
> c) Потому что Realm не связан с функциями, а задаёт изоляцию глобальной среды и встроенных объектов  
> d) Потому что Realm создаётся только при использовании eval
> 
> > > [!success]- Answer  
> > > c) Потому что Realm не связан с функциями, а задаёт изоляцию глобальной среды и встроенных объектов  
> > > Explanation:  
> > > Realm — это изолированная среда (intrinsics + global object), не привязанная к вызовам функций.  
> > > a неверен: может быть несколько realms.  
> > > b неверен: Realm ≠ Execution Context.  
> > > d неверен: eval не создаёт realm.

> [!question] Какое утверждение наиболее корректно описывает влияние outer lexical environment chain на производительность поиска переменных?  
> a) Поиск всегда O(1), так как используется хеш-таблица глобального уровня  
> b) Поиск может быть O(n) по глубине цепочки окружений  
> c) Поиск выполняется только во время компиляции, не в runtime  
> d) Поиск всегда происходит через глобальный объект
> 
> > > [!success]- Answer  
> > > b) Поиск может быть O(n) по глубине цепочки окружений  
> > > Explanation:  
> > > Lexical lookup может проходить по цепочке outer environments.  
> > > a неверен: нет глобального O(1) для всех переменных.  
> > > c неверен: это runtime механизм.  
> > > d неверен: глобальный объект — лишь крайняя точка.

> [!question] Что наиболее точно описывает роль Host Hooks (например, setTimeout, fetch) в модели исполнения JavaScript?  
> a) Это встроенные ECMAScript функции стандарта языка  
> b) Это функции, реализованные хостом, которые JS вызывает через внешние интерфейсы  
> c) Это часть Execution Context Stack  
> d) Это синтаксические конструкции языка
> 
> > > [!success]- Answer  
> > > b) Это функции, реализованные хостом, которые JS вызывает через внешние интерфейсы  
> > > Explanation:  
> > > Host APIs предоставляются средой (browser/Node).  
> > > a неверен: они не часть ECMAScript.  
> > > c неверен: не часть ECS.  
> > > d неверен: это не синтаксис языка.

> [!question] Почему модель "JavaScript как строго однопоточный язык" является упрощением?  
> a) Потому что JS использует GPU для параллельных вычислений  
> b) Потому что существуют Agent Clusters и SharedArrayBuffer для параллелизма  
> c) Потому что Event Loop выполняет код параллельно  
> d) Потому что функции выполняются одновременно в одном Execution Context
> 
> > > [!success]- Answer  
> > > b) Потому что существуют Agent Clusters и SharedArrayBuffer для параллелизма  
> > > Explanation:  
> > > JS модель допускает многопоточность через agents.  
> > > a неверен: GPU не является базовой моделью JS.  
> > > c неверен: event loop не даёт параллелизм.  
> > > d неверен: Execution Context не параллелится.

> [!question] Что наиболее точно описывает разницу между microtask queue (PromiseJobs) и macrotask-like host queues?  
> a) Microtasks всегда выполняются после полного завершения всех macrotasks  
> b) Microtasks имеют приоритет и выполняются перед возвратом управления хосту  
> c) Microtasks выполняются только в Node.js  
> d) Microtasks выполняются синхронно внутри функции Promise constructor
> 
> > > [!success]- Answer  
> > > b) Microtasks имеют приоритет и выполняются перед возвратом управления хосту  
> > > Explanation:  
> > > PromiseJobs drain происходит перед следующей host task.  
> > > a неверен: порядок обратный.  
> > > c неверен: есть и в браузере.  
> > > d неверен: Promise executor синхронен, но microtasks нет.

> [!question] Почему утверждение "Execution Context Stack можно произвольно переупорядочивать движком" является частично верным, но вводящим в заблуждение?  
> a) Потому что движок действительно может полностью менять порядок выполнения функций  
> b) Потому что переупорядочивание происходит только на уровне асинхронных задач, а не синхронного ECS  
> c) Потому что ECS — это физический стек CPU  
> d) Потому что порядок выполнения всегда фиксирован спецификацией без исключений
> 
> > > [!success]- Answer  
> > > b) Потому что переупорядочивание происходит только на уровне асинхронных задач, а не синхронного ECS  
> > > Explanation:  
> > > Асинхронность влияет на задачи, но не ломает синхронный стек вызовов.  
> > > a неверен: порядок функций не произвольно меняется.  
> > > c неверен: ECS не CPU stack.  
> > > d неверен: асинхронность добавляет вариативность.

> [!question] Какое утверждение наиболее точно описывает смысл "suspend/resume" в Execution Context?  
> a) Полная сериализация состояния в JSON  
> b) Сохранение и восстановление состояния выполнения без потери локального контекста  
> c) Уничтожение контекста и создание нового при возобновлении  
> d) Перемещение выполнения в глобальный scope
> 
> > > [!success]- Answer  
> > > b) Сохранение и восстановление состояния выполнения без потери локального контекста  
> > > Explanation:  
> > > Suspend/resume сохраняет execution state (closures, locals).  
> > > a неверен: сериализация не используется.  
> > > c неверен: контекст не уничтожается.  
> > > d неверен: нет перемещения в global scope.

> [!question] Какое утверждение наиболее точно объясняет, почему Global Object нельзя считать единой универсальной точкой доступа ко всем данным программы?  
> a) Потому что он существует только в strict mode  
> b) Потому что каждый Realm может иметь собственный Global Object  
> c) Потому что Global Object всегда пустой  
> d) Потому что Global Object доступен только через fetch API
> 
> > > [!success]- Answer  
> > > b) Потому что каждый Realm может иметь собственный Global Object  
> > > Explanation:  
> > > Realms изолируют глобальные объекты.  
> > > a неверен: strict mode не ограничивает существование.  
> > > c неверен: он содержит встроенные объекты.  
> > > d неверен: доступ не через fetch.

> [!question] Почему утверждение "Execution Context = function invocation" является упрощением, а не точным описанием?  
> a) Потому что Execution Context создаётся только для async функций  
> b) Потому что Execution Context включает не только функции, но и global, module, eval contexts  
> c) Потому что Execution Context существует только в браузере  
> d) Потому что invocation не влияет на Execution Context
> 
> > > [!success]- Answer  
> > > b) Потому что Execution Context включает не только функции, но и global, module, eval contexts  
> > > Explanation:  
> > > Execution Context имеет несколько типов.  
> > > a неверен: не только async.  
> > > c неверен: модель универсальна.  
> > > d неверен: invocation напрямую создаёт context.

> [!question] Что наиболее точно описывает роль outer lexical environment link в замыканиях (closures)?  
> a) Он копирует значения переменных в функцию при создании  
> b) Он сохраняет ссылку на внешнее окружение, позволяя доступ к переменным после выхода из контекста  
> c) Он удаляет внешние переменные после возврата функции  
> d) Он превращает все переменные в глобальные
> 
> > > [!success]- Answer  
> > > b) Он сохраняет ссылку на внешнее окружение, позволяя доступ к переменным после выхода из контекста  
> > > Explanation:  
> > > Closures работают через сохранение lexical environment reference.  
> > > a неверен: нет копирования.  
> > > c неверен: удаление не происходит.  
> > > d неверен: глобализация не происходит.

> [!question] Какое утверждение наиболее точно описывает взаимодействие Host Environment и Job Queue?  
> a) Host напрямую управляет всеми microtasks  
> b) Host инициирует выполнение задач, но JS engine управляет внутренними Job queues  
> c) Job Queue существует только в браузере  
> d) Job Queue полностью заменяет Event Loop
> 
> > > [!success]- Answer  
> > > b) Host инициирует выполнение задач, но JS engine управляет внутренними Job queues  
> > > Explanation:  
> > > Хост триггерит выполнение, движок управляет jobs.  
> > > a неверен: управление распределено.  
> > > c неверен: модель универсальна.  
> > > d неверен: job queue и event loop разные уровни.

> [!question] Почему утверждение "Agent Cluster гарантирует отсутствие race conditions" является некорректным?  
> a) Потому что race conditions невозможны в JS  
> b) Потому что shared memory требует явной синхронизации через Atomics  
> c) Потому что Agent Cluster отключает многопоточность  
> d) Потому что race conditions решаются только garbage collector
> 
> > > [!success]- Answer  
> > > b) Потому что shared memory требует явной синхронизации через Atomics  
> > > Explanation:  
> > > SharedArrayBuffer не предотвращает race conditions сам по себе.  
> > > a неверен: возможны.  
> > > c неверен: многопоточность остаётся.  
> > > d неверен: GC не решает синхронизацию.

---

> [!question] Почему утверждение "Promise всегда создаёт асинхронное выполнение executor-функции" является некорректным с точки зрения ECMAScript?  
> a) Потому что executor Promise выполняется синхронно во время создания Promise  
> b) Потому что executor выполняется только в microtask queue  
> c) Потому что Promise executor выполняется в отдельном Agent  
> d) Потому что Promise executor откладывается до следующего Event Loop tick
> 
> > > [!success]- Answer  
> > > a) Потому что executor Promise выполняется синхронно во время создания Promise  
> > > Explanation:  
> > > Executor вызывается сразу при создании Promise, до постановки jobs.  
> > > b неверен: microtasks касаются then/catch, не executor.  
> > > c неверен: новый Agent не создаётся.  
> > > d неверен: нет привязки к macrotask tick.

> [!question] Какой механизм объясняет, почему `.then()` callback выполняется позже синхронного кода, даже если Promise уже resolved?  
> a) Promise хранит callback внутри Execution Context Stack  
> b) then-handlers ставятся в PromiseJobs queue (microtask queue)  
> c) then вызывается напрямую внутри resolver функции  
> d) Promise блокирует Event Loop до завершения цепочки
> 
> > > [!success]- Answer  
> > > b) then-handlers ставятся в PromiseJobs queue (microtask queue)  
> > > Explanation:  
> > > then всегда планируется как job.  
> > > a неверен: ECS не хранит callbacks.  
> > > c неверен: нет синхронного вызова then.  
> > > d неверен: Promise не блокирует loop.

> [!question] Почему цепочка `.then().then()` не является просто последовательным вызовом функций?  
> a) Потому что каждый then создаёт новый Realm  
> b) Потому что каждый then создаёт новый Promise и планирует job, а не вызывает функцию напрямую  
> c) Потому что then выполняется синхронно внутри одного Execution Context  
> d) Потому что then преобразуется в callback hell на уровне компиляции
> 
> > > [!success]- Answer  
> > > b) Потому что каждый then создаёт новый Promise и планирует job, а не вызывает функцию напрямую  
> > > Explanation:  
> > > chaining = job scheduling, not direct calls.  
> > > a неверен: Realm не создаётся.  
> > > c неверен: then асинхронен.  
> > > d неверен: нет компиляционной трансформации.

> [!question] Что наиболее точно описывает microtask checkpoint?  
> a) Это момент, когда GC очищает память после каждой функции  
> b) Это точка, где движок полностью опустошает microtask queue перед возвратом в host  
> c) Это часть Event Loop, выполняемая только при таймерах  
> d) Это синхронный участок кода между двумя function calls
> 
> > > [!success]- Answer  
> > > b) Это точка, где движок полностью опустошает microtask queue перед возвратом в host  
> > > Explanation:  
> > > microtask checkpoint — drain очереди jobs.  
> > > a неверен: GC не связан.  
> > > c неверен: не только timers.  
> > > d неверен: это не синхронный участок.

> [!question] Почему утверждение "module execution начинается с выполнения top-level кода без промежуточных стадий" является упрощением?  
> a) Потому что ES modules проходят стадии linking → instantiation → evaluation  
> b) Потому что modules всегда выполняются в global scope без контекста  
> c) Потому что modules выполняются только после Event Loop  
> d) Потому что modules компилируются в native code до выполнения
> 
> > > [!success]- Answer  
> > > a) Потому что ES modules проходят стадии linking → instantiation → evaluation  
> > > Explanation:  
> > > Module graph имеет фазы, включая linking.  
> > > b неверен: modules имеют own context.  
> > > c неверен: не завязано на event loop напрямую.  
> > > d неверен: native compilation не обязательна.

> [!question] Какое утверждение наиболее точно описывает module instantiation?  
> a) Это выполнение всего кода модулей  
> b) Это создание binding-структур и linking импортов без выполнения кода  
> c) Это преобразование модулей в AST  
> d) Это загрузка модулей в глобальный объект
> 
> > > [!success]- Answer  
> > > b) Это создание binding-структур и linking импортов без выполнения кода  
> > > Explanation:  
> > > instantiation = wiring imports/exports.  
> > > a неверен: это evaluation.  
> > > c неверен: AST — компиляция.  
> > > d неверен: modules не грузятся в global object.

> [!question] Почему утверждение "SharedArrayBuffer автоматически синхронизирует доступ между потоками" является неверным?  
> a) Потому что он работает только в Node.js  
> b) Потому что синхронизация требует Atomics  
> c) Потому что он работает только в одном Agent  
> d) Потому что он запрещён ECMAScript
> 
> > > [!success]- Answer  
> > > b) Потому что синхронизация требует Atomics  
> > > Explanation:  
> > > SAB — shared memory, но не synchronization primitive.  
> > > a неверен: доступен в разных средах.  
> > > c неверен: он именно для multi-agent.  
> > > d неверен: он разрешён с ограничениями.

> [!question] Что наиболее точно объясняет поведение `queueMicrotask`?  
> a) Она ставит задачу в macrotask queue  
> b) Она добавляет job в microtask queue (PromiseJobs)  
> c) Она выполняется только после setTimeout  
> d) Она создаёт новый Execution Context
> 
> > > [!success]- Answer  
> > > b) Она добавляет job в microtask queue (PromiseJobs)  
> > > Explanation:  
> > > queueMicrotask = explicit microtask scheduling.  
> > > a неверен: это не macrotask.  
> > > c неверен: порядок другой.  
> > > d неверен: context не создаётся.

> [!question] Почему порядок выполнения microtasks может быть опасен для производительности?  
> a) Потому что они блокируют CPU навсегда  
> b) Потому что они выполняются до возврата в host, блокируя rendering cycle  
> c) Потому что они выполняются в отдельном thread  
> d) Потому что они уничтожают Execution Context Stack
> 
> > > [!success]- Answer  
> > > b) Потому что они выполняются до возврата в host, блокируя rendering cycle  
> > > Explanation:  
> > > long microtask chains can starve rendering.  
> > > a неверен: не навсегда.  
> > > c неверен: обычно same thread.  
> > > d неверен: ECS не уничтожается.

> [!question] Какое утверждение наиболее точно описывает relation между module graph и Execution Context?  
> a) Каждый модуль создаёт новый Agent  
> b) Module graph определяет порядок linking/evaluation, а Execution Context используется во время evaluation  
> c) Execution Context заменяет module graph  
> d) Module graph существует только в runtime heap
> 
> > > [!success]- Answer  
> > > b) Module graph определяет порядок linking/evaluation, а Execution Context используется во время evaluation  
> > > Explanation:  
> > > graph = structure, context = execution.  
> > > a неверен: agents не создаются на модуль.  
> > > c неверен: разные уровни.  
> > > d неверен: graph — не heap-only concept.

---
> [!question] Почему утверждение "garbage collector автоматически понимает, какие замыкания (closures) больше не нужны, исходя из того, что функция завершила выполнение" является упрощением?  
> a) Потому что GC анализирует только стек вызовов и игнорирует heap  
> b) Потому что живучесть closure определяется достижимостью объектов в heap, а не фактом завершения функции  
> c) Потому что closures удаляются сразу после выхода из Execution Context  
> d) Потому что GC работает только для глобального объекта
> 
> > > [!success]- Answer  
> > > b) Потому что живучесть closure определяется достижимостью объектов в heap, а не фактом завершения функции  
> > > Explanation:  
> > > Closure живёт, пока на него есть ссылки в heap (lexical environment reference).  
> > > a неверен: GC работает не только со стеком.  
> > > c неверен: завершение контекста не означает удаление closure.  
> > > d неверен: GC работает со всей памятью.

> [!question] Почему утверждение "все объекты, созданные внутри функции, удаляются после выхода из Execution Context" является неверным?  
> a) Потому что объекты могут быть возвращены и сохранены в heap через ссылки  
> b) Потому что GC не существует в JavaScript  
> c) Потому что Execution Context хранится в global scope  
> d) Потому что объекты перемещаются в Realm после завершения функции
> 
> > > [!success]- Answer  
> > > a) Потому что объекты могут быть возвращены и сохранены в heap через ссылки  
> > > Explanation:  
> > > объекты выживают через references (closures, returns).  
> > > b неверен: GC существует.  
> > > c неверен: EC не хранится в global scope.  
> > > d неверен: Realm не «хранилище объектов».

> [!question] Что наиболее точно объясняет явление "async starvation" при интенсивном использовании microtasks?  
> a) Microtasks выполняются параллельно в разных Agent  
> b) Microtask queue может бесконечно продлевать выполнение до возврата control to host  
> c) Event Loop блокируется навсегда при Promise rejection  
> d) GC останавливает выполнение microtasks
> 
> > > [!success]- Answer  
> > > b) Microtask queue может бесконечно продлевать выполнение до возврата control to host  
> > > Explanation:  
> > > бесконечные microtasks блокируют macrotask/render phase.  
> > > a неверен: обычно single-thread.  
> > > c неверен: rejection не блокирует loop.  
> > > d неверен: GC не управляет очередями.

> [!question] Почему утверждение "Promise.resolve().then(...) выполняется немедленно" является некорректным?  
> a) Потому что Promise.resolve не существует  
> b) Потому что then всегда планируется как microtask, даже если Promise уже resolved  
> c) Потому что then выполняется в синхронном Execution Context  
> d) Потому что Promise API работает только асинхронно через timers
> 
> > > [!success]- Answer  
> > > b) Потому что then всегда планируется как microtask, даже если Promise уже resolved  
> > > Explanation:  
> > > even resolved promise schedules job.  
> > > a неверен: Promise.resolve существует.  
> > > c неверен: then не синхронен.  
> > > d неверен: не через timers.

> [!question] Какое утверждение наиболее точно описывает поведение nested microtasks?  
> a) Они выполняются строго после следующего macrotask  
> b) Они формируют рекурсивное drain microtask queue до полного опустошения  
> c) Они создают новый Execution Context Stack  
> d) Они выполняются только после GC cycle
> 
> > > [!success]- Answer  
> > > b) Они формируют рекурсивное drain microtask queue до полного опустошения  
> > > Explanation:  
> > > microtasks run-to-completion before host regain control.  
> > > a неверен: не ждут macrotask.  
> > > c неверен: ECS не создаётся.  
> > > d неверен: GC не связан.

> [!question] Почему утверждение "Event Loop определяет порядок выполнения microtasks и macrotasks" является некорректным с точки зрения ECMAScript?  
> a) Потому что Event Loop отсутствует в спецификации ECMAScript и является частью host environment  
> b) Потому что microtasks не существуют  
> c) Потому что macrotasks выполняются в parallel threads  
> d) Потому что порядок определяется только GC
> 
> > > [!success]- Answer  
> > > a) Потому что Event Loop отсутствует в спецификации ECMAScript и является частью host environment  
> > > Explanation:  
> > > event loop defined in HTML spec.  
> > > b неверен: microtasks exist.  
> > > c неверен: not necessarily parallel.  
> > > d неверен: GC irrelevant.

> [!question] Какое утверждение наиболее точно описывает связь между Realm и GC (garbage collection)?  
> a) Realm автоматически уничтожается после завершения каждой функции  
> b) Realm может быть собран GC только если все связанные объекты и Execution Context недостижимы  
> c) Realm не участвует в garbage collection вообще  
> d) Realm существует только в compile-time
> 
> > > [!success]- Answer  
> > > b) Realm может быть собран GC только если все связанные объекты и Execution Context недостижимы  
> > > Explanation:  
> > > realm lifetime depends on reachability.  
> > > a неверен: не per-function.  
> > > c неверен: участвует через objects.  
> > > d неверен: runtime entity.

> [!question] Почему утверждение "async/await блокирует Execution Context Stack" является некорректным?  
> a) Потому что await превращает код в synchronous function  
> b) Потому что await только приостанавливает текущий Execution Context, не блокируя поток  
> c) Потому что await создаёт новый thread  
> d) Потому что await запрещён в strict mode
> 
> > > [!success]- Answer  
> > > b) Потому что await только приостанавливает текущий Execution Context, не блокируя поток  
> > > Explanation:  
> > > suspension ≠ blocking.  
> > > a неверен: не sync.  
> > > c неверен: no threads.  
> > > d неверен: allowed in strict mode.

> [!question] Что наиболее точно описывает поведение chained promises при исключении ошибки в середине цепочки?  
> a) Ошибка немедленно останавливает Event Loop  
> b) Ошибка превращается в rejected Promise, который передаётся дальше по chain via microtasks  
> c) Ошибка игнорируется движком  
> d) Ошибка превращает весь Execution Context в invalid state
> 
> > > [!success]- Answer  
> > > b) Ошибка превращается в rejected Promise, который передаётся дальше по chain via microtasks  
> > > Explanation:  
> > > rejection propagates through promise chain.  
> > > a неверен: loop continues.  
> > > c неверен: не игнорируется.  
> > > d неверен: EC не инвалидируется.

> [!question] Почему утверждение "module evaluation выполняется сразу после import" является неточным?  
> a) Потому что evaluation может быть отложен до завершения linking всех зависимостей  
> b) Потому что modules не выполняются вообще  
> c) Потому что import всегда синхронный runtime call  
> d) Потому что modules выполняются только в worker threads
> 
> > > [!success]- Answer  
> > > a) Потому что evaluation может быть отложен до завершения linking всех зависимостей  
> > > Explanation:  
> > > module graph must be fully linked first.  
> > > b неверен: выполняются.  
> > > c неверен: import is static.  
> > > d неверен: not only workers.

> [!question] Какое утверждение наиболее точно объясняет поведение GC при наличии циклических ссылок?  
> a) Циклы всегда приводят к утечкам памяти  
> b) GC удаляет циклы, если они недостижимы из root set  
> c) GC игнорирует циклы полностью  
> d) GC удаляет только примитивы
> 
> > > [!success]- Answer  
> > > b) GC удаляет циклы, если они недостижимы из root set  
> > > Explanation:  
> > > modern GC is reachability-based.  
> > > a неверен: cycles ≠ leak automatically.  
> > > c неверен: cycles handled.  
> > > d неверен: GC works on objects.

> [!question] Почему утверждение "microtasks выполняются после каждого await и всегда отдельно от Promise chain" является некорректным?  
> a) Потому что await не использует microtasks  
> b) Потому что await и Promise chain используют одну и ту же microtask queue  
> c) Потому что microtasks выполняются только в Node.js  
> d) Потому что await блокирует Event Loop
> 
> > > [!success]- Answer  
> > > b) Потому что await и Promise chain используют одну и ту же microtask queue  
> > > Explanation:  
> > > await is built on promises/microtasks.  
> > > a неверен: uses microtasks.  
> > > c неверен: cross-platform.  
> > > d неверен: no blocking.

> [!question] Какое утверждение наиболее точно описывает роль Execution Context при module evaluation?  
> a) Execution Context не используется в module evaluation  
> b) Module evaluation выполняется внутри Execution Context с отдельным lexical environment  
> c) Module evaluation всегда создаёт новый Realm  
> d) Module evaluation происходит вне JS engine
> 
> > > [!success]- Answer  
> > > b) Module evaluation выполняется внутри Execution Context с отдельным lexical environment  
> > > Explanation:  
> > > modules execute within EC + module environment.  
> > > a неверен: используется.  
> > > c неверен: realm не создаётся.  
> > > d неверен: always inside engine.

> [!question] Почему утверждение "GC и Event Loop синхронизированы по шагам выполнения" является неверным?  
> a) Потому что GC работает только при синтаксических ошибках  
> b) Потому что GC запускается по heuristics движка, а не по шагам Event Loop  
> c) Потому что Event Loop управляет GC напрямую  
> d) Потому что GC выполняется только вручную через API
> 
> > > [!success]- Answer  
> > > b) Потому что GC запускается по heuristics движка, а не по шагам Event Loop  
> > > Explanation:  
> > > GC is independent subsystem.  
> > > a неверен: not error-driven.  
> > > c неверен: no direct control.  
> > > d неверен: no manual API in JS.

> [!question] Почему утверждение "Realm = глобальный объект + набор встроенных функций" является неполным?  
> a) Потому что Realm включает только globalThis  
> b) Потому что Realm включает intrinsics, global object и execution environment configuration  
> c) Потому что Realm не включает встроенные объекты  
> d) Потому что Realm существует только для modules
> 
> > > [!success]- Answer  
> > > b) Потому что Realm включает intrinsics, global object и execution environment configuration  
> > > Explanation:  
> > > Realm = full execution environment.  
> > > a неверен: too narrow.  
> > > c неверен: includes intrinsics.  
> > > d неверен: exists beyond modules.

---
