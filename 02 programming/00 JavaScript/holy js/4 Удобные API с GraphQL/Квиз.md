#flashquiz/proga/js/holyjs/4
> [!question] Какие три ключевых требования к "удобному API" между клиентом и сервером формулирует автор доклада?
> a) Эффективность (минимум запросов и лишних данных), стабильность (не нужно менять API при каждом изменении клиента) и удобные инструменты (автодополнение, валидация).
> b) Безопасность, масштабируемость и низкая стоимость хостинга.
> c) Поддержка WebSocket, GraphQL-схемы и статической типизации на клиенте.
> d) Открытый исходный код, кроссплатформенность и наличие официальной документации на русском языке.
>> [!success]- Answer
>> a) Эффективность (минимум запросов и лишних данных), стабильность (не нужно менять API при каждом изменении клиента) и удобные инструменты (автодополнение, валидация).
>> Explanation:
>> Это три требования, вокруг которых выстроен весь дальнейший разбор GraphQL в докладе.
>> b), c), d) не упоминаются как основные критерии "удобного API" в докладе.

> [!question] Почему стабильность API особенно критична именно для мобильных приложений по сравнению с веб-приложениями?
> a) Разработчик не может контролировать, когда пользователь обновит мобильное приложение, и сервер может годами обслуживать множество разных версий клиента одновременно.
> b) Мобильные приложения технически не поддерживают REST API вообще.
> c) Веб-приложения не требуют обратной совместимости API в принципе.
> d) Мобильные сети физически не поддерживают JSON-формат данных.
>> [!success]- Answer
>> a) Разработчик не может контролировать, когда пользователь обновит мобильное приложение, и сервер может годами обслуживать множество разных версий клиента одновременно.
>> Explanation:
>> В докладе прямо указано, что версия мобильного приложения может "стоять" у пользователя годами, в отличие от веб-фронтенда, который можно обновить централизованно.
>> b), c), d) не соответствуют содержанию доклада.

> [!question] Из каких двух связанных частей состоит GraphQL, согласно докладу?
> a) Из языка запросов (описание графа нужных данных) и языка описания схемы (формальная типизированная система доступных данных на сервере).
> b) Из HTTP-протокола и WebSocket-протокола.
> c) Из клиентской библиотеки Relay и серверной библиотеки Apollo Server.
> d) Из системы кэширования и системы аутентификации.
>> [!success]- Answer
>> a) Из языка запросов (описание графа нужных данных) и языка описания схемы (формальная типизированная система доступных данных на сервере).
>> Explanation:
>> Именно наличие обеих частей вместе позволяет проверять запросы по схеме и строить универсальные инструменты.
>> b), c), d) описывают конкретные технологии/библиотеки, а не сами составные части спецификации GraphQL.

> [!question] Зачем нужна типизированная схема на GraphQL-сервере, помимо документирования доступных данных?
> a) Каждый входящий запрос проверяется по схеме ещё до выполнения, поэтому клиент не может запросить несуществующее поле, а сама схема может быть отдана программно для построения универсальных инструментов.
> b) Схема нужна исключительно для генерации документации в формате PDF.
> c) Схема заменяет необходимость в резолверах — данные достаются автоматически без дополнительного кода.
> d) Схема нужна только для проверки прав доступа пользователя.
>> [!success]- Answer
>> a) Каждый входящий запрос проверяется по схеме ещё до выполнения, поэтому клиент не может запросить несуществующее поле, а сама схема может быть отдана программно для построения универсальных инструментов.
>> Explanation:
>> Именно наличие формальной схемы и её программная доступность (интроспекция) отличает GraphQL от классического REST, где единого формата метаданных не сложилось.
>> b), c), d) не соответствуют роли схемы, описанной в докладе.

> [!question] Каким образом GraphQL обеспечивает обратную совместимость при добавлении новой фичи (например, поля "лайки" к посту)?
> a) Достаточно добавить новое поле в схему на сервере; старые клиенты, не запрашивающие это поле, продолжают работать без изменений, а новые клиенты просто начинают его запрашивать.
> b) Нужно создать полностью новую версию API (v2) и держать обе версии параллельно бесконечно.
> c) Все существующие клиенты автоматически обязаны обновиться при добавлении любого нового поля.
> d) Добавление нового поля всегда требует миграции базы данных клиента.
>> [!success]- Answer
>> a) Достаточно добавить новое поле в схему на сервере; старые клиенты, не запрашивающие это поле, продолжают работать без изменений, а новые клиенты просто начинают его запрашивать.
>> Explanation:
>> Это ключевое преимущество GraphQL перед классическим REST в вопросе версионирования, разобранное в докладе.
>> b), c), d) не соответствуют механике GraphQL, описанной в докладе.

> [!question] При каком условии в GraphQL безопасно удалять поле из схемы, не рискуя сломать существующих клиентов?
> a) Только когда по логам реальных запросов видно, что ни один клиент больше не запрашивает это поле.
> b) Поле можно удалять в любой момент, если оно помечено как @deprecated, независимо от того, используется ли оно ещё клиентами.
> c) Поле можно удалять только раз в год согласно спецификации GraphQL.
> d) Удаление поля никогда не является безопасным в GraphQL ни при каких условиях.
>> [!success]- Answer
>> a) Только когда по логам реальных запросов видно, что ни один клиент больше не запрашивает это поле.
>> Explanation:
>> Пометка @deprecated лишь скрывает поле из автодополнения инструментов, подталкивая разработчиков перестать его использовать, но фактическое удаление безопасно только после подтверждения отсутствия использования.
>> b), c), d) не соответствуют объяснению из доклада.

> [!question] Что такое resolve-функция (резолвер) в реализации GraphQL-сервера, и в чём заключается её ключевая гибкость?
> a) Функция, назначенная конкретному полю схемы, которая знает, откуда взять данные; резолверы разных полей могут обращаться к разным источникам данных, и для клиента всё выглядит как единый согласованный граф.
> b) Функция, которая полностью заменяет необходимость в схеме и типах данных.
> c) Функция, которая работает исключительно с одной конкретной базой данных, указанной при инициализации сервера.
> d) Функция, вызываемая только один раз при запуске сервера для предзагрузки всех данных в память.
>> [!success]- Answer
>> a) Функция, назначенная конкретному полю схемы, которая знает, откуда взять данные; резолверы разных полей могут обращаться к разным источникам данных, и для клиента всё выглядит как единый согласованный граф.
>> Explanation:
>> Именно эта гибкость резолверов делает GraphQL удобным для объединения разнородных источников данных (БД, сторонние API, legacy REST) в единый граф.
>> b), c), d) не соответствуют роли резолверов, описанной в докладе.

> [!question] В чём заключается проблема N+1 в контексте GraphQL, проиллюстрированная примером с комментариями к картинке?
> a) Если резолвер для получения списка комментариев реализован "в лоб", а затем для каждого комментария отдельно запрашивается его автор, то при 10 комментариях возникает 10 дополнительных отдельных запросов вместо одного объединённого.
> b) Проблема N+1 означает, что GraphQL-сервер всегда требует ровно на один запрос больше, чем REST API для тех же данных.
> c) Проблема N+1 возникает только при использовании подписок (subscriptions) в GraphQL.
> d) Проблема N+1 — это ограничение, при котором GraphQL-схема не может содержать больше одного корневого типа запроса.
>> [!success]- Answer
>> a) Если резолвер для получения списка комментариев реализован "в лоб", а затем для каждого комментария отдельно запрашивается его автор, то при 10 комментариях возникает 10 дополнительных отдельных запросов вместо одного объединённого.
>> Explanation:
>> Это классическая проблема N+1, знакомая из ORM, применительно к вложенным GraphQL-резолверам.
>> b), c), d) не соответствуют определению проблемы N+1 из доклада.

> [!question] Какие два приёма используются в продакшене (например, в Facebook) для решения проблемы N+1 в GraphQL?
> a) Батчинг множества однотипных запросов в один пакетный запрос, и кэш на время одного GraphQL-запроса для повторяющихся объектов.
> b) Полный отказ от вложенных полей в схеме и переход на плоскую структуру данных.
> c) Ограничение схемы только одним уровнем вложенности без исключений.
> d) Дублирование базы данных для каждого отдельного клиента.
>> [!success]- Answer
>> a) Батчинг множества однотипных запросов в один пакетный запрос, и кэш на время одного GraphQL-запроса для повторяющихся объектов.
>> Explanation:
>> Оба приёма прямо описаны в докладе как основной способ решения N+1 в реальных GraphQL-серверах.
>> b), c), d) не соответствуют содержанию доклада.

> [!question] Чем принципиально отличается работа Relay с клиентским хранилищем данных от паттерна Redux?
> a) В отличие от Redux, Relay не требует ручного написания экшенов — он заранее знает схему и типы данных из GraphQL-запроса и автоматически нормализует пришедшие данные по типу и id.
> b) Relay вообще не использует центральное хранилище данных, в отличие от Redux.
> c) Redux требует GraphQL-схему для работы, а Relay — нет.
> d) Relay и Redux идентичны по архитектуре и отличаются только названием.
>> [!success]- Answer
>> a) В отличие от Redux, Relay не требует ручного написания экшенов — он заранее знает схему и типы данных из GraphQL-запроса и автоматически нормализует пришедшие данные по типу и id.
>> Explanation:
>> Это ключевое архитектурное отличие, подчёркнутое в докладе как преимущество Relay именно благодаря наличию GraphQL-схемы.
>> b), c), d) не соответствуют содержанию доклада.

> [!question] Почему привязка компонента к фрагменту GraphQL-запроса в Relay делает компонент более переиспользуемым, а не более жёстко связанным с данными?
> a) Компонент можно перенести в любое другое место дерева React-приложения без ручной настройки источника данных — Relay сам подтягивает нужные данные по дереву вложенных фрагментов.
> b) Потому что фрагменты GraphQL автоматически удаляются при переносе компонента в другое место дерева.
> c) Потому что фрагменты работают только с глобальным состоянием Redux, а не с конкретным компонентом.
> d) Потому что каждый фрагмент содержит полную копию всей схемы сервера целиком.
>> [!success]- Answer
>> a) Компонент можно перенести в любое другое место дерева React-приложения без ручной настройки источника данных — Relay сам подтягивает нужные данные по дереву вложенных фрагментов.
>> Explanation:
>> Это ключевой аргумент доклада против интуитивного ощущения "жёсткой связки" компонента и данных.
>> b), c), d) не соответствуют механике Relay, описанной в докладе.

> [!question] Что произойдёт в Relay, если компонент попытается использовать поле данных, которое не было явно запрошено в его собственном GraphQL-фрагменте?
> a) Relay не даст компоненту получить доступ к этому полю, гарантируя, что компонент видит только явно запрошенные им данные.
> b) Relay автоматически добавит недостающее поле в фрагмент компонента без участия разработчика.
> c) Приложение продолжит работать, но поле будет содержать случайное значение.
> d) Relay отправит дополнительный отдельный HTTP-запрос за недостающим полем в фоне.
>> [!success]- Answer
>> a) Relay не даст компоненту получить доступ к этому полю, гарантируя, что компонент видит только явно запрошенные им данные.
>> Explanation:
>> Это гарантия, описанная в докладе как способ защититься от случайного использования незапрошенных данных.
>> b), c), d) не соответствуют механике Relay из доклада.

> [!question] Что означает "оптимистичность с откатом" применительно к мутациям в Relay?
> a) Изменение сразу применяется на клиенте без ожидания ответа сервера, а при отказе сервера автоматически откатывается.
> b) Мутация выполняется только после получения подтверждения от трёх независимых серверов одновременно.
> c) Мутация никогда не отправляется на сервер, пока пользователь не подтвердит действие дважды.
> d) Откат означает, что мутацию нельзя выполнить повторно в течение одной сессии.
>> [!success]- Answer
>> a) Изменение сразу применяется на клиенте без ожидания ответа сервера, а при отказе сервера автоматически откатывается.
>> Explanation:
>> Это стандартное определение оптимистичного обновления, применённое в докладе к мутациям Relay.
>> b), c), d) не соответствуют объяснению из доклада.

> [!question] Почему GraphQL плохо подходит для эффективной трансляции запросов в реляционные SQL-базы данных, по словам автора?
> a) Не существует общего решения, надёжно транслирующего произвольный вложенный граф запроса в один эффективный SQL-запрос, так как данные на разных уровнях вложенности могут требовать разной сортировки.
> b) GraphQL технически несовместим с протоколом SQL на уровне спецификации.
> c) SQL-базы данных не поддерживают JSON, а GraphQL возвращает только JSON.
> d) Reindex и Facebook официально запрещают использование SQL вместе с GraphQL.
>> [!success]- Answer
>> a) Не существует общего решения, надёжно транслирующего произвольный вложенный граф запроса в один эффективный SQL-запрос, так как данные на разных уровнях вложенности могут требовать разной сортировки.
>> Explanation:
>> Именно поэтому на практике GraphQL чаще комбинируют с NoSQL-хранилищами или используют SQL как key-value store, как упомянуто в докладе.
>> b), c), d) не соответствуют объяснению из доклада.

> [!question] В каких двух случаях автор доклада прямо не рекомендует использовать GraphQL?
> a) Когда нужен полностью реактивный интерфейс (подписки ещё не были готовы к полноценному продакшену вне Facebook), и когда данные не имеют единого источника достоверности (например, полу-P2P синхронизируемый чат).
> b) Когда приложение написано на React, и когда используется TypeScript.
> c) Когда команда разработки насчитывает более 10 человек.
> d) Когда приложение работает только в браузере без мобильного клиента.
>> [!success]- Answer
>> a) Когда нужен полностью реактивный интерфейс (подписки ещё не были готовы к полноценному продакшену вне Facebook), и когда данные не имеют единого источника достоверности (например, полу-P2P синхронизируемый чат).
>> Explanation:
>> Это прямой ответ автора на вопрос о границах применимости GraphQL в конце доклада.
>> b), c), d) не упоминаются в докладе как ограничения применимости.

> [!question] Каким образом можно ограничить потенциально экспоненциально растущую сложность вложенных GraphQL-запросов (например, "друзья друзей")?
> a) Спецификация на момент доклада не имела встроенного механизма; отдельные реализации (например, на Scala) добавляли статический анализ сложности запроса до его выполнения.
> b) GraphQL автоматически ограничивает глубину вложенности до трёх уровней на уровне спецификации.
> c) Такая проблема принципиально не может возникнуть в GraphQL благодаря архитектуре языка запросов.
> d) Единственное решение — полностью отключить поддержку вложенных полей в схеме.
>> [!success]- Answer
>> a) Спецификация на момент доклада не имела встроенного механизма; отдельные реализации (например, на Scala) добавляли статический анализ сложности запроса до его выполнения.
>> Explanation:
>> Автор прямо называет это открытой проблемой на момент доклада, решаемой на уровне конкретных реализаций, а не самой спецификации.
>> b), c), d) не соответствуют содержанию доклада.

> [!question] Зачем компании вроде Facebook и Reindex кодируют глобальные идентификаторы объектов (например, через base64) в непрозрачную для клиента строку?
> a) Чтобы клиент не мог полагаться на внутреннюю структуру ID, что позволяет в будущем сменить формат хранения данных, не ломая контракт с клиентом.
> b) Чтобы уменьшить размер ответа сервера за счёт более компактного представления чисел.
> c) Чтобы скрыть от пользователей общее количество объектов в базе данных из соображений конкуренции.
> d) Это требование спецификации GraphQL, обязательное для всех реализаций.
>> [!success]- Answer
>> a) Чтобы клиент не мог полагаться на внутреннюю структуру ID, что позволяет в будущем сменить формат хранения данных, не ломая контракт с клиентом.
>> Explanation:
>> Это прямой ответ автора на вопрос про обфускацию ID в Q&A части доклада.
>> b), c), d) не соответствуют объяснению из доклада.

> [!question] В чём ключевое отличие Falcor (Netflix) от GraphQL, согласно сравнению в докладе?
> a) Falcor не имеет формальной типизированной схемы и является скорее языком запросов, способным работать с произвольным (не обязательно GraphQL-совместимым) API.
> b) Falcor полностью идентичен GraphQL по архитектуре и отличается только синтаксисом запросов.
> c) Falcor поддерживает подписки (subscriptions), а GraphQL на момент доклада — нет.
> d) Falcor — это исключительно серверная библиотека без клиентской части.
>> [!success]- Answer
>> a) Falcor не имеет формальной типизированной схемы и является скорее языком запросов, способным работать с произвольным (не обязательно GraphQL-совместимым) API.
>> Explanation:
>> Автор прямо указывает, что Falcor стоит сравнивать скорее с Relay, чем с самим GraphQL, именно из-за отсутствия схемы.
>> b), c), d) не соответствуют сравнению, данному в докладе.

> [!question] Что означают "частичные (потоковые) запросы" — одно из готовящихся направлений развития GraphQL, упомянутых в докладе?
> a) Возможность пометить часть полей как менее приоритетные, чтобы сервер сначала присылал данные для немедленной отрисовки, а второстепенные данные — позже отдельными частями.
> b) Возможность разбить один GraphQL-запрос на несколько независимых HTTP-соединений для параллельной загрузки.
> c) Автоматическое сжатие ответа сервера с помощью потокового gzip-архивирования.
> d) Возможность запрашивать только часть полей схемы без её полного описания.
>> [!success]- Answer
>> a) Возможность пометить часть полей как менее приоритетные, чтобы сервер сначала присылал данные для немедленной отрисовки, а второстепенные данные — позже отдельными частями.
>> Explanation:
>> Это прямое описание идеи директив вроде @defer/@stream, озвученной в докладе как будущее направление развития GraphQL.
>> b), c), d) не соответствуют описанию из доклада.

> [!question] Как в GraphQL, согласно ответу автора в Q&A, принято обрабатывать ситуацию отсутствия прав доступа к конкретному полю?
> a) Резолвер этого поля проверяет права доступа и возвращает null, а клиент должен уметь интерпретировать null как "либо данных нет, либо нет прав на просмотр".
> b) Сервер возвращает HTTP-код 403 для всего запроса целиком, даже если прав не хватает только на одно поле.
> c) GraphQL-спецификация автоматически прерывает выполнение всего запроса при отсутствии прав хотя бы на одно поле.
> d) Поле полностью удаляется из схемы для всех пользователей без прав доступа.
>> [!success]- Answer
>> a) Резолвер этого поля проверяет права доступа и возвращает null, а клиент должен уметь интерпретировать null как "либо данных нет, либо нет прав на просмотр".
>> Explanation:
>> Это прямой ответ автора на вопрос про права доступа к вложенным полям в Q&A части доклада.
>> b), c), d) не соответствуют паттерну, описанному в докладе.
