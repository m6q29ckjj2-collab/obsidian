#flashquiz/proga/js/holyjs/1
> [!question] Автор бенчмарка на Хабре сделал вывод «выделение литерала массива внутри функции — дорогая операция», сравнив создание массива внутри и снаружи функции find. Какое возражение точнее всего соответствует разбору из доклада?
> a) Вывод корректен — выделение массива действительно всегда дороже, если оно происходит внутри функции.
> b) Замедление было вызвано отсутствием быстрого пути создания литерала массива в конкретной старой версии V8, а не фундаментальной дороговизной выделения массива.
> c) Замедление объясняется тем, что массив снаружи функции хранится в другом сегменте памяти.
> d) Замедление было вызвано сборкой мусора, вызванной вызовом find.
>> [!success]- Answer
>> b) Замедление было вызвано отсутствием быстрого пути создания литерала массива в конкретной старой версии V8, а не фундаментальной дороговизной выделения массива.
>> Explanation:
>> Профилирование показало проблему в конкретной реализации движка (аналог FastCloneShallowArray), а не в самой операции выделения массива.
>> a) Это ровно то ошибочное обобщение, которое разбирает доклад.
>> c) Не упоминается и не соответствует объяснению из доклада.
>> d) GC не был причиной в этом примере.

> [!question] В примере с ChaCha20 ручной инлайнинг тела функции в цикл неожиданно замедлил код в 10 раз. Что оказалось истинной причиной?
> a) Ручной инлайнинг увеличил размер кода настолько, что он перестал помещаться в инструкционный кэш.
> b) JIT счёл часть кода мёртвым и удалил её, из-за чего в бенчмарке измерялось меньше реальной работы, чем задумывалось, пока не была добавлена подсказка компилятору.
> c) Ручной инлайнинг сломал корректность шифрования, и код завершался с ошибкой раньше времени.
> d) Ручной инлайнинг помешал JIT применить SIMD-инструкции.
>> [!success]- Answer
>> b) JIT счёл часть кода мёртвым и удалил её, из-за чего в бенчмарке измерялось меньше реальной работы, чем задумывалось, пока не была добавлена подсказка компилятору.
>> Explanation:
>> Добавление директивы-подсказки («не удалять эту строку») возвращало ожидаемую производительность — значит, дело было в удалении мёртвого кода.
>> a) Не упоминается в докладе.
>> c) Не соответствует описанию — код продолжал исполняться и выдавать результат.
>> d) Не упоминается в докладе.

> [!question] Библиотека Benchmark.js (движок jsPerf) оборачивает измеряемый код в дополнительный цикл повторений. Какое из следствий этого подхода иллюстрирует пример с подсчётом буквы «е» через slice в цикле?
> a) Повторные итерации в цикле-обёртке получали уже пустую строку, потому что предыдущая итерация «съела» её через slice, поэтому измерялась не заявленная операция.
> b) Slice создаёт новую строку в памяти, что всегда быстрее в Firefox благодаря специальной оптимизации строк.
> c) Benchmark.js специально предварительно кэширует результат первой итерации для всех остальных.
> d) Firefox использует другой алгоритм сборки мусора для строк, что ускоряет slice.
>> [!success]- Answer
>> a) Повторные итерации в цикле-обёртке получали уже пустую строку, потому что предыдущая итерация «съела» её через slice, поэтому измерялась не заявленная операция.
>> Explanation:
>> Именно это делает результат бенчмарка бессмысленным — измеряется обработка пустой строки, а не исходной.
>> b), c), d) не упоминаются и не соответствуют механике, разобранной в докладе.

> [!question] Формула «стоимость операции = общее время / число повторов N» предполагает, что операция стоит одну и ту же величину C при каждом повторении. Почему это допущение обычно неверно в среде с JIT-компиляцией?
> a) Потому что JIT всегда компилирует код заново при каждом вызове функции.
> b) Потому что существует минимум неоптимизированная и оптимизированная версии операции (а часто и больше), и реальная стоимость меняется в процессе выполнения.
> c) Потому что процессор не может гарантировать одинаковое время выполнения одной и той же инструкции дважды.
> d) Потому что сборщик мусора запускается строго на каждой N-й итерации.
>> [!success]- Answer
>> b) Потому что существует минимум неоптимизированная и оптимизированная версии операции (а часто и много больше), и реальная стоимость меняется в процессе выполнения.
>> Explanation:
>> Это ключевая мысль доклада о математике микробенчмарков — стоимость операции не константна в мире JIT.
>> a) JIT наоборот старается не перекомпилировать без необходимости.
>> c), d) не являются объяснением, которое даёт доклад.

> [!question] Если реальная операция стоит C только на первой итерации, а остальные N−1 итераций были удалены компилятором как мёртвый код, что произойдёт с оценкой «стоимость операции» при увеличении N?
> a) Оценка будет стремиться к C, потому что среднее значение сходится к истинной стоимости.
> b) Оценка будет стремиться к нулю, создавая иллюзию произвольно дешёвой операции.
> c) Оценка останется постоянной и равной C независимо от N.
> d) Оценка будет расти линейно с N.
>> [!success]- Answer
>> b) Оценка будет стремиться к нулю, создавая иллюзию произвольно дешёвой операции.
>> Explanation:
>> (C + 0·(N−1))/N → 0 при росте N — классическая иллюстрация того, как dead code elimination обманывает микробенчмарк.
>> a), c), d) противоречат приведённой в докладе арифметике.

> [!question] Бенчмарк показал, что `~~x` («двойная тильда») быстрее `parseInt` в одном из браузеров. Какое объяснение точнее всего соответствует разбору доклада?
> a) `~~x` — это действительно более быстрая операция преобразования в число на уровне процессора в любом движке.
> b) Значение x фактически не менялось в цикле, поэтому JIT применил constant folding и вынос инварианта, а неиспользуемый результат был удалён как мёртвый код — измерялся почти пустой цикл.
> c) `parseInt` в этом браузере не был оптимизирован из-за отсутствия JIT-поддержки строковых функций.
> d) Разница объясняется тем, что `~~x` не создаёт промежуточную строку, а `parseInt` создаёт.
>> [!success]- Answer
>> b) Значение x фактически не менялось в цикле, поэтому JIT применил constant folding и вынос инварианта, а неиспользуемый результат был удалён как мёртвый код — измерялся почти пустой цикл.
>> Explanation:
>> Именно эта комбинация оптимизаций («умная собака») превращает бенчмарк в измерение пустоты.
>> a), c), d) не соответствуют объяснению из доклада и являются как раз тем самым ошибочным обобщением, которое доклад критикует.

> [!question] Автор советует использовать минимум две «вращающиеся» непредсказуемые переменные и обязательно потреблять результат каждой итерации микробенчмарка. От каких двух оптимизаций компилятора это защищает соответственно?
> a) От loop unrolling и от инлайнинга функций.
> b) От constant folding (и выноса инвариантов) и от dead code elimination.
> c) От сборки мусора и от JIT-деоптимизации.
> d) От спекулятивного выполнения процессора и от branch prediction.
>> [!success]- Answer
>> b) От constant folding (и выноса инвариантов) и от dead code elimination.
>> Explanation:
>> Непредсказуемые «вращающиеся» переменные мешают компилятору свести вычисление к константе; потребление результата не даёт компилятору посчитать код мёртвым.
>> a), c), d) не являются целью этих двух приёмов согласно докладу.

> [!question] Почему даже приёмы «вращения переменных» и «потребления результата» не гарантируют полностью честный микробенчмарк, согласно докладу?
> a) Потому что компилятор может применить loop unrolling (раскрутку цикла), продублировав тело и всё равно найдя в нём мёртвый или инвариантный код.
> b) Потому что JavaScript не позволяет объявлять более одной переменной в цикле.
> c) Потому что Benchmark.js игнорирует пользовательские переменные.
> d) Потому что раскрутка цикла запрещена спецификацией ECMAScript.
>> [!success]- Answer
>> a) Потому что компилятор может применить loop unrolling (раскрутку цикла), продублировав тело и всё равно найдя в нём мёртвый или инвариантный код.
>> Explanation:
>> Это прямое продолжение мысли доклада: защитные приёмы снижают, но не устраняют риск того, что компилятор «съест» бенчмарк.
>> b), c), d) не соответствуют содержанию доклада.

> [!question] В старой версии V8 доступ к обычному свойству (не геттеру), объявленному на прототипе, был примерно в 8 раз медленнее, чем к той же переменной, вынесенной вручную из цикла. Какова была реальная причина?
> a) В этой версии V8 отсутствовало инлайн-кэширование для обычных свойств на прототипе — оно работало только для аксессоров.
> b) Обычные свойства на прототипе в принципе не поддерживаются движком и требуют обхода через Proxy.
> c) Причина в том, что прототип хранился в отдельном сегменте памяти с более медленным доступом.
> d) Причина в переполнении кэша процессора при глубоких цепочках прототипов.
>> [!success]- Answer
>> a) В этой версии V8 отсутствовало инлайн-кэширование для обычных свойств на прототипе — оно работало только для аксессоров.
>> Explanation:
>> В актуальной на момент доклада версии V8 оба случая работают одинаково быстро — вручную кэшировать свойства прототипа не требуется.
>> b), c), d) не соответствуют объяснению доклада и являются придуманными обобщениями.

> [!question] В следующем эксперименте геттер на прототипе оказался в 8 раз быстрее обычного свойства — доклад называет это «уличной магией». В чём заключался обман?
> a) Геттер использует внутренний быстрый ассемблерный путь, недоступный для обычных свойств.
> b) Значение, возвращаемое геттером, оказалось эффективно константным для цикла, и JIT вынес его вычисление за пределы цикла — измерялась почти пустая операция, а не реальная стоимость геттера.
> c) Обычные свойства требуют дополнительной проверки типов при каждом обращении, а геттеры — нет.
> d) Геттеры в V8 кэшируются глобально между вызовами функции.
>> [!success]- Answer
>> b) Значение, возвращаемое геттером, оказалось эффективно константным для цикла, и JIT вынес его вычисление за пределы цикла — измерялась почти пустая операция, а не реальная стоимость геттера.
>> Explanation:
>> Это ещё один пример того, как loop-invariant code motion полностью меняет смысл результата микробенчмарка.
>> a), c), d) не упоминаются и не соответствуют разбору доклада.

> [!question] Почему повторный вызов функции со счётчиком, растущим в цикле, оказался резко медленнее первого вызова той же функции?
> a) При превышении диапазона Smi число переходит к int32, затем к double, а double хранится в «коробочном» (boxed) представлении в куче — каждое приращение требует новой аллокации.
> b) Второй вызов функции всегда провоцирует полную сборку мусора перед стартом.
> c) V8 намеренно замедляет повторные вызовы одной и той же функции для защиты от бесконечных циклов.
> d) Счётчик при повторном вызове хранится как строка вместо числа.
>> [!success]- Answer
>> a) При превышении диапазона Smi число переходит к int32, затем к double, а double хранится в «коробочном» (boxed) представлении в куче — каждое приращение требует новой аллокации.
>> Explanation:
>> Именно переход к боксированным числам объясняет резкое замедление, особенно когда счётчик стартует уже с большего значения.
>> b), c), d) не соответствуют механике, описанной в докладе.

> [!question] Что оказалось истинным триггером перехода счётчика к «коробочному» представлению чисел в разобранном примере — раньше, чем можно было бы ожидать по чистому переполнению диапазона?
> a) Использование оператора `++` вместо `+= 1`.
> b) Сложение счётчика со строкой в конце цикла, которое «протекало» в анализ типов и заставляло V8 заранее выбрать коробочное представление.
> c) Объявление счётчика через `var` вместо `let`.
> d) Использование строгого режима (`'use strict'`).
>> [!success]- Answer
>> b) Сложение счётчика со строкой в конце цикла, которое «протекало» в анализ типов и заставляло V8 заранее выбрать коробочное представление.
>> Explanation:
>> Скрытие этого сложения от анализа компилятора устраняло эффект — значит, именно оно было первопричиной, а не сам факт переполнения диапазона.
>> a), c), d) не упоминаются в докладе как причина.

> [!question] Чем принципиально различаются «быстрые» (fast properties) и «медленные» (dictionary mode) представления объектов в V8?
> a) Быстрые свойства хранятся как поля с фиксированным смещением, привязанные к скрытому классу; медленные — как хеш-таблица (словарь).
> b) Быстрые свойства доступны только для чтения, а медленные — для чтения и записи.
> c) Быстрые свойства существуют только у примитивов, а медленные — только у объектов.
> d) Различие носит чисто синтаксический характер и не влияет на производительность.
>> [!success]- Answer
>> a) Быстрые свойства хранятся как поля с фиксированным смещением, привязанные к скрытому классу; медленные — как хеш-таблица (словарь).
>> Explanation:
>> Переход между скрытыми классами при определённых модификациях объекта может перевести его в словарный (медленный) режим.
>> b), c), d) не соответствуют механике V8, описанной в докладе.

> [!question] Почему более новая версия V8 (5.3) оказалась медленнее старой версии (3.31) при повторном вызове одной и той же тестовой функции с геттером?
> a) В версии 3.31 существовал код, насильно приводивший объекты к быстрому представлению; его убрали в новых версиях, потому что такая принудительная конверсия сама по себе стоит производительности.
> b) В версии 5.3 геттеры на прототипе были полностью отключены из соображений безопасности.
> c) В версии 5.3 убрали инлайн-кэширование для всех объектов без исключения.
> d) Версия 5.3 требует явного вызова `Object.freeze` для оптимизации геттеров.
>> [!success]- Answer
>> a) В версии 3.31 существовал код, насильно приводивший объекты к быстрому представлению; его убрали в новых версиях, потому что такая принудительная конверсия сама по себе стоит производительности.
>> Explanation:
>> Из-за удаления этого кода объект в новой версии на повторном вызове оставался в медленном (словарном) представлении.
>> b), c), d) не упоминаются в докладе и не соответствуют реальной архитектуре V8.

> [!question] В бенчмарке «функция vs метод на прототипе» метод неожиданно оказался быстрее функции. Какова была настоящая причина, согласно докладу?
> a) Методы на прототипе в принципе быстрее функций из-за более компактного машинного кода.
> b) Функция-версия в этом тесте так и не «прогрелась» до оптимизированного состояния, тогда как метод получил нужный прогрев за счёт паттерна вызовов в тесте.
> c) Функция создавала новое замыкание при каждом вызове, а метод — нет.
> d) Метод использовал SIMD-инструкции для подсчёта хэша строки.
>> [!success]- Answer
>> b) Функция-версия в этом тесте так и не «прогрелась» до оптимизированного состояния, тогда как метод получил нужный прогрев за счёт паттерна вызовов в тесте.
>> Explanation:
>> Добавление подсказки компилятору о «горячести» функции устраняло разницу — значит, дело было в прогреве, а не в самой природе функции/метода.
>> a), c), d) не соответствуют разбору доклада.

> [!question] Разработчики транслятора Dart → JavaScript обнаружили, что добавление защитной проверки в for-in цикл замедляет код на 18%. Что оказалось истинной причиной замедления?
> a) Сама проверка требовала дополнительного обхода прототипной цепочки на каждой итерации.
> b) V8-баг, связанный с обрезанием значения до int32: формально никогда не исполняемая ветка (обработка исключения) «протекала» в анализ типов и заставляла компилятор выбрать менее эффективный путь для всего цикла.
> c) Проверка вызывала дополнительную сборку мусора на каждой итерации цикла.
> d) Проверка была реализована через try/catch, что само по себе запрещает оптимизацию всей функции.
>> [!success]- Answer
>> b) V8-баг, связанный с обрезанием значения до int32: формально никогда не исполняемая ветка (обработка исключения) «протекала» в анализ типов и заставляла компилятор выбрать менее эффективный путь для всего цикла.
>> Explanation:
>> Явное приведение значения к int32 в одном месте устраняло эффект — значит, дело было именно в этом баге движка, а не в самой проверке.
>> a), c), d) не соответствуют объяснению из доклада.

> [!question] Какой главный практический вывод делает автор доклада из истории с for-in циклом и мнимым багом транслятора?
> a) Нужно всегда избегать for-in циклов в производительном коде.
> b) Необъяснимо странное поведение производительности, скорее всего, является багом или особенностью движка, и о нём стоит сообщать напрямую разработчикам виртуальной машины.
> c) Нужно всегда добавлять защитные проверки во все циклы, независимо от их стоимости.
> d) Транслируемый код всегда медленнее, чем написанный вручную, и с этим ничего нельзя сделать.
>> [!success]- Answer
>> b) Необъяснимо странное поведение производительности, скорее всего, является багом или особенностью движка, и о нём стоит сообщать напрямую разработчикам виртуальной машины.
>> Explanation:
>> Это центральная мысль доклада о взаимодействии с разработчиками VM вместо молчаливого обхода странностей.
>> a), c), d) — избыточные обобщения, не соответствующие выводу доклада.

> [!question] Как в докладе (в ответах на вопросы) оценивается производительность стрелочных функций в V8 на момент выступления?
> a) Они оптимизируются наравне с обычными функциями и не несут скрытого штрафа по производительности.
> b) Они категорически не поддерживают JIT-оптимизацию и всегда исполняются в интерпретируемом режиме.
> c) Они быстрее обычных функций примерно в 2 раза благодаря отсутствию собственного `this`.
> d) Их производительность зависит исключительно от версии Node.js, а не от V8.
>> [!success]- Answer
>> a) Они оптимизируются наравне с обычными функциями и не несут скрытого штрафа по производительности.
>> Explanation:
>> Это прямой ответ автора на вопрос из зала.
>> b), c), d) не соответствуют ответу, данному в докладе.

> [!question] Как в докладе оценивается адекватность Benchmark.js/jsPerf по сравнению, например, с JMH (Java Microbenchmark Harness)?
> a) Benchmark.js полностью бесполезен и не должен использоваться вообще.
> b) Benchmark.js достаточно адекватен для межбраузерных сравнений, но не претендует на строго научную методологию, в отличие от JMH, заточенного под одну конкретную JVM/HotSpot.
> c) Benchmark.js и JMH идентичны по методологии и дают одинаковые гарантии точности.
> d) Benchmark.js эффективен только для сравнения производительности внутри одной версии одного браузера.
>> [!success]- Answer
>> b) Benchmark.js достаточно адекватен для межбраузерных сравнений, но не претендует на строго научную методологию, в отличие от JMH, заточенного под одну конкретную JVM/HotSpot.
>> Explanation:
>> Автор признаёт инструмент рабочим и приемлемо справляющимся с прогревом и усреднением, но не «научным» в строгом смысле.
>> a), c), d) искажают позицию, высказанную в докладе.
