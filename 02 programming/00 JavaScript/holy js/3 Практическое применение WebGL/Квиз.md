#flashquiz/proga/js/holyjs/3
> [!question] От какой технологии произошёл WebGL, и что было добавлено в неё специально для использования в браузере?
> a) От OpenGL ES (урезанной версии OpenGL для embedded-систем), с добавлением дополнительных математических функций для 3D-сцен.
> b) От Direct3D, с добавлением поддержки DOM-событий.
> c) От Flash Stage3D, с добавлением поддержки canvas.
> d) От Vulkan, с удалением поддержки шейдеров.
>> [!success]- Answer
>> a) От OpenGL ES (урезанной версии OpenGL для embedded-систем), с добавлением дополнительных математических функций для 3D-сцен.
>> Explanation:
>> Это прямо указано в докладе как происхождение WebGL и объяснение его "урезанности" по сравнению с полным OpenGL.
>> b), c), d) не соответствуют содержанию доклада.

> [!question] Почему нельзя напрямую "потрогать" отдельные объекты 3D-сцены на canvas так же, как обычные DOM-элементы?
> a) Потому что canvas по стандарту HTML5 хранит содержимое как набор пикселей, а не как дерево элементов, хотя WebGL всё же позволяет программно определить объект под курсором.
> b) Потому что WebGL полностью запрещает взаимодействие с объектами сцены после отрисовки.
> c) Потому что объекты сцены существуют только в памяти видеокарты и недоступны из JavaScript в принципе.
> d) Потому что canvas — это SVG-элемент, а не растровый холст.
>> [!success]- Answer
>> a) Потому что canvas по стандарту HTML5 хранит содержимое как набор пикселей, а не как дерево элементов, хотя WebGL всё же позволяет программно определить объект под курсором.
>> Explanation:
>> Автор явно уточняет, что это "не привычно", но не является технической невозможностью.
>> b), c), d) не соответствуют объяснению из доклада.

> [!question] В чём заключается ключевое технологическое преимущество WebGL, объясняющее его высокую производительность по сравнению с технологиями вроде Flash?
> a) Шейдеры WebGL исполняются на GPU (видеокарте), а не на CPU.
> b) WebGL не использует растеризацию и работает напрямую с векторной графикой.
> c) WebGL не требует наличия canvas и рисует прямо в DOM.
> d) WebGL кэширует все кадры сцены заранее при загрузке страницы.
>> [!success]- Answer
>> a) Шейдеры WebGL исполняются на GPU (видеокарте), а не на CPU.
>> Explanation:
>> Именно это объясняет, почему браузерные игры на WebGL могут приближаться по производительности к десктопным.
>> b), c), d) не соответствуют механике WebGL, описанной в докладе.

> [!question] Какова роль vertex shader (вершинного шейдера) в конвейере WebGL?
> a) Он вызывается один раз для каждой вершины объекта и вычисляет итоговые координаты геометрии на основе входного массива точек.
> b) Он вызывается один раз для каждого пикселя и определяет его итоговый цвет.
> c) Он выполняется только один раз при загрузке страницы для всей сцены целиком.
> d) Он отвечает исключительно за загрузку текстур в память видеокарты.
>> [!success]- Answer
>> a) Он вызывается один раз для каждой вершины объекта и вычисляет итоговые координаты геометрии на основе входного массива точек.
>> Explanation:
>> Это базовое разделение обязанностей между вершинным и фрагментным шейдером, объяснённое в докладе.
>> b) Это описание fragment shader, а не vertex shader.
>> c), d) не соответствуют содержанию доклада.

> [!question] Как называется процесс перехода от векторной геометрии, вычисленной вершинным шейдером, к закрашенным пикселям на экране?
> a) Растеризация (rasterization).
> b) Триангуляция (triangulation).
> c) Тесселяция (tessellation).
> d) Компиляция (compilation).
>> [!success]- Answer
>> a) Растеризация (rasterization).
>> Explanation:
>> Именно растеризация объясняет, почему соседние пиксели могут закрашиваться градиентом между цветами ближайших вершин.
>> b), c), d) — другие термины компьютерной графики, не упомянутые в этом контексте доклада.

> [!question] Чем отличаются классификаторы переменных varying и uniform в шейдерах GLSL?
> a) varying — значение, вычисленное в вершинном шейдере и передаваемое (с интерполяцией) во фрагментный шейдер; uniform — значение, одинаковое для всех вершин/пикселей за один вызов рендера.
> b) varying — значение, которое нельзя изменять внутри шейдера; uniform — значение, которое можно изменять только во фрагментном шейдере.
> c) varying используется только для целых чисел, uniform — только для векторов.
> d) Оба классификатора обозначают одно и то же и являются синонимами в GLSL.
>> [!success]- Answer
>> a) varying — значение, вычисленное в вершинном шейдере и передаваемое (с интерполяцией) во фрагментный шейдер; uniform — значение, одинаковое для всех вершин/пикселей за один вызов рендера.
>> Explanation:
>> Пример из доклада — передача UV-координат через varying и передача текстуры через uniform sampler2D.
>> b), c), d) не соответствуют семантике GLSL, описанной в докладе.

> [!question] Зачем в примере кода тег `<script type="x-shader/x-vertex">` используется для хранения исходного кода шейдера?
> a) Это не настоящий MIME-тип, а способ семантически пометить содержимое как "здесь лежит шейдер", чтобы браузер не пытался исполнить его как обычный JavaScript.
> b) Это официальный MIME-тип, зарегистрированный в спецификации HTML5 специально для GLSL-кода.
> c) Без этого атрибута шейдер физически не сможет быть скомпилирован WebGL-движком.
> d) Атрибут указывает браузеру, что скрипт нужно загрузить асинхронно.
>> [!success]- Answer
>> a) Это не настоящий MIME-тип, а способ семантически пометить содержимое как "здесь лежит шейдер", чтобы браузер не пытался исполнить его как обычный JavaScript.
>> Explanation:
>> Автор прямо отвечает на этот вопрос из зала именно так — это соглашение, а не техническое требование WebGL API.
>> b), c), d) не соответствуют ответу, данному в докладе.

> [!question] Что произойдёт, если во фрагментном шейдере, реализующем grayscale-фильтр, значение суммы (R + G + B) поделить не на 3, а на другое число?
> a) Получится некорректный (не соответствующий реальной яркости) оттенок серого, поскольку принцип строится именно на среднем арифметическом трёх каналов.
> b) Шейдер перестанет компилироваться и выдаст ошибку.
> c) Изображение вернётся к исходному цветному виду без каких-либо изменений.
> d) Пиксель станет полностью прозрачным.
>> [!success]- Answer
>> a) Получится некорректный (не соответствующий реальной яркости) оттенок серого, поскольку принцип строится именно на среднем арифметическом трёх каналов.
>> Explanation:
>> Это прямо показано в демонстрации отладки шейдера в докладе — изменение делителя меняет итоговый оттенок серого.
>> b), c), d) не соответствуют показанному в докладе поведению.

> [!question] В чём общая идея "мультяшного" (toon) шейдера, упомянутого в докладе для упрощения фотографии до картинки с меньшим числом цветов?
> a) Перевод цвета в другое цветовое пространство (например, HSV) и присвоение пикселю ближайшего цвета из ограниченной палитры на основе пороговых значений (thresholds).
> b) Полное удаление текстуры и замена её однотонной заливкой.
> c) Увеличение количества цветов в изображении в несколько раз для большей детализации.
> d) Применение размытия по Гауссу без изменения цветовой палитры.
>> [!success]- Answer
>> a) Перевод цвета в другое цветовое пространство (например, HSV) и присвоение пикселю ближайшего цвета из ограниченной палитры на основе пороговых значений (thresholds).
>> Explanation:
>> Это базовый принцип, описанный в докладе, хотя реальный алгоритм toon-шейдера значительно сложнее.
>> b), c), d) не соответствуют объяснению из доклада.

> [!question] Чем пост-обработка (post-processing) в three.js принципиально отличается от применения обычного материала к объекту сцены?
> a) Эффект добавляется поверх уже полностью отрисованной сцены целиком через EffectComposer, а не встраивается в материал конкретного объекта.
> b) Пост-обработка применяется только к вершинным шейдерам, а материалы — только к фрагментным.
> c) Пост-обработка полностью заменяет необходимость в камере и источниках света.
> d) Пост-обработка работает только с системами частиц, а не с обычной геометрией.
>> [!success]- Answer
>> a) Эффект добавляется поверх уже полностью отрисованной сцены целиком через EffectComposer, а не встраивается в материал конкретного объекта.
>> Explanation:
>> Именно поэтому в цикле рендера вызывается composer.render(), а не renderer.render(scene, camera).
>> b), c), d) не соответствуют механике пост-обработки, описанной в докладе.

> [!question] Зачем в WebGL-сценах применяют систему частиц (particle system) вместо отрисовки каждого мелкого объекта (травинки, огня, волос) отдельным полигоном?
> a) Отрисовка каждого объекта по отдельности была бы непрактично тяжёлой по производительности; система частиц задаёт единый набор точек с общими атрибутами и массовым управлением поведением.
> b) Полигональная геометрия технически не поддерживает мелкие объекты меньше одного пикселя.
> c) Система частиц полностью исключает необходимость использования фрагментного шейдера.
> d) Система частиц нужна только для объектов, которые не должны двигаться.
>> [!success]- Answer
>> a) Отрисовка каждого объекта по отдельности была бы непрактично тяжёлой по производительности; система частиц задаёт единый набор точек с общими атрибутами и массовым управлением поведением.
>> Explanation:
>> Пример из доклада — сравнение "полигонального" огня и огня на системе частиц в игре Quake, где второй выглядит заметно живее и эффективнее по ресурсам.
>> b), c), d) не соответствуют объяснению доклада.

> [!question] Почему requestAnimationFrame предпочтительнее setInterval для организации цикла рендера интерактивной WebGL-сцены?
> a) requestAnimationFrame позволяет браузеру самому выбирать оптимальный момент перерисовки кадра, синхронизируясь с частотой обновления экрана.
> b) setInterval физически не поддерживается внутри WebGL-контекста.
> c) requestAnimationFrame автоматически останавливает рендер при уходе пользователя со страницы, а setInterval — нет.
> d) Разница чисто синтаксическая и не влияет на производительность.
>> [!success]- Answer
>> a) requestAnimationFrame позволяет браузеру самому выбирать оптимальный момент перерисовки кадра, синхронизируясь с частотой обновления экрана.
>> Explanation:
>> Это прямое объяснение из доклада, почему для интерактивных сцен выбирают именно requestAnimationFrame.
>> b), c), d) не соответствуют объяснению доклада (хотя requestAnimationFrame действительно приостанавливается в неактивных вкладках, это не было названо основной причиной выбора).

> [!question] Какой практический совет по оптимизации производительности даётся в докладе для случая, когда WebGL-сцена сейчас статична (нет вращения или анимации)?
> a) Не вызывать requestAnimationFrame повторно, фактически приостанавливая цикл рендера до следующего пользовательского взаимодействия.
> b) Полностью удалять сцену из памяти и создавать её заново при каждом взаимодействии.
> c) Принудительно понижать частоту кадров до 1 FPS с помощью setInterval.
> d) Заменять WebGL-рендер статичным PNG-скриншотом сцены.
>> [!success]- Answer
>> a) Не вызывать requestAnimationFrame повторно, фактически приостанавливая цикл рендера до следующего пользовательского взаимодействия.
>> Explanation:
>> Это ответ автора на вопрос об узких местах производительности — простой способ снизить нагрузку без изменения архитектуры сцены.
>> b), c), d) не упоминаются в докладе как рекомендация.

> [!question] Какое ограничение старых версий WebGL, связанное с текстурами, называется в докладе как источник проблем производительности в сложных сценах?
> a) Официально поддерживается ограниченное число одновременно используемых текстур — порядка 8.
> b) Текстуры принципиально не поддерживаются в WebGL и эмулируются через canvas 2D.
> c) Каждая текстура обязана быть строго квадратной и не более 16×16 пикселей.
> d) WebGL поддерживает только чёрно-белые текстуры без альфа-канала.
>> [!success]- Answer
>> a) Официально поддерживается ограниченное число одновременно используемых текстур — порядка 8.
>> Explanation:
>> Это обсуждается в Q&A доклада как одно из узких мест при сложных сценах с большим числом объектов.
>> b), c), d) не соответствуют реальным ограничениям WebGL, обсуждаемым в докладе.

> [!question] Почему WebGL называют технологией растровой, а не векторной графики, и как это оценивается в докладе?
> a) WebGL в итоге выводит изображение как набор пикселей, а не масштабируемые фигуры; в докладе это не называется недостатком самим по себе, а особенностью области применения технологии.
> b) Потому что WebGL официально не поддерживает работу с текстурами и изображениями вообще.
> c) Потому что WebGL используется только для отрисовки текста и шрифтов.
> d) В докладе растровая природа WebGL прямо называется главным техническим недостатком без оговорок.
>> [!success]- Answer
>> a) WebGL в итоге выводит изображение как набор пикселей, а не масштабируемые фигуры; в докладе это не называется недостатком самим по себе, а особенностью области применения технологии.
>> Explanation:
>> Автор явно уточняет: "это не минус — это способ применения", то есть особенность, которую нужно учитывать, а не проблема сама по себе.
>> b), c), d) не соответствуют содержанию доклада.

> [!question] Как в докладе рекомендуется синхронизировать анимацию WebGL-сцены на canvas с CSS-анимацией других элементов страницы при написании логики на чистом JavaScript?
> a) Автоматической синхронизации нет — разработчик вручную, в нужный момент внутри логики сцены, добавляет/убирает CSS-классы соответствующим DOM-элементам за пределами canvas.
> b) CSS-анимации автоматически синхронизируются с рендер-циклом WebGL через общий requestAnimationFrame браузера без дополнительного кода.
> c) Такая синхронизация технически невозможна ни при каких условиях.
> d) Нужно полностью отказаться от CSS-анимаций и реализовывать все эффекты внутри WebGL-шейдеров.
>> [!success]- Answer
>> a) Автоматической синхронизации нет — разработчик вручную, в нужный момент внутри логики сцены, добавляет/убирает CSS-классы соответствующим DOM-элементам за пределами canvas.
>> Explanation:
>> Это прямой ответ автора на вопрос из зала о согласовании WebGL-сцены и CSS-анимации.
>> b), c), d) не соответствуют ответу, данному в докладе.

> [!question] Почему при выборе библиотеки для работы с WebGL автор доклада предпочла three.js альтернативам вроде игровых движков?
> a) Многие альтернативы прямо позиционировались как движки для создания игр, тогда как в задачах компании требовался более детальный контроль над конкретными объектами сцены, а не полноценный игровой движок.
> b) three.js — единственная библиотека, поддерживающая WebGL 2 на момент доклада.
> c) Альтернативные библиотеки не поддерживали работу с шейдерами вообще.
> d) three.js была единственной библиотекой с открытым исходным кодом на тот момент.
>> [!success]- Answer
>> a) Многие альтернативы прямо позиционировались как движки для создания игр, тогда как в задачах компании требовался более детальный контроль над конкретными объектами сцены, а не полноценный игровой движок.
>> Explanation:
>> Это прямой ответ автора на вопрос о выборе между three.js и альтернативами вроде Babylon.js.
>> b), c), d) не соответствуют ответу, данному в докладе.

> [!question] Что автор говорит о применимости пост-обработки (в духе EffectComposer) к рендерингу обычной HTML/CSS-страницы, а не WebGL-канваса?
> a) На момент доклада такой возможности нет — пост-обработка ограничена содержимым самого WebGL-канваса.
> b) Пост-обработку HTML-страниц можно реализовать напрямую через EffectComposer без дополнительных библиотек.
> c) Для этого нужно использовать WebGL 2, который полностью решает эту задачу.
> d) Такая возможность существует, но требует отключения JavaScript на странице.
>> [!success]- Answer
>> a) На момент доклада такой возможности нет — пост-обработка ограничена содержимым самого WebGL-канваса.
>> Explanation:
>> Автор отвечает, что "натянуть" постобработку на обычную HTML/CSS-страницу пока нельзя, хотя это выглядело бы эффектно.
>> b), c), d) не соответствуют содержанию ответа в докладе.

> [!question] Какой практический приём для надёжной работы с большими 3D-моделями (например, данными с 3D-сканера) упоминается в ответах на вопросы?
> a) Разбивать большую модель на части вместо загрузки одним целым бинарным файлом.
> b) Полностью отказываться от текстур в пользу однотонной заливки.
> c) Загружать модель исключительно в формате SVG.
> d) Использовать только системы частиц вместо полигональной геометрии для больших моделей.
>> [!success]- Answer
>> a) Разбивать большую модель на части вместо загрузки одним целым бинарным файлом.
>> Explanation:
>> Это конкретный практический приём, упомянутый автором применительно к большим моделям, получаемым с 3D-сканера в её компании.
>> b), c), d) не упоминаются в докладе.
