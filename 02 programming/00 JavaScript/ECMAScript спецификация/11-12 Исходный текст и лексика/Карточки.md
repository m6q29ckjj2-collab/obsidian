---
tags:
  - javascript
  - ecmascript
  - flashcards
clause: 11-12
---

# Главы 11–12 · Исходный текст и лексика — карточки

> [!info] Формат
> Плагин **Spaced Repetition**. Разбор → [[Отчёт]] · квиз → [[Квиз]] · [11](https://262.ecma-international.org/#sec-ecmascript-language-source-code) / [12](https://262.ecma-international.org/#sec-ecmascript-language-lexical-grammar)

#flashcards/ecma262/лексика

---

## Исходный текст

Из чего состоит исходный текст ECMAScript::Из ==кодовых точек Unicode==, а не байтов; кодировка интерпретируется до начала лексического разбора

Какой тип кода строгий по умолчанию::==Module==. Script требует директиву, функция наследует режим места объявления

Что произойдёт при `x = 1` без объявления в обычном скрипте::Создастся ==глобальная переменная== — присваивание необъявленному имени работает в sloppy mode

Что произойдёт при том же присваивании внутри модуля::==ReferenceError==. Модуль строгий по умолчанию, и директива для этого не нужна

---

## Формат и токены

Чем терминатор строки отличается от обычного пробела::Терминатор строки участвует в ==автоматической расстановке точек с запятой==, обычный пробел — нет

Что такое hashbang-комментарий::Синтаксис `#!/usr/bin/env node` в первой строке — отдельная продукция грамматики, а не обычный комментарий

Сколько категорий токенов определяет спецификация::==Пять==: имя, зарезервированное слово, знак пунктуации, литерал шаблона, обычный литерал

Является ли `yield` зарезервированным словом всегда
?
==Нет==. Вне генератора и вне строгого режима он работает как обычный идентификатор. Внутри генератора зарезервирован.

Является ли `interface` зарезервированным словом в обычном скрипте::Только ==в строгом режиме==. В sloppy mode это допустимый идентификатор

---

## Литералы

Одна ли грамматика используется для числовых литералов в коде и для разбора числа из строки::==Нет==, это разные грамматики. Разделители-подчёркивания работают только в литералах кода

Что вернёт `Number('1_000')` и что вернёт `1_000` в коде::`NaN` и `1000` — разные грамматики дают разный результат на одном тексте

Зачем появилась форма `\u{...}` в дополнение к `\uXXXX`::Старая форма кодирует ровно ==одну 16-битную единицу== и не может задать символ вне базовой плоскости одним блоком

Какие два параллельных значения есть у шаблонного литерала::==TV== (обработанное значение) и ==TRV== (сырой текст без интерпретации escape-последовательностей)

На какой паре построен `String.raw`::На ==TV и TRV==: свойство `raw` тегового шаблона даёт сырой текст

---

## Автоматическая расстановка точек с запятой

Сколько правил ASI описывает спецификация::==Три==: неподходящий токен после терминатора строки, конец программы при незавершённом разборе, явный запрет переноса в отдельных продукциях

Что выведет код
?
```js
function f() {
  return
    { value: 1 };
}
f();
```
`undefined`. После `return` действует явный запрет переноса строки, поэтому точка вставляется сразу после ключевого слова.

Когда ASI фактически не срабатывает и код склеивается с предыдущей строкой::Когда следующая строка начинается с `(`, `[`, обратной кавычки, `+`, `-` или `/` — парсер способен продолжить предыдущее выражение

Какая практика защищает от склейки строк при отсутствии точек с запятой::Начинать такую строку с ==ведущей точки с запятой==
