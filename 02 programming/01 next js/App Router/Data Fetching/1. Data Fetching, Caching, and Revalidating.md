---
создал заметку: 2024-05-24
tags:
  - next_js
  - app_router
---
Data fetching is a core part of any application. This page goes through how you can fetch, cache, and revalidate data in React and Next.js.

There are four ways you can fetch data:
1. [On the server, with `fetch`](https://nextjs.org/docs/app/building-your-application/data-fetching/fetching-caching-and-revalidating#fetching-data-on-the-server-with-fetch)
2. [On the server, with third-party libraries](https://nextjs.org/docs/app/building-your-application/data-fetching/fetching-caching-and-revalidating#fetching-data-on-the-server-with-third-party-libraries)
3. [On the client, via a Route Handler](https://nextjs.org/docs/app/building-your-application/data-fetching/fetching-caching-and-revalidating#fetching-data-on-the-client-with-route-handlers)
4. [On the client, with third-party libraries](https://nextjs.org/docs/app/building-your-application/data-fetching/fetching-caching-and-revalidating#fetching-data-on-the-client-with-route-handlers).

## [Fetching Data on the Server with `fetch`](https://nextjs.org/docs/app/building-your-application/data-fetching/fetching-caching-and-revalidating#fetching-data-on-the-server-with-fetch)
Next.js расширяет собственный веб-API выборки, позволяя вам настраивать поведение кэширования и повторной проверки для каждого запроса выборки на сервере. React расширяет возможности выборки, чтобы автоматически запоминать запросы на выборку при рендеринге дерева компонентов React.

Вы можете использовать выборку с async/await в компонентах сервера, в обработчиках маршрутов и в действиях сервера.

Например:
![[Pasted image 20240524213825.png]]
> **Good to know**:
> -  Next.js предоставляет полезные функции, которые могут вам понадобиться при получении данных в компонентах сервера, таких как файлы cookie и заголовки. Это приведет к динамическому отображению маршрута, поскольку они полагаются на информацию о времени запроса.
> - В обработчиках маршрутов запросы на выборку не запоминаются, поскольку обработчики маршрутов не являются частью дерева компонентов React.
> - In [Server Actions](https://nextjs.org/docs/app/building-your-application/data-fetching/server-actions-and-mutations), `fetch` requests are not cached (defaults `cache: no-store`).
> - To use `async`/`await` in a Server Component with TypeScript, you'll need to use TypeScript `5.1.3` or higher and `@types/react` `18.2.8` or higher.

### [Caching Data](https://nextjs.org/docs/app/building-your-application/data-fetching/fetching-caching-and-revalidating#caching-data)
Кэширование сохраняет данные, поэтому их не нужно повторно получать из источника данных при каждом запросе.

По умолчанию Next.js автоматически кэширует возвращаемые значения выборки в кэше данных на сервере. Это означает, что данные можно получать во время сборки или во время запроса, кэшировать и повторно использовать при каждом запросе данных.
![[Pasted image 20240524214606.png]]
However, there are exceptions, `fetch` requests are not cached when:
 - Used inside a [Server Action](https://nextjs.org/docs/app/building-your-application/data-fetching/server-actions-and-mutations).
 - Used inside a [Route Handler](https://nextjs.org/docs/app/building-your-application/routing/route-handlers) that uses the `POST` method
 > **What is the Data Cache?**
 > Кэш данных — это постоянный HTTP-кеш. В зависимости от вашей платформы кеш может автоматически масштабироваться и использоваться в нескольких регионах.

### [Revalidating Data](https://nextjs.org/docs/app/building-your-application/data-fetching/fetching-caching-and-revalidating#revalidating-data)
Revalidation is the process of purging the Data Cache and re-fetching the latest data. This is useful when your data changes and you want to ensure you show the latest information.
Cached data can be revalidated in two ways:
- Повторная проверка по времени: автоматическая повторная проверка данных по прошествии определенного времени. Это полезно для данных, которые изменяются нечасто, а свежесть не так важна.
- Повторная проверка по требованию. Повторная проверка данных вручную на основе события (например, отправки формы). При повторной проверке по требованию можно использовать подход на основе тегов или путей для одновременной повторной проверки групп данных. Это полезно, если вы хотите, чтобы самые последние данные отображались как можно скорее (например, при обновлении контента из вашей headless CMS).
#### [Time-based Revalidation](https://nextjs.org/docs/app/building-your-application/data-fetching/fetching-caching-and-revalidating#time-based-revalidation)
To revalidate data at a timed interval, you can use the `next.revalidate` option of `fetch` to set the cache lifetime of a resource (in seconds).
![[Pasted image 20240524224139.png]]
Alternatively, to revalidate all `fetch` requests in a route segment, you can use the [Segment Config Options](https://nextjs.org/docs/app/api-reference/file-conventions/route-segment-config).
![[Pasted image 20240524224159.png]]
Если у вас есть несколько запросов на выборку в статически отображаемом маршруте, и каждый из них имеет разную частоту повторной проверки. Для всех запросов будет использоваться наименьшее время. Для динамически отображаемых маршрутов каждый запрос на выборку будет проверяться независимо.
Learn more about [time-based revalidation](https://nextjs.org/docs/app/building-your-application/caching#time-based-revalidation).
#### [On-demand Revalidation](https://nextjs.org/docs/app/building-your-application/data-fetching/fetching-caching-and-revalidating#on-demand-revalidation)
Data can be revalidated on-demand by path ([`revalidatePath`](https://nextjs.org/docs/app/api-reference/functions/revalidatePath)) or by cache tag ([`revalidateTag`](https://nextjs.org/docs/app/api-reference/functions/revalidateTag)) inside a [Server Action](https://nextjs.org/docs/app/building-your-application/data-fetching/server-actions-and-mutations) or [Route Handler](https://nextjs.org/docs/app/building-your-application/routing/route-handlers).
Next.js has a cache tagging system for invalidating `fetch` requests across routes.
1. When using `fetch`, you have the option to tag cache entries with one or more tags.
2. Then, you can call `revalidateTag` to revalidate all entries associated with that tag
For example, the following `fetch` request adds the cache tag `collection`:
![[Pasted image 20240524224411.png]]
You can then revalidate this `fetch` call tagged with `collection` by calling `revalidateTag` in a Server Action:
![[Pasted image 20240524224436.png]]
Learn more about [on-demand revalidation](https://nextjs.org/docs/app/building-your-application/caching#on-demand-revalidation).
#### [Error handling and revalidation](https://nextjs.org/docs/app/building-your-application/data-fetching/fetching-caching-and-revalidating#error-handling-and-revalidation)
Если при попытке повторной проверки данных возникает ошибка, последние успешно сгенерированные данные будут продолжать обслуживаться из кэша. При следующем запросе Next.js повторит попытку повторной проверки данных.
### [Opting out of Data Caching](https://nextjs.org/docs/app/building-your-application/data-fetching/fetching-caching-and-revalidating#opting-out-of-data-caching)
`fetch` requests are **not** cached if:
- The `cache: 'no-store'` is added to `fetch` requests
- The `revalidate: 0` option is added to individual `fetch` requests.
- The `fetch` request is inside a Router Handler that uses the `POST` method.
- The `fetch` request comes after the usage of `headers` or `cookies`.
- The `const dynamic = 'force-dynamic'` route segment option is used.
- The `fetchCache` route segment option is configured to skip cache by default.
- The `fetch` request uses `Authorization` or `Cookie` headers and there's an uncached request above it in the component tree.
#### [Individual `fetch` Requests](https://nextjs.org/docs/app/building-your-application/data-fetching/fetching-caching-and-revalidating#individual-fetch-requests)
Чтобы отказаться от кэширования для отдельных запросов на выборку, вы можете установить для параметра кэширования в fetch значение «без хранения». Это будет получать данные динамически, при каждом запросе.
![[Pasted image 20240524224722.png]]
View all the available `cache` options in the [`fetch` API reference](https://nextjs.org/docs/app/api-reference/functions/fetch).
#### [Multiple `fetch` Requests](https://nextjs.org/docs/app/building-your-application/data-fetching/fetching-caching-and-revalidating#multiple-fetch-requests)
Если у вас есть несколько запросов на выборку в сегменте маршрута (например, макете или странице), вы можете настроить поведение кэширования всех запросов данных в сегменте, используя параметры конфигурации сегмента.
Однако мы рекомендуем настраивать поведение кэширования каждого запроса на выборку индивидуально. Это дает вам более детальный контроль над поведением кэширования.

## [Fetching data on the Server with third-party libraries](https://nextjs.org/docs/app/building-your-application/data-fetching/fetching-caching-and-revalidating#fetching-data-on-the-server-with-third-party-libraries)
В тех случаях, когда вы используете стороннюю библиотеку, которая не поддерживает или не предоставляет выборку (например, базу данных, клиент CMS или ORM), вы можете настроить поведение кэширования и повторной проверки этих запросов с помощью конфигурации сегмента маршрута. Option и функция кэширования React.
Кэшируются ли данные или нет, будет зависеть от того, отображается ли сегмент маршрута статически или динамически. Если сегмент статический (по умолчанию), выходные данные запроса будут кэшироваться и повторно проверяться как часть сегмента маршрута. Если сегмент является динамическим, выходные данные запроса не будут кэшироваться и будут извлекаться повторно при каждом запросе при визуализации сегмента.
You can also use the experimental [`unstable_cache` API](https://nextjs.org/docs/app/api-reference/functions/unstable_cache).
### [Example](https://nextjs.org/docs/app/building-your-application/data-fetching/fetching-caching-and-revalidating#example)
In the example below:
- The React `cache` function is used to [memoize](https://nextjs.org/docs/app/building-your-application/caching#request-memoization) data requests.
- The `revalidate` option is set to `3600` in the Layout and Page segments, meaning the data will be cached and revalidated at most every hour.
![[Pasted image 20240524225037.png]]
Although the `getItem` function is called twice, only one query will be made to the database.
![[Pasted image 20240524231131.png]]
![[Pasted image 20240524231205.png]]
## [Fetching Data on the Client with Route Handlers](https://nextjs.org/docs/app/building-your-application/data-fetching/fetching-caching-and-revalidating#fetching-data-on-the-client-with-route-handlers)
Если вам нужно получить данные в клиентском компоненте, вы можете вызвать обработчик маршрута из клиента. Обработчики маршрутов выполняются на сервере и возвращают данные клиенту. Это полезно, если вы не хотите предоставлять клиенту конфиденциальную информацию, например токены API.
See the [Route Handler](https://nextjs.org/docs/app/building-your-application/routing/route-handlers) documentation for examples.
> **Server Components and Route Handlers**
> Поскольку серверные компоненты визуализируются на сервере, вам не нужно вызывать обработчик маршрута из серверного компонента для получения данных. Вместо этого вы можете получить данные непосредственно внутри серверного компонента.

## [Fetching Data on the Client with third-party libraries](https://nextjs.org/docs/app/building-your-application/data-fetching/fetching-caching-and-revalidating#fetching-data-on-the-client-with-third-party-libraries)
You can also fetch data on the client using a third-party library such as [SWR](https://swr.vercel.app/) or [TanStack Query](https://tanstack.com/query/latest). These libraries provide their own APIs for memoizing requests, caching, revalidating, and mutating data.
> **Future APIs**:
> `use` is a React function that **accepts and handles a promise** returned by a function. Wrapping `fetch` in `use` is currently **not** recommended in Client Components and may trigger multiple re-renders. Learn more about `use` in the [React docs](https://react.dev/reference/react/use).



