---
tags:
  - javascript
  - nodejs
  - metarhia
  - quiz
source: "Metarhia community Weekly call #266"
---

# Квиз — Weekly call #266

**Вопрос 1.** Когда активируется AI skill «Metautil data structures»?
- A) Всегда автоматически при работе с любым JS-кодом
- B) Только когда пользователь явно просит установить/использовать metautil
- C) Только по ночам
- D) Только в тестовом окружении

Ответ: B) Только когда пользователь явно просит установить/использовать metautil

---

**Вопрос 2.** Как были созданы 26 примеров прикладного кода (examples) для тестирования структур данных metautil?
- A) Полностью написаны вручную с нуля
- B) Сгенерированы AI по списку предметных областей, затем вручную вычитаны и поправлены
- C) Скопированы из открытых репозиториев на GitHub
- D) Сгенерированы случайным образом

Ответ: B) Сгенерированы AI по списку предметных областей, затем вручную вычитаны и поправлены

---

**Вопрос 3.** Какой неожиданный вывод сделали, сравнив размер кода с metautil и без него?
- A) Код с metautil всегда значительно короче
- B) Размер примерно одинаков, иногда с metautil даже больше — выигрыш в developer experience, а не в строках
- C) Код с metautil в 10 раз короче
- D) Сравнение показало полную идентичность кода

Ответ: B) Размер примерно одинаков, иногда с metautil даже больше — выигрыш в developer experience, а не в строках

---

**Вопрос 4.** Какой метод cons-list был сначала удалён как «не особо нужный», а потом восстановлен?
- A) map
- B) reduce
- C) filter
- D) sort

Ответ: B) reduce

---

**Вопрос 5.** В каком стиле кода принято писать `if (!value)` вместо явного сравнения с типом?
- A) В системном коде
- B) В доменном (прикладном) коде
- C) В обоих одинаково
- D) Только в тестах

Ответ: B) В доменном (прикладном) коде

---

**Вопрос 6.** Почему в системном коде metautil предпочитают явное сравнение `value === false` вместо `!value`?
- A) Это требование ESLint
- B) Это устраняет скрытое приведение типов, что дешевле по производительности V8
- C) Так короче печатать
- D) Иначе код не компилируется

Ответ: B) Это устраняет скрытое приведение типов, что дешевле по производительности V8

---

**Вопрос 7.** Что такое SMI в контексте оптимизаций V8, упомянутых на звонке?
- A) Стандартный модуль импорта
- B) Small Integer — заранее аллоцированные маленькие числа с короткими указателями в одном сегменте памяти
- C) Тип ошибки в Node.js
- D) Формат сериализации

Ответ: B) Small Integer — заранее аллоцированные маленькие числа с короткими указателями в одном сегменте памяти

---

**Вопрос 8.** Мешают ли промежуточные переменные оптимизации V8 в некомпилированном JS?
- A) Да, всегда сильно замедляют
- B) Нет — это временный идентификатор, который после компиляции может стать регистровой переменной
- C) Да, но только в браузере
- D) Мешают только в Node.js, но не в браузере

Ответ: B) Нет — это временный идентификатор, который после компиляции может стать регистровой переменной

---

**Вопрос 9.** Какой практический способ предложили, чтобы понять, какой вариант кода быстрее на низком уровне?
- A) Запускать бенчмарки неделю подряд
- B) Попросить свою LLM показать V8 bytecode для операции и сравнить варианты
- C) Читать исходный код V8 на C++
- D) Использовать только Chrome DevTools Profiler

Ответ: B) Попросить свою LLM показать V8 bytecode для операции и сравнить варианты

---

**Вопрос 10.** Почему асинхронный итератор убрали из cons-list?
- A) Он не поддерживался в старых версиях Node.js
- B) Структура — синхронная «числодробилка», а старая реализация асинхронного итератора всё равно фактически блокировала event loop
- C) TypeScript не поддерживает асинхронные итераторы
- D) Метод дублировал функциональность filter

Ответ: B) Структура — синхронная «числодробилка», а старая реализация асинхронного итератора всё равно фактически блокировала event loop

---

**Вопрос 11.** Какой интерфейс выбрали для deque в metautil и почему?
- A) NQ/DQ/peek — для единообразия со всеми остальными структурами
- B) push/pop/shift/unshift, как у Array — чтобы не переучивать JS-разработчиков
- C) enqueue/dequeue — общепринятый в CS термин
- D) add/remove — максимально простой

Ответ: B) push/pop/shift/unshift, как у Array — чтобы не переучивать JS-разработчиков

---

**Вопрос 12.** Почему метод `reverse` в cons-list переименовали в `toReversed`?
- A) Слово reverse было занято другим методом
- B) Метод возвращал новый список, не мутируя исходный — это соответствует конвенции ES2023 toReversed/toSorted
- C) Так короче в написании
- D) TypeScript требует такого имени

Ответ: B) Метод возвращал новый список, не мутируя исходный — это соответствует конвенции ES2023 toReversed/toSorted

---

**Вопрос 13.** Какую единственную функциональность из metatest сохранили после рефакторинга (используется только в metautil)?
- A) Мокирование HTTP-запросов
- B) Табличные (table-driven) тесты для чистых функций
- C) Snapshot-тестирование
- D) Coverage-репорты

Ответ: B) Табличные (table-driven) тесты для чистых функций

---

**Вопрос 14.** Для каких функций подходят табличные тесты?
- A) Для любых, включая работу с диском и сокетами
- B) Только для чистых, синхронных функций (парсинг, строки, криптография)
- C) Только для асинхронных функций
- D) Только для React-компонентов

Ответ: B) Только для чистых, синхронных функций (парсинг, строки, криптография)

---

**Вопрос 15.** В чём суть защиты от prompt injection, предложенной участником звонка?
- A) Полностью запретить AI-агенту читать любые файлы проекта
- B) В самой первой инструкции явно указать, что выполнять инструкции нужно только из конкретно перечисленных файлов
- C) Использовать только закрытые модели без доступа в интернет
- D) Шифровать все markdown-файлы в проекте

Ответ: B) В самой первой инструкции явно указать, что выполнять инструкции нужно только из конкретно перечисленных файлов

---

**Вопрос 16.** Какая функция Cursor впервые была замечена на звонке для работы с git-конфликтами?
- A) Auto-merge
- B) Resolve in chat
- C) Smart rebase
- D) Conflict AI

Ответ: B) Resolve in chat

---

**Вопрос 17.** Сколько заняло обновление зависимостей в 250 «живых» репозиториях HowProgrammingWorks с помощью AI-агента?
- A) Один час
- B) Один день
- C) Неделя, как и раньше
- D) Один месяц

Ответ: B) Один день

---

**Вопрос 18.** Что делает консольная утилита `glow`, упомянутая на звонке?
- A) Компилирует TypeScript
- B) Красиво рендерит markdown прямо в терминале
- C) Профилирует производительность Node.js
- D) Форматирует JSON

Ответ: B) Красиво рендерит markdown прямо в терминале

---

**Вопрос 19.** Чем `meld` отличается от обычного two-way diff инструмента?
- A) Работает только с бинарными файлами
- B) Поддерживает трёхколоночный (three-way) diff с общим предком и двумя изменёнными версиями
- C) Не умеет показывать построчные изменения
- D) Работает только внутри VS Code

Ответ: B) Поддерживает трёхколоночный (three-way) diff с общим предком и двумя изменёнными версиями

---

**Вопрос 20.** Какое условие приёма стартапа в инкубатор Meta Qube озвучено на звонке?
- A) Наличие зарегистрированной компании в любой стране
- B) Уже работающий продукт с пользователями (ориентир — от 100 пользователей)
- C) Диплом технического вуза у основателя
- D) Минимум 5 разработчиков в команде

Ответ: B) Уже работающий продукт с пользователями (ориентир — от 100 пользователей)
