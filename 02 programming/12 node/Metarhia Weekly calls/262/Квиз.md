---
tags:
  - javascript
  - nodejs
  - metarhia
  - quiz
source: "Metarhia community Weekly call #262"
---

# Квиз — Weekly call #262

**Вопрос 1.** Какую внешнюю зависимость заменила собственная реализация WebSocket в Metacom?
- A) `express`
- B) `ws`
- C) `socket.io`
- D) `uWebSockets.js`

Ответ: B) `ws`

---

**Вопрос 2.** Почему MetaSQL не считается частью ядра Metarhia?
- A) Она написана на другом языке
- B) Это необязательная надстройка с зависимостью на Postgres, а ядро не имеет runtime-зависимостей
- C) Она устарела и будет удалена
- D) Она платная

Ответ: B) Это необязательная надстройка с зависимостью на Postgres, а ядро не имеет runtime-зависимостей

---

**Вопрос 3.** Какая минимальная версия Node.js теперь поддерживается в Metacom после дропа 18/20/21?
- A) 16
- B) 20
- C) 22
- D) 24

Ответ: C) 22

---

**Вопрос 4.** Почему одиночные строковые константы выносят прямо в место использования (in place)?
- A) Так требует ESLint
- B) Вынос в константу не экономит память при однократном использовании и усложняет код
- C) TypeScript не поддерживает вынесенные константы
- D) Это ускоряет сборку

Ответ: B) Вынос в константу не экономит память при однократном использовании и усложняет код

---

**Вопрос 5.** Почему в WebSocket-модуле заменили Set/Map на Array/plain objects для кодов закрытия?
- A) Set и Map не поддерживаются в старых версиях Node.js
- B) Коллекция маленькая и почти не меняется — накладные расходы на хэш-функцию не окупаются
- C) Array быстрее сериализуется в JSON
- D) Это требование TypeScript

Ответ: B) Коллекция маленькая и почти не меняется — накладные расходы на хэш-функцию не окупаются

---

**Вопрос 6.** Сколько параллельных реализаций ping/pong-механизма нашли в кодовой базе до рефакторинга?
- A) Одну
- B) Две
- C) Три
- D) Пять

Ответ: C) Три

---

**Вопрос 7.** Куда перенесли Result-контейнер (ok/fail обёртку), ранее находившийся внутри Metacom?
- A) В отдельную новую библиотеку
- B) В metautil
- C) В Impress
- D) Оставили в Metacom

Ответ: B) В metautil

---

**Вопрос 8.** Почему поля head/tail в LinkedList сделаны не приватными?
- A) Приватные поля не поддерживаются в целевой версии Node.js
- B) LinkedList — низкоуровневый класс, который никогда не используется напрямую пользователем
- C) Это ускоряет доступ к полям
- D) Так удобнее для тестов

Ответ: B) LinkedList — низкоуровневый класс, который никогда не используется напрямую пользователем

---

**Вопрос 9.** Что такое PersistentList в metautil?
- A) Список, сохраняемый на диск автоматически
- B) Неизменяемая структура в духе Lisp-списков, где каждое добавление создаёт новый экземпляр со ссылкой на предыдущий
- C) Список с ограниченным числом элементов
- D) Кэшированная версия обычного List

Ответ: B) Неизменяемая структура в духе Lisp-списков, где каждое добавление создаёт новый экземпляр со ссылкой на предыдущий

---

**Вопрос 10.** На чём технически основана реализация Struct (persistent object) в metautil?
- A) На JSON-сериализации
- B) На цепочке прототипов (Object.create)
- C) На WeakMap
- D) На Proxy-объектах

Ответ: B) На цепочке прототипов (Object.create)

---

**Вопрос 11.** Какой встроенный метод JS используется для реализации immutable-варианта Struct?
- A) Object.seal
- B) Object.freeze
- C) Object.preventExtensions
- D) Object.assign

Ответ: B) Object.freeze

---

**Вопрос 12.** Чем отличаются методы fork и branch у Struct?
- A) fork создаёт полную независимую копию, branch добавляет новые поля через defineProperty, не отсоединяясь от цепочки
- B) Они полностью идентичны
- C) fork только для mutable, branch только для immutable
- D) branch удаляет объект, fork создаёт

Ответ: A) fork создаёт полную независимую копию, branch добавляет новые поля через defineProperty, не отсоединяясь от цепочки

---

**Вопрос 13.** Какую задачу поставили AI-агенту в эксперименте с динамическим именем класса?
- A) Ускорить создание класса
- B) Сделать так, чтобы constructor.name соответствовал имени пользователя, без eval/new Function/патчинга .name
- C) Добавить TypeScript-типы к классу
- D) Убрать конструктор класса

Ответ: B) Сделать так, чтобы constructor.name соответствовал имени пользователя, без eval/new Function/патчинга .name

---

**Вопрос 14.** Почему решение агента через Object.defineProperty(cls, 'name', ...) было отклонено?
- A) Оно не работает в Node.js
- B) Это добавляет новое свойство поверх класса («брендинг»), а не по-настоящему переименовывает конструктор
- C) Это слишком медленно
- D) TypeScript выдаёт ошибку компиляции

Ответ: B) Это добавляет новое свойство поверх класса («брендинг»), а не по-настоящему переименовывает конструктор

---

**Вопрос 15.** Какой нативный приём ECMAScript в итоге сработал для динамического имени класса?
- A) Использование Symbol.toStringTag
- B) Объектный литерал с вычисляемым именем ключа: `{[name]: class {...}}[name]`
- C) Reflect.construct
- D) Proxy с обработчиком get

Ответ: B) Объектный литерал с вычисляемым именем ключа: `{[name]: class {...}}[name]`

---

**Вопрос 16.** На какой версии Node.js впервые перестали падать тесты Collector после регрессии с AbortController?
- A) 20
- B) 22
- C) 23
- D) 24

Ответ: D) 24

---

**Вопрос 17.** Какая гипотеза о причине бага с AbortController в итоге прозвучала на звонке?
- A) Ошибка в бизнес-логике самого Collector
- B) Нестабильная/забагованная ранняя реализация AbortController/AbortSignal в старых версиях Node.js
- C) Неправильный порядок импортов
- D) Утечка памяти в V8

Ответ: B) Нестабильная/забагованная ранняя реализация AbortController/AbortSignal в старых версиях Node.js

---

**Вопрос 18.** Какое итоговое решение приняли по багу с AbortController в Collector?
- A) Полностью отказаться от поддержки старых версий Node.js
- B) Откатить на рабочую реализацию с setTimeout для этого файла, не трогая остальные оптимизации
- C) Переписать Collector на промисы без таймаутов вообще
- D) Форкнуть Node.js и пропатчить ядро

Ответ: B) Откатить на рабочую реализацию с setTimeout для этого файла, не трогая остальные оптимизации

---

**Вопрос 19.** Какой вывод сделал Тимур о применимости AI для диагностики платформенных багов вроде AbortController regression?
- A) AI справляется с этим лучше человека
- B) У модели нет интуиции о таких багах — нужен человеческий опыт и целенаправленный поиск по changelog'ам
- C) Такие баги невозможно найти в принципе
- D) AI всегда находит баг за одну итерацию

Ответ: B) У модели нет интуиции о таких багах — нужен человеческий опыт и целенаправленный поиск по changelog'ам

---

**Вопрос 20.** Какая дорожная карта развития стека Metarhia озвучена в конце звонка?
- A) Сразу переписать всё на Rust
- B) Стабилизировать metautil → пересадить metacom → перенести local-first/CRDT в GlobalStorage → новые релизы metacom и GlobalStorage
- C) Полностью отказаться от WebSocket в пользу HTTP/3
- D) Слить Metacom и Impress в одну библиотеку

Ответ: B) Стабилизировать metautil → пересадить metacom → перенести local-first/CRDT в GlobalStorage → новые релизы metacom и GlobalStorage
